# 全量代码审阅结果（第十一轮，2026-09-03）

七维度并行审阅全部代码 + 每条经「怀疑者/维护者」双视角独立核实（两方一致才采信）。
原始工作流 `wf6jbzud4`（59 个代理）。另有 1 条由本机实测数据得出，见文末。

**已排除、不要再提**：对外公示渠道对接、信创真机、8 类文书正文与真实裁量基准、
CaseDetailView 拆分；以及 [开发待办.md](开发待办.md) 里已记录的功能缺口。

## 缺陷 25 条

### 1. [CRITICAL] 陈述申辩弹窗从不回填却全量提交：二次保存清空已录陈述申辩/复核意见，并把 hearingRequested 重置为 false，解除 2075 听证守卫
*前端 — `frontend/src/views/case/CaseDetailView.vue:1145`*

**现状/复现**：statementForm（797 行）初值恒为 {statement:'', statementReview:'', hearingRequested:false, hearingHeldAt:null, statementWaived:false}，模板第 52 行的「陈述申辩/听证」按钮只做 dlg.statement=true，从不从 detail.notices[0] 回填；onStatement（1144-1149）把整个 form POST 到 /statement。后端 CaseService.recordStatement（CaseService.java:397-425）对每个非 null 字段无条件覆盖：`if (req.statement()!=null) n.setStatement(...)`、`if (req.hearingRequested()!=null) n.setHearingRequested(req.hearingRequested())`。真实流程：案件 NOTIFIED 期间该弹窗必然被打开多次。① 第 1 天当事人申请听证 → 打开弹窗、开「申请听证」、保存 → notice.hearing_requested=true；② 第 2 天当事人递交书面陈述申辩 → 再次打开弹窗（表单已回到空白初值）、只填「陈述申辩内容」、保存 → 随表一并发出 hearingRequested:false → 后端把 true 覆写成 false，同时 statementReview:'' 覆盖掉此前已录的复核意见。

**影响**：当事人的听证申请被静默抹掉：ProcedureService.scheduleHearing 此后报「当事人未申请听证」无法排期，而 CaseService.decide 的 2075 守卫（`notice.getHearingRequested()` 为真才拦截）随之失效——负责人可以在未组织听证的情况下直接作出处罚决定，案卷里也看不出当事人曾申请过听证。这正是 2075 守卫要防的违法情形（第41条）。附带地，statement/statementReview 被空串覆盖会造成已录法定记录丢失；且因为 statement 恒为 ''（非 null），告知满 3 日（statement_deadline_days=3）后该弹窗任何保存都必被 2046 拦死，复核意见永远无法补录。

**修法**：打开弹窗时先从 detail.value.notices?.[0] 回填（openStatement()：statement/statementReview/hearingRequested/hearingHeldAt/statementWaived 逐字段取自最新告知记录），提交时把空串归一为 null 并只发生变化字段，例如 `{statement: statementForm.statement || null, statementReview: statementForm.statementReview || null, hearingRequested: statementForm.hearingRequested || null, ...}`——后端对 null 是「不修改」语义，正好避免覆盖。

> 核实：结论成立，且被**低估**而非夸大。三处代码逐一核实无误。

【属实部分】
1. 前端确无回填：CaseDetailView.vue:52 的按钮只有 `dlg.statement = true`，全文件不存在 openStatement()；statementForm（:797）初值 `{statement:'', statementReview:'', hearingRequested:false, hearingHeldAt:null, statementWaived:false}`；onStatement（:1144-1149）整体 POST。
2. 后端确无条件覆盖：CaseService.java:406-418，`if (req.statement()!=null) n.setStatement(...)`、`if (req.hearingRequested()!=null) n.setHearingRequested(req.hearingRequested())`。Jackson 默认不把 "" 归一为 null，故 '' 恒为非 null。
3. 实际受损字段恰为 3 个：statement→''、statementReview→''、hearingRequested→false。报告没有声称 statementWaived / hearingHeldAt 被抹（后端分别只认 TRUE 和非 null），这一点描述是准确的，未夸大。

【尝试反驳，未能推翻】
- decide() 的第二道 2075（CaseService.java:495-499，按 case_hearing status='SCHEDULED' 计数）**确实**是一道独立兜底，但只在听证已排期后生效。而 scheduleHearing（ProcedureService.java:62-63）要求 `notice.getHearingRequested()` 为真，一旦被抹就抛 2039 排不了期 → 永远不会有 SCHEDULED 行 → 这道兜底永远不触发。「已申请未排期」这个窗口结构性地很宽（hearing_notice_ahead_days=7 决定了排期不可能当天完成）。抹掉后果是「听证排不了 + 决定畅通」的双输，正是 2075 要防的情形。
- 无第二个调用 /statement 的前端入口（全项目仅此一处）。
- 开发待办.md 未记录此项（:172-178 记的是「决定前置条件清单」，不同缺陷）。

【报告确有一处夸大】
「该弹窗必然被打开多次…表单已回到空白初值」不成立于同一次页面会话：statementForm 是组件级 reactive，resetCaseLocalForms() 只在 watch(route.params.id) 里调用（:1365-1371），弹窗关闭、保存成功、load() 均不复位。所以同一次坐下来反复开弹窗看到的是上次输入值，重复保存是幂等的、不破坏数据。抹除只发生在**刷新/离开再进/切案之后**——而报告自己的「第1天/第2天」场景恰好满足这一条件，故机制成立、触发条件应改述为「新的页面加载」而非「每次重开」。这与仓库对 decisionForm 已记录的同类核实（开发待办.md:178 第①点）是同一条性质。

【报告漏报的更严重一环——建议一并修】
首次保存即把 statement 写成 ''（该列可空、实体默认 null，CaseNotice.java:47-48），而 decide() 的 2076 守卫（CaseService.java:505-509）判的是 `notice.getStatement() == null`。因此**只要打开弹窗、只拨「申请听证」开关、陈述申辩文本框一字未填、保存一次**，就写入 statement=''，等于向 decide() 声明「当事人已陈述申辩」，当场解除「陈述申辩期届满前不得作出处罚决定（第41条）」。这不需要第二次保存，是同一个「空串非 null」缺陷打掉的第二道守卫。

【损害窗口与人群的准确边界】
- 仅影响 hearingEntitled=true 的案件（拟罚款 ≥ hearing_threshold，默认 10 万）——恰是 2075 最要紧的大额案。
- 破坏性保存只能落在告知起 3 日内（statement_deadline_days=3）：超期后因 statement 恒非 null，任何保存必被 2046 拦死，反而不再写坏数据。但报告的两步场景正好都在这 3 日内。
- 超期后 2046（:402-405）与听证申请期 2046（:414-417，hearing_request_days=3）双重锁死，意味着**被抹掉的听证申请在第 3 日后无法通过 UI 恢复，复核意见也永远补录不上**——报告这一点属实。

【对修复建议的修正】
notices?.[0] 取最新告知是对的（CaseService.java:779 用 findByCaseIdOrderByIdDesc）。但只做 `hearingRequested: statementForm.hearingRequested || null` 不够好：false→null 会让「撤回误勾的听证申请」永远无法操作。正确做法是打开时逐字段回填（回填后整体提交本身就安全，且保留取消勾选的能力），同时把 statement/statementReview 的空串归一为 null——后者对修复 2076 绕过是必需的，仅靠回填解决不了「首次保存写入 ''」。

> 核实：真实缺陷，确认，建议修复（但建议的补丁需修正）。

一、代码核实（逐条对上）
- 前端 CaseDetailView.vue:797 statementForm 初值恒为 {statement:'', statementReview:'', hearingRequested:false, hearingHeldAt:null, statementWaived:false}；1353 行切案复位为同一组空值。
- 模板 52 行 `@click="dlg.statement = true"`，全文件不存在 openStatement()（仅 1116 openReviewDialog / 1151 openDecide / 1256 openInstallment）。弹窗永远以空白初值打开。
- onStatement(1144-1149) 把整个 reactive 对象 POST 给 /statement。
- 后端 CaseService.java:405-419 是刻意的「null=不修改」偏更新语义：`if (req.statement()!=null) n.setStatement(...)`、`if (req.hearingRequested()!=null) n.setHearingRequested(...)`。空串非 null，false 非 null。前端契约与后端契约正面冲突。

二、发生概率：高，且比报告写的更容易触发
该弹窗是 NOTIFIED 期间录入陈述申辩、复核意见、听证申请、明确放弃的唯一入口，普通程序案件必然多次打开。报告的两日场景成立；但更低的门槛是：只要空白打开点一次「保存」，statement 即被写成 ""。

三、后果：错误数据 + 守卫失效，且静默、不可自愈（比报告所述更重）
1. hearing_requested true→false 静默覆盖 → CaseService.java:491 的 2075 守卫失效，负责人可在未组织听证下直接处罚（第41条）。
2. 无恢复路径。2075 有第二层兜底（CaseService.java:494-498 统计 case_hearing 中 status='SCHEDULED' 的行），但此处永不生效——hearing_requested 一旦为 false，ProcedureService.java:62 直接抛 2039「当事人未申请听证」，那条排期行根本无法被创建。覆盖动作恰好关上了本可兜住它的唯一那扇门。
3. 2076 守卫一并失效（报告未提）。CaseService.java:505 的条件含 `notice.getStatement() == null`；空白保存写入的 "" 非 null，该条件恒假，「陈述申辩期届满前不得作出处罚决定」被永久解除。这是一次点击即可触发，不需要听证场景。
4. 案卷里写下与事实相反的记载（报告未提）。DocumentService.java:204 按 hearing_requested 渲染「当事人已申请听证/未申请听证」进入卷内目录的告知书条目——归档文书出现虚假事实陈述。
5. statement/statementReview 被空串覆盖，已录法定记录丢失。
6. 2046 死锁属实：statement 恒为 ""（非 null）使 CaseService.java:402 的期限守卫在告知满 3 日后对任何一次保存都触发，复核意见此后永远无法补录——而复核意见天然是在陈述申辩期之后才写的。

四、十轮审阅为何漏掉
tools/e2e-case.py 的每一次调用都发稀疏 payload：657/1135 行只发 {"hearingRequested": True}，482/500/541 行只发 {"statementWaived": True}，185 行只发 statement+statementReview。E2E 验证的是 API 契约，而 API 契约本身是对的；没有任何用例走前端那种「全量默认值」payload。另可佐证维护者已识别该威胁模型：CaseService.java:371-372 在再次告知时特意把 hearingRequested=true 继承下来，并注释「否则当事人申请听证后只要以同额再告知一次，2075 守卫即失效」——次要路径已加固，主路径反而敞着。

五、改动风险与收益
收益高、风险低：纯前端局部改动，后端语义正确且已被 E2E 覆盖，不需动。仓内已有同源回填先例（openDecide 从 detail.value.notices?.[0] 取值），照抄即可；resetCaseLocalForms 已覆盖 statementForm，无需额外维护。

六、对建议补丁的一处修正（重要）
`hearingRequested: statementForm.hearingRequested || null` 在加了回填之后是错的。CaseService.java:414-417 规定：当 hearingRequested 为 true 且已过 notifiedAt + hearing_request_days（默认3日）时抛 2046「听证申请已超过期限」。回填出 true 后在第 5 天原样回传，会让此后每一次保存都被拒——用一个新死锁替换旧死锁。正确做法是提交时与回填快照逐字段做 diff，只发真正被改动的字段（而不是「只发真值字段」）；未改动的字段一律发 null，正好落进后端「不修改」语义。

### 2. [CRITICAL] 升级时用 cp 原地覆盖运行中的 jar，旧 JVM 在整个备份窗口里读的是被改写的字节
*工程与性能 — `deploy/server-build.sh:32`*

**现状/复现**：服务器执行 `bash /opt/ybcase-src/deploy/server-build.sh`。第 [4/5] 步 server-build.sh:32 `cp "$SRC"/server/target/ybcase-server-*.jar "$DEPLOY/ybcase-server.jar"` —— cp 不带 --remove-destination，是 O_TRUNC 原地改写同一个 inode，而 docker-compose.yml:31 把这个文件 bind mount 进正在运行的 app 容器（`./ybcase-server.jar:/app/ybcase-server.jar:ro`，:ro 只挡容器内写、挡不住宿主机写）。改写完成后脚本才走第 [5/5] 步 server-build.sh:44 `cd "$DEPLOY" && ./upgrade.sh`，而 upgrade.sh 的第一件事是 `./backup.sh`：一次完整 `pg_dump -Fc` 再把整个 dump 灌回 db 容器跑 `pg_restore --list` 校验（backup.sh:18-27）。库到几百 MB 时这个窗口是几十秒到几分钟。整个窗口里旧 JVM 还在对外服务，且 Spring Boot fat jar 的类与资源是**惰性**读的（LaunchedClassLoader 全程持有该 fd）。复现：升级期间请求一个当天还没走过的代码路径（如 /archive-full 卷宗合成、审计导出、某张只在特定分支用到的模板），该类的字节要从新 jar 的旧偏移量处读 → java.util.zip.ZipException / NoClassDefFoundError。

**影响**：每次升级都有一段“容器健康检查仍报 UP、但部分接口 500”的时间窗，长度等于备份耗时（随库增长而变长）。失败是随机的、只影响那些恰好还没被加载的类，日志里表现为 ZipException/NoClassDefFoundError 而不是任何业务错误，事后极难归因到部署动作。-XX:+ExitOnOutOfMemoryError 也兜不住（不是 OOM），进程不会自杀重启。

**修法**：两处改一处即可：(1) 落盘用新 inode，把 server-build.sh:32 改成 `cp "$SRC"/server/target/ybcase-server-*.jar "$DEPLOY/ybcase-server.jar.new" && mv -f "$DEPLOY/ybcase-server.jar.new" "$DEPLOY/ybcase-server.jar"` —— rename 是原子的且换 inode，运行中的 JVM 继续读旧 inode 直到 --force-recreate；(2) 或把 upgrade.sh 里的 `./backup.sh` 提到 server-build.sh 的 cp 之前，让“覆盖→重建”之间不再夹一个长任务。建议两条都做。前端 dist 也有同样的时间差（server-build.sh:35-36 先删空再拷，此时 Caddy 已在发新前端、后端还是旧 jar），一并前移。

> 核实：成立。我逐条尝试反驳，机制部分全部证实，且实测产物比结论描述的更糟。

【已核实的事实链】
1. server-build.sh:32 `cp "$SRC"/server/target/ybcase-server-*.jar "$DEPLOY/ybcase-server.jar"` — 无 `--remove-destination`。GNU cp 对已存在的普通文件走 `open(dst, O_WRONLY|O_CREAT|O_TRUNC)`，inode 不变、先截断到 0 再回写。补充一点结论没说的：**加 `-f` 也没用**（`-f` 只在 open 失败后才 unlink 重试，正常 open 成功时不触发），所以 31 行那个 `cp -f` 的写法不能被误当成已经规避。
2. docker-compose.yml:32 `- ./ybcase-server.jar:/app/ybcase-server.jar:ro` 是**单文件 bind mount**，绑的就是同一个 inode；`:ro` 只在容器挂载点上设只读，宿主机写照样穿透。compose 工作目录 = upgrade.sh 的 `cd /opt/ybcase` = `$DEPLOY`，`./ybcase-server.jar` 与 cp 的目标是同一文件，无歧义。
3. 惰性读实锤（比结论描述更强）：server/pom.xml:56 有 `repackage`，产物 server/target/ybcase-server-1.2.0.jar **66 MB / 328 条目 / 89 个嵌套 lib**，Spring Boot 3.5.5，jar 内是 `org/springframework/boot/loader/launch/*` + `loader/zip/*`（FileChannelDataBlock，pread 按偏移量惰性取字节，FileChannel 全生命周期持有）。嵌套 lib 是 **Stored 不压缩**（实测 `BOOT-INF/lib/spring-core-6.2.10.jar  Stored  0%`），即类字节直接躺在外层 jar 的固定偏移上——外层被换掉后按旧偏移读到的就是新 jar 的无关字节。而且 O_TRUNC 先归零，cp 进行中任何越过当前长度的 pread 直接短读/EOF。页缓存救不了：write 是原地更新同一批 page，MAP_SHARED 与 pread 看到的都是新内容。
4. 窗口确实夹在中间：32 行覆盖 → 34-36 dist 删空重拷 → 40-41 拷脚本 → 44 `./upgrade.sh` → upgrade.sh:9 `./backup.sh`（pg_dump -Fc，再把整个 dump 灌回容器写 /tmp/verify.dump 后 `pg_restore --list`，backup.sh:15-24，等于全量数据过两遍）→ upgrade.sh:15 `cmp -s` 再读 132 MB → 才到 upgrade.sh:27 `--force-recreate app`。期间 Caddy 照常反代，无停服、无维护页。
5. 没有别处兜住。全仓 grep `inode|原地覆盖|remove-destination|停服|维护窗口` 无任何记录，开发待办.md / 上线清单.md 均未提及。

【结论里被夸大或说错的地方（4 处，都不影响成立）】
- **“容器健康检查仍报 UP”不准确**：docker-compose.yml 只有 db 有 healthcheck（15-19 行），**app 段 22-49 行根本没有 healthcheck**；唯一的 actuator 探测在 upgrade.sh:30-37，且是 `--force-recreate` **之后**才跑。所以窗口内不是“健康检查误报 UP”，而是**根本没有任何东西在观测**——实质更糟，但陈述有误。
- **“某张只在特定分支用到的模板”举错了例子**：全仓 grep `ClassPathResource|getResourceAsStream|classpath:` 在 core/server 主源码里**零命中**，本项目运行期不从 classpath 读模板/资源。真正的惰性人口是**类**，而且量很大：hibernate-core 12 MB、byte-buddy 9 MB、tomcat-embed-core 3.6 MB、jackson-databind 1.7 MB。典型触发：某实体第一次生成 LAZY 代理才加载 Byte Buddy；第一次 multipart 上传才加载 tomcat fileupload；Jackson 每种类型的 serializer 首次序列化才构建。机制比结论写的更硬。
- **“-XX:+ExitOnOutOfMemoryError 也兜不住”是废话**：正确但与本议题无关，属于修辞填充。
- **“两处改一处即可”是错的**：方案(2)（把 backup 提到 cp 之前）只**缩短**窗口、不消除——66 MB 的原地 O_TRUNC 回写本身、dist 的 rm+cp、upgrade.sh:15 读 132 MB 的 cmp，仍然全部夹在覆盖与 `--force-recreate` 之间。真正闭合窗口的只有方案(1)。应表述为：(1) 必做，(2) 是附加收益。

【结论漏掉、但会放大严重性的两点】
- **push-update.sh:16 更糟**：`scp "$JAR" "$HOST:/opt/ybcase/ybcase-server.jar"`。scp/sftp 同样 `O_WRONLY|O_CREAT|O_TRUNC` 原地截断，等于**先把线上活着的 jar 清零，再跨 WAN 往香港灌 66 MB**，窗口是分钟级且完全取决于链路质量；随后 upgrade.sh 再叠一个备份窗口。上线清单.md:94 把这条“scp 覆盖 → docker compose up -d”写成了正式手工升级步骤。
- **万一进程真死了比 500 更坏**：`restart: unless-stopped` 会立刻把它拉起来读**写了一半的 jar** → “Invalid or corrupt jarfile” 崩溃循环，直到 cp 写完；然后它就在 backup.sh 的 pg_dump 还没跑完时**启动新代码并跑 Flyway 迁移**。upgrade.sh:8-9 “先备份再重启”的整个排序意义被击穿，回滚点变成跨迁移的撕裂快照。

【方案(1) 有个必须同改的坑，结论没提】
改成 `mv`（换 inode）后，运行中的容器**永远**不会自己看到新 jar，必须重建。脚本路径没问题（upgrade.sh:27 用了 `--force-recreate app`），但**上线清单.md:94 记的手工路径是 `scp 覆盖 → docker compose up -d`（无 --force-recreate）**：compose 检测不到配置变化就不重建 app，旧容器守着旧 inode，**升级会“显示成功但实际什么都没换”**——一个更难查的故障。这正是团队在 dist 目录上已经踩过的同一个 inode/bind-mount 陷阱（commit 41251fc、server-build.sh:33 的注释），只是方向相反，所以必须在同一次改动里把 上线清单.md:94 和 push-update.sh:16 一起改掉。

【dist 那半段：结论把两件事混为一谈，且给的药不对症】
(a) server-build.sh:35-36 先 `rm -rf dist/*` 再 `cp -r`，期间 `/srv/dist/index.html` 不存在，Caddyfile:29 的 `try_files {path} /index.html` 也救不了，Caddy 对全站 404 —— 真实但只有一两秒；顺带，删掉旧 hash 分片会让**已经打开的浏览器标签页**加载懒路由时 404。(b) 新前端配旧 jar 的版本错配持续整个备份窗口。结论建议的“一并前移”只是把这个缺口挪个位置，并不能消除 (a)；dist 要真修得先拷到 `dist.new` 再换内容（不能换目录 inode）。

判定：真 bug，值得修。优先级排序应为 push-update.sh:16 与 上线清单.md:94 ≥ server-build.sh:32，因为跨 WAN 那条路径的暴露窗口最长。

> 核实：属实，机制描述准确，但"后果"需要修正为体验/可归因性问题而非数据错误。

已逐条验证：
1) deploy/server-build.sh:32 `cp` 无 --remove-destination，GNU cp 对已存在普通文件是 O_TRUNC 原地改写，inode 不变；第31行的 `cp -f` 也不改变这点（-f 仅在目标打不开时才 unlink 重试）。
2) deploy/docker-compose.yml:32 `./ybcase-server.jar:/app/ybcase-server.jar:ro` 是单文件 bind mount，容器内外同一 inode，:ro 只挡容器内写。
3) 覆盖(:32)确实早于 `./upgrade.sh`(:44)，而 upgrade.sh:9 第一件事就是 ./backup.sh；backup.sh:15-24 先 pg_dump -Fc 落盘，再把整个 dump 经 docker compose exec -T 的 stdin 完整回灌一遍跑 pg_restore --list —— 两趟全量 I/O。
4) 窗口长度的前提成立：V9__v11_formalization.sql:70 中 attachment_storage 默认 'DB'，扫描件/音像证据以 bytea 存库，库随办案量增长，dump 随之变大。当前 fat jar 67MB，Spring Boot 3.5.5 loader 全程持有外层 jar fd、按启动时缓存的偏移量惰性读取嵌套条目，冷类（rarely-used 端点、springdoc、导出/打印分支、未触及的三方库类）在窗口内被读到旧偏移即 ZipException/NoClassDefFoundError。

发生概率：条件性。需要(a)窗口内有流量 (b)该流量触及尚未加载的类。夜间无人使用则为 0；工作时间部署则"每次升级至少一次 500"在几十个百分点量级，且随库增长而上升。自我掩盖性强：健康检查照报 UP，日志只有 ZipException 无业务上下文，事后容器已重建。

后果修正（报告此处判断有误）：不是错误数据。类加载失败抛 NoClassDefFoundError/ClassFormatError（Error 子类）或 ClassNotFoundException，Spring 默认回滚规则覆盖 RuntimeException 与 Error，所有写路径均走 @Transactional service（CaseService/ProcedureService/ExecutionService 等），失败即回滚；报告举例的 /archive-full 本身是只读 GET（FormalizationController.java:280）。库里不会留半截数据。代价是升级期间随机接口 500 + 事后极难归因。

改动权衡：只采纳建议(1)，不做(2)。
- (1) `cp` 到 .new 再 `mv -f`：同目录同文件系统 rename 原子且换 inode，单文件 bind mount 钉住旧 inode，运行中 JVM 一直读旧 inode 直到 upgrade.sh:27 的 --force-recreate 重建容器重新解析路径。仓库本身已懂这套语义——server-build.sh:33 对 dist 目录用的正是同一规则的反面（要保住 inode）。附带消除 67MB 拷贝过程中的撕裂窗口，且构建中途失败不再把截断 jar 留在生效路径。风险极低、两行改动、无新失败模式。
- (2) 前移 backup.sh 代价不对等：它由 upgrade.sh:9 调用，而 upgrade.sh:4 明确是可独立执行入口。前移意味着每次部署跑两遍 pg_dump，或加跳过开关把契约拆到两个脚本。有了(1)后顺序已不重要。

侧重修正：前端 rm -rf 到 cp -r 的空窗因 dist 仅 1.4MB 实为亚秒级，非主要问题；每次升级必然发生的是"Caddy 已发新前端、后端仍旧 jar"持续整个备份窗口的版本错配，前移也解决不了，属独立条目。另 upgrade.sh:47 打印的回滚命令 `cp ybcase-server.jar.prev ybcase-server.jar` 是同样的原地覆盖形状，只是紧跟 --force-recreate 故窗口约 1 秒，为一致性可一并改为 .new + mv -f。

### 3. [HIGH] 改用户：口令策略校验失败仍把姓名/科室/角色写库（@Transactional 未回滚）
*core 模块 — `core/src/main/java/cn/ybcase/core/web/SysUserController.java:73`*

**现状/复现**：admin 登录 → 用户管理 → 编辑 banban → 角色改勾「系统管理员(ADMIN)」，重置密码填 "123" → 保存。界面红字「密码不能少于 8 位」，弹窗不关（client.ts:83 对 code!=0 reject）。关闭弹窗刷新列表：banban 的角色已经变成 ADMIN，只有口令没改。原因：update() 第 73 行先 applyFields(u, req) 改了托管实体（realName/title/deptId/phone/roles 全在里面），第 74-81 行才做 passwordPolicyError 校验；方法带 @Transactional，第 76 行 return R.fail(1102) 不抛异常 → 事务照常提交，Hibernate 脏检查把改动全部 flush（并非只有走 save() 才落库）。create() 是先校验后赋值，两处口径不一致。

**影响**：管理员看到的是「保存失败」，实际角色/科室/姓名已变更——最坏情况是无意中把 ADMIN 角色授出去而操作者以为没生效；审计日志里这条 PUT 记的是 http_status=200，事后也看不出发生过变更。

**修法**：把 passwordPolicyError(req.password()) 的判断整体前移到 applyFields 之前（与 create() 一致），或在 return R.fail 前调 TransactionAspectSupport.currentTransactionStatus().setRollbackOnly()。顺带修 frontend/src/views/system/UsersView.vue:49 的占位符「至少 6 位」——后端策略是 8 位且含字母与数字。

> 核实：成立，四条证伪路径全部失败，但安全定性应降一档。核实链：(1) @Transactional 在控制器上确实生效——BureauServerApplication 是标准 @SpringBootApplication(scanBasePackages="cn.ybcase")+@EnableJpaRepositories，JPA 自动配置装配 JpaTransactionManager，类非 final、方法 public，CGLIB 正常代理。(2) 「不 save() 就不落库」被证伪：findById 返回托管实体，applyFields 的 realName/title/deptId/phone 五个 setter 与 roles.clear()/add() 全是脏检查目标；roles 是 @ManyToMany(fetch=EAGER)+@JoinTable(sys_user_role)（SysUser.java:76-81），清空重加在 commit flush 时产生 delete+insert。决定性佐证是 application.yml:17 open-in-view: false——唯一持久化上下文就是这个事务边界，它 commit 就是写库。(3) 「别处兜住」被证伪：全仓 grep setRollbackOnly|TransactionAspectSupport|rollbackFor 零命中；BureauExceptionAdvice 只处理抛出的异常，return R.fail 走不到它；测试只有 GuardIntegrationTest/WorkdaysTest，无 /system/users 用例；前端 UsersView.vue:44 的 <el-form> 没有 :rules，弱口令必然打到后端。(4) 「属统一风格」被证伪且反坐实：create()(54-64)、AuthController.changePassword(85-92)、SysRoleController.grant(52-58) 全部「先校验后改实体」，update() 第 73 行是全仓唯一逆序。额外加强证据两条（原结论未提）：UsersView.vue:145 reject 后 await load() 被跳过，表格仍显示旧角色，形成「保存失败+表格没变」双重假象；幽灵提交会递增 @Version rowVersion(SysUser.java:66)，并发编辑者会因一次"失败"保存吃到 2102 乐观锁冲突。夸大之处三点：①「无意中把 ADMIN 授出去」偏重——端点是类级 @PreAuthorize("hasRole('ADMIN')")，触发者必是管理员本人且 ADMIN 是他自己勾的，非 ADMIN 无法利用，越权面为零，应按 MEDIUM 数据一致性/UX 缺陷记而非安全高危；②「事后看不出发生过变更」偏重——http_status=200 属实（BIZ_CODE_ATTR 只由异常通道写入），但审计表本来只记 method/path/status，成功改角色也同样看不出改了什么，日志骗的是"界面报了失败"而非变更本身；③触发面窄，仅 PUT 且 password 非空白才走这条路，纯改角色（口令留空）无 bug。行号小偏：占位符「至少 6 位」在 UsersView.vue:50（49 是 <el-input 开标签），client.ts 的 code !== 0 在第 67 行；占位符与后端 8 位+字母+数字策略（SysUserController.java:99-103）不符属实。修法首选把 passwordPolicyError 前移到 applyFields 之前并保留「空白=不改密」语义，优于 setRollbackOnly，因为与 create() 口径一致且不依赖调用方记得事务语义。

> 核实：真实缺陷，报告的机理、影响面和审计后果全部核对无误，建议采纳（附一处实现注意点与一处报告表述订正）。

【机理已逐行核实】
1. SysUserController.java:69 update() 带 @Transactional；:71 findById 取回的是事务持久化上下文中的托管实体（application.yml 里 open-in-view=false 不影响这一点——脏检查发生在事务提交的 flush，与 OSIV 无关）。
2. :73 applyFields(u, req) 先行改写托管实体：realName/title/deptId/phone，且 roleCodes 非 null 时执行 u.getRoles().clear() + 重新 add（:105-115）。SysUser.java:67-72 roles 是 @ManyToMany(fetch=EAGER)，会产生 sys_user_role 的 delete+insert。
3. :74-76 才做口令策略校验。R.java 里 fail() 只是构造返回对象、不抛异常；全仓 grep "setRollbackOnly|currentTransactionStatus" **零命中**，也没有任何按 R.code 回滚的切面。故 return R.fail(1102) → 事务正常提交 → Hibernate 脏检查 flush 出 UPDATE sys_user 与 join 表增删。第 82 行的 save() 在这里对"是否落库"不起决定作用，报告说的"并非只有走 save() 才落库"是对的。
4. create()（:53-66）确实是先校验、且操作的是 new 出来的瞬态实体，两处口径不一致成立。setEnabled() 也是先校验后改，无同类问题。全仓扫描 @Transactional 中"先改托管实体后 R.fail"的模式，只有 AuthController.java:49-57 另有一处，但那是登录失败计数必须持久化，属有意为之，不是缺陷。update() 是唯一一例。

【审计后果核实：报告正确】
BureauAuditFilter.java:63-64 只有在 request attribute BIZ_CODE_ATTR 存在时才把业务码并入 http_status 列，而全仓唯一设置该 attribute 的是 BureauExceptionAdvice（针对抛出的 BizException）。控制器直接 return R.fail 不会设置它，所以这条 PUT 在 sys_audit_log 里就是 http_status=200；且该表只有 username/method/path/http_status/client_ip/request_id，不留请求体与前后值。事后确实无法从审计里分辨"失败的编辑"和"成功的编辑"。

【前端放大了后端缺陷】
UsersView.vue:49 占位符 form.id ? '留空则不修改' : '至少 6 位'，而后端策略是 ≥8 位且同时含字母与数字（:98-103）。这个提示实际上在诱导管理员输入 6 位数字口令，从而显著提高踩中后端缺陷的概率——两个问题是耦合的，应一起修。save()（:141-147）PUT 的是整个 form，编辑时 roleCodes/realName/title/deptId/phone 必然随请求发出；client.ts 对 code!=0 reject，导致 dialogVisible=false 与 load() 都不执行，弹窗不关、列表仍显示改前的旧值——与复现描述完全一致。SysUserController 没有任何测试覆盖（grep 测试目录零命中）。

【发生概率】需要三件事同时成立：(a) 编辑既有用户（非新增），(b) 至少改了一个非口令字段，(c) 口令不合规。(c) 是高频的——管理员替同事重置口令的第一反应就是 123456/888888，而弹窗自己写着"至少 6 位"；(a)+(b) 正是入职/调岗的常规流程，改角色科室和重置口令本来就在同一个弹窗里一次提交。绝对频次不高（一个市局用户管理是低频操作），但一旦发生这类操作，首次提交命中该路径的条件概率不低。
需要指出一个减轻因素：最常见的补救动作是立刻重输合规口令再保存，结果状态与预期一致、无残留。真正留下脏数据的是"看到失败就放弃/关闭弹窗"这一支——也正是报告描述的场景（本想授 ADMIN，看到报错改主意关掉弹窗，角色却已授出）。

【后果性质】是错误数据，不是体验问题。角色变更（尤其 ADMIN——它把着立案、用户管理、审计导出等口子）在界面明示"保存失败"的情况下静默落库，列表不刷新看不出来，审计里永远看不出来。对一个已试运行、按等保要求留痕的执法系统，"权限被静默授出且无痕"是后果形态里最坏的一种。

【风险收益】修复是纯语句顺序调整：把口令校验（连同 encode/passwordUpdatedAt/tokenVersion 的计算）提到 applyFields 之前，成功路径行为完全不变，不动表结构、不动接口契约、不动前端调用，风险极低。收益是消除一个静默越权授予。相比之下 setRollbackOnly 是次选：能止血，但把"先改后校验"的顺序留在原地，下一个改这个方法的人还会踩。建议按建议一（前移）修，并补一个"改用户+弱口令 → 断言角色未变"的集成测试，同时把 UsersView.vue:49 的占位符改成"至少 8 位，含字母和数字"。

【一处实现注意点，报告未提】不能照搬 create() 的写法：update() 里口令为空表示"留空则不修改"，校验必须仍然包在 req.password() != null && !req.password().isBlank() 的条件内，只是把这整个条件块提到 applyFields 之前；直接无条件前移会让"不改口令的编辑"全部报 1102。

【一处表述订正】报告说"只有口令没改"——准确说是口令、passwordUpdatedAt、tokenVersion 三者都没改，其余字段全改了；不影响结论。

### 4. [HIGH] 内置 admin 的 ADMIN 角色可被摘除，摘完全系统无人可管理（与第 90 行停用守卫不自洽）
*core 模块 — `core/src/main/java/cn/ybcase/core/web/SysUserController.java:111`*

**现状/复现**：admin 登录 → 用户管理 → 点 admin 行的「编辑」（UsersView.vue:31 只对「停用」按钮做了 v-if 隐藏，编辑按钮对 admin 行照常显示）→ 角色多选框清空或改成 HANDLER → 保存，返回 200 成功。此后全部 hasRole('ADMIN') 接口（/api/system/users、/api/system/roles、/api/system/depts、/api/config/{key}、/api/audit/logs 及 server 侧管理接口）一律 403。BureauUserInitializer:28 只在 userRepository.count()==0 时建账号，重启不会补回 ADMIN；系统里没有第二个 ADMIN 账号（种子只建了 admin/banban/fazhi/juzhang 四个，仅 admin 是 ADMIN）。

**影响**：一次误操作即让系统进入不可管理状态，只能直连 PostgreSQL 执行 SQL 往 sys_user_role 补行才能恢复——试运行现场没有 DBA 就得停摆。setEnabled 第 90 行已经守住了「不能停用内置管理员」，同一后果的另一条路径却完全没守。

**修法**：在 update()/applyFields() 加同源守卫："admin".equals(u.getUsername()) 时 roleCodes 必须含 ADMIN，否则 R.fail(1105,"不能移除内置管理员的 ADMIN 角色")；更稳妥的做法是保存后校验「拥有 ADMIN 角色的启用账号数 >= 1」，否则拒绝。前端同时对 admin 行的角色选择框置灰。

> 核实：成立，四条证伪路径全部失败，且描述总体偏保守而非夸大。

反驳尝试与结果：
1. 别处有没有同源守卫？没有。全仓 grep `"admin".equals` / `内置管理员` 只命中 SysUserController.java:90 一处。SysRoleController.java:54 有 `if ("ADMIN".equals(role.getCode())) return R.fail(1302,"内置管理员角色不可修改授权")` —— 但它守的是「ADMIN 角色自身的权限集」，不是「用户↔角色的挂接」。这条反而加强结论：作者已在两处（1104 停用、1302 改授权）刻意加固 ADMIN，唯独 applyFields 这条路没守，属遗漏而非有意设计。
2. 安全层有没有硬编码 admin 兜底？没有，而且比报告说的更糟。DbUserDetailsService.java:23-24 权限完全来自 `user.getRoles()`；JwtAuthenticationFilter.java:34 每个请求都 `loadUserByUsername` 重新查库取权限。所以摘掉角色后不需要重新登录，**当前在线 admin 下一个请求即 403**，无任何缓冲期可供反悔。报告写「此后一律 403」还低估了即时性。
3. 数据库层有没有约束/触发器/种子兜住？没有。V1__bureau_core.sql:48 只是普通 join 表，全仓无 trigger；全部迁移中无 `insert into sys_user`，所以 BureauUserInitializer.java:28 的 `count()==0` 重启后永远为假，确实不会补回 ADMIN。四个种子账号只有 admin 是 ADMIN，属实。
4. 有没有恢复工具？没有。tools/*.py、deploy/*.sh（含 upgrade.sh/backup.sh）无一处触及 sys_user_role。「只能直连 PostgreSQL 补行」属实。

前端复现链完整：UsersView.vue:31 编辑按钮无 v-if（对比 :32 停用按钮有 `v-if="row.username !== 'admin'"`）；openEdit 整行 `Object.assign(form, {...row})` 带出 roleCodes；:66 的 `el-select multiple` 无任何 disabled 绑定；save() 直接 PUT 整个 form。清空即 `roleCodes: []`，后端 `req.roleCodes() != null` 为真 → clear() 后无一 add → 零角色落库。

需要修正的两点（都不推翻结论）：
- 影响面报告写窄了。403 的实际范围还包括 CauseController:27（案由维护）、BureauLawController:40/52（法规维护）、FeedbackController:76/100/142、FormalizationController:116/237（文书模板）、ProcedureController:111、WorkbenchController:86。
- 「系统停摆」需限定：`hasAnyRole('LEADER','ADMIN')` 的立案/中止/终止/延期/审查决定/结案，以及 LEGAL 的法制审核，靠 juzhang/fazhi/banban 仍可运转。死的是整个系统管理面（用户/角色/科室/参数/审计/案由/法规/模板），案件业务本身不会立刻停。结论仍是「不可管理」，但试运行现场不至于当天办不了案。

另发现一条更廉价的同后果路径，建议一并纳入修复：applyFields:112-113 用 `findByCode(code).ifPresent` 静默吞掉未知 role code，且全程无「至少一个角色」校验——传个拼错的 code 或干脆不传，任何账号都能被存成零角色。所以除了 admin 专项守卫外，应在 create()/update() 统一加「roleCodes 解析后不得为空」+「保存后拥有 ADMIN 的启用账号数 >= 1」两道校验。

> 核实：属实，且比报告描述的还稍严重一点。读码确认三点：

1）缺陷成立。SysUserController.applyFields() 第 110-114 行无条件 `u.getRoles().clear()` 后按 roleCodes 重建，对 username=admin 无任何守卫；同文件第 90 行 setEnabled 已守住「不能停用内置管理员」(1104)，SysRoleController:54 还守住了「内置管理员角色不可修改授权」(1302)——说明该不变量是代码本意，只是漏了 user↔role 这条边。前端 UsersView.vue:31 只对「停用」按钮做了 v-if，「编辑」对 admin 行照常渲染，openEdit 把 roleCodes 灌进表单、save 整体 PUT，角色多选框对 admin 行始终可改且始终生效。复现路径准确。

2）锁死是终局性的，四条退路全部堵死：JwtAuthenticationFilter 每次请求经 DbUserDetailsService 从库里重新装载权限，摘除下一跳即生效，没有令牌宽限窗口可用来自救；V1__bureau_core.sql 只种角色/菜单不种 sys_user，唯一建号入口 BureauUserInitializer 卡在 count()==0，重启不补；SysUserController 无 @DeleteMapping，删不到 0 让初始化器重跑；SecurityConfig 只放行 /api/auth/login、/api/config/public、/actuator/health、swagger，无自注册无引导修复口。故「只能直连 PG 补 sys_user_role」的判断成立。影响面：用户/角色/科室、/api/config、审计查询、案由维护(CauseController:27)、法规维护、反馈处置、ProcedureController:111、WorkbenchController:86 全部 403；HANDLER/LEGAL/LEADER 的办案链路仍可用，属管理面停摆而非全系统停摆。

3）报告漏了两处同源问题，应一并修：
- update() 带 @Transactional，第 73 行 applyFields 先落到托管实体，第 74-76 行密码策略不合规才 return R.fail(1102)。R.fail 是正常返回不触发回滚，脏检查照样提交——用户看到「密码须同时包含字母和数字」的失败提示，ADMIN 角色其实已经被摘掉了。这条让误操作的触发面更宽。
- 按 username 守只是次优解：它挡不住摘除第二个 ADMIN 账号的角色，setEnabled 的 1104 同样只认字面量 admin。建议直接落报告里那个更稳妥的方案——在 update()/setEnabled() 提交前校验「enabled 且持有 ADMIN 的账号数 ≥ 1」，否则 R.fail(1105)，一把守住两条路径。

概率与后果权衡：不是随手可触发（多选框无 clearable，须刻意点掉 ADMIN 标签的 × 或改选其他角色），但种子 admin 的科室/手机号/岗位都是空的，试运行布场时管理员必然会打开这个编辑框去补，角色框就在同一弹窗里、离误触一步。后果不是脏数据也不是体验问题，是可用性硬故障：现场无 DBA 即停摆，且系统自身无法自愈。改动风险极低——新增一个保存前校验加前端置灰，不触碰任何既有流程与法定守卫，纯增量。收益远大于风险，建议修。

### 5. [HIGH] 期限扣除的结束日期不限未来，一次即可凭空延长办案期限180日，绕过延期审批上限
*核心服务 — `server/src/main/java/cn/ybcase/bureau/service/CaseService.java:204`*

**现状/复现**：POST /bureau/cases/{id}/exclusions {reason:"HEARING", startAt: 今天, endAt: 今天+179}。addExclusion 只校验了 startAt 不得早于立案日、不得晚于今天（200-203行），对 endAt 仅校验 ≥ startAt 且区间跨度 ≤ exclusion_max_days（默认180），**没有 endAt 不得晚于今天的校验**。该行落库后 end_at is not null，立即被 totalExclusionDays()（893行）与 detail() 的 sum(end_at - start_at)（761行）计入，effectiveDeadline = deadline_at + 179。前端 CaseDetailView.vue:453 的结束日期 el-date-picker 同样不禁未来日期，且该接口在 CaseController:146 无 @PreAuthorize，普通办案员即可调用。

**影响**：直接击穿第45条办案期限体系：extend() 的 2009 守卫把首次延期限制在30日、累计90日，且继续延期必须有签字确认的集体讨论记录（273-281行），而这里一条无需任何审批的记录就能拿到最多180日——是全部法定延期额度的两倍。decide() 的 2008 超期守卫（540-542行）随之失效，超期案件可以正常下决定，办案期限看板/督办提醒（MessageService:65-77、BureauStatsController:64）也一并被洗白。addExclusion 自己198行的注释写明了这正是要防的事（"期限扣除会直接推后办案期限，是绕开延期须负责人批准且累计≤上限的口子——须严格校验区间"），且同一仓库里自动登记扣除的 OversightService.endExpertReview:224 明确有 `if (end.isAfter(LocalDate.now())) throw 2066 "评审结束日期不得晚于今天"`——手工登记这条漏了，同一概念两套口径。

**修法**：在 205 行的 endAt 非空分支里补一条与 startAt 对称的上界校验：`if (req.endAt().isAfter(LocalDate.now())) throw new BizException(2032, "结束日期不得晚于今天；扣除事由尚未结束的请留空结束日期，事由结束后再补登");`。留空 end_at 的进行中扣除本来就不计入期限（895行 `end_at is not null`），语义正好对应"扣除还没结束"，不需要额外改动。

> 核实：成立，但建议的修法有误，不能照抄。

【核实：漏洞属实，且无人兜住】
CaseService:200-212 对 startAt 有双边界（≥filed_at 且 ≤today），对 endAt 只有相对界（≥startAt、span≤exclusion_max_days）。逐一排除了兜底：
- V2__bureau_case.sql:113-120 建表无任何 CHECK 约束，end_at 可空可为未来；
- 无跨记录累计上限，仅单条 180（V13 种子确为 '180'）；
- CaseController:146 确无 @PreAuthorize；CaseScopeInterceptor 只做 case_view_scope 数据范围，不管角色 → 承办办案员本人即可调用；
- 该值无过滤地喂给五处消费方：decide() 2008（539-542）、detail().effectiveDeadline（782）、MessageService:65-77、BureauStatsController:64/68、WorkbenchController:34；
- 与 OversightService.startExpertReview:205 / endExpertReview:224 的"不得晚于今天"两套口径，属实。
requireActive 允许 INVESTIGATING/REPORTED/NOTIFIED，正是 decide() 前的状态，可在下决定前临门一脚登记。

【描述的夸大处（轻微）】
1) 上限是 179 日不是 180：span=end-start+1≤180，而 totalExclusionDays 与各处 SQL 算的是 end-start，故最多 +179。作者正文的复现写了 179，标题写 180，口径不一。
2) 不可叠加：已存在 [today, today+179] 后，任何 startAt≤today 的新记录都会被 213-218 的重叠校验拒掉，天花板就是一次性 +179，非无限。仍是法定延期累计上限 90 日的约两倍，结论方向不变。
3) "一并被洗白"略重：该记录会渲染在前端"期限扣除"页签（CaseDetailView.vue:202-205），事后可查；不过 DocumentService.timeline 确实不含扣除，大事记里看不到。

【对"建议"的实质反驳：照建议改会造成死锁，且不是一行的事】
1) 无法补登 = 死锁。全仓 exclusions 只有 @PostMapping，没有任何 PUT/PATCH/结束扣除接口，前端也只有"登记扣除期间"一个入口。按建议的提示语"留空结束日期，事由结束后再补登"操作：留空的记录永远关不掉；而事后补登同一区间会被 213-218 的重叠校验拒绝——因为 coalesce(end_at, current_date) 让这条未结束记录与任何截止到今天的区间都重叠，2032 永久拒。作者说"留空语义正好对应、不需要额外改动"，恰恰相反：必须同时补一个"结束扣除"接口（照 endExpertReview 的样子），或把 end_at is null 的记录排除出重叠判断。
2) 会打断现有 E2E，且不止一处。tools/e2e-case.py:161-165 对当天立案的案件登记 APPRAISE [today, today+5] 并断言 effectiveDeadline == today+95；加了上界后，当天立案的案件只能 startAt=today 且 endAt≤today，任何扣除都恒为 0 日，该步骤在 API 层变得无法表达。1178-1182 的重叠拒绝用例也依赖那条被接受的未来结束记录做前置。
3) 留空路径无提醒。MessageService 的办案期限那段只统计 end_at is not null，且全仓没有针对 end_at is null 的任何提醒分支，如实留空的办案员得不到任何补登提示，反而会被 2008 误拦。

【结论】守卫该补，但正确修法是：补 endAt 上界 + 新增结束/补登扣除接口（或放宽重叠校验对未结束记录的判定）+ 改 E2E 三处 + 补未结束扣除的督办提醒。按原文那一行改上去，会把一个可绕过的守卫换成一个办案员出不来的死循环。

> 核实：真缺陷，确认属实，建议照收，另有两点补充。

【已核实】CaseService.java:199-211 中 startAt 有上下界（不早于立案日、不晚于今天），endAt 只有 ≥startAt 和 span≤exclusion_max_days(180) 两条，确无上界；V2__bureau_case.sql:113 建表也无 CHECK 约束。落库后 end_at is not null 即刻被四处口径同时采信：totalExclusionDays()(893)、detail() 的 sum(end_at-start_at)(761)、MessageService:65-77、BureauStatsController:64。CaseController.java:146 确无 @PreAuthorize（仅有 CaseScopeInterceptor 按路径变量限本人范围内案件——即"受益人本人"）。OversightService.java:224 确有 `if (end.isAfter(LocalDate.now())) throw 2066 "评审结束日期不得晚于今天"`，同一概念两套口径成立。前端 CaseDetailView.vue:451/454 两个 el-date-picker 均无 :disabled-date，且 795 行表单默认 startAt=endAt=today。

【一处订正】上限是 179 日不是 180：span=DAYS.between+1≤180，而 totalExclusionDays 按 end-start 计，故单条最多推后 179 日。不影响结论（对比首次延期 30 日、累计 90 日的法定额度，仍是全部额度的两倍）。另：想靠多条叠加更多也不行——第二条的 startAt 落在未来会被 202 行拦下，所以敞口就是这一条约 179 日。

【报告未提、且抬高了严重度的一点】case_period_exclusion 是只进不出的表：CaseController 上无 DeleteMapping/PutMapping，全仓库只有 CaseService:219 与 OversightService:245 两处 insert，没有任何 update/delete。也就是说填错的未来结束日期无法更正、无法作废，错误的期限数字永久固化在案。这也意味着修复上线前应先跑一次存量排查（select 出 end_at > current_date 的行），试运行库里若已有此类数据，只能走 DBA 手工订正。

【发生概率】高，而且主要不是恶意路径而是误录路径。公告送达期（ANNOUNCE，60 日）和委托鉴定（APPRAISE，通常约定完成期限）这两类事由的时长在开始时就是已知的，办案员在登记当下顺手把"预计结束日期"填成未来日期，是完全自然的操作；弹窗两个日期都默认今天、无任何提示，也不阻拦。恶意路径则是一次 POST、无角色门槛、无审批留痕（不写 biz_approval），随时可用。

【后果】错误数据且带法律后果，不是体验问题。有效期限被提前虚增最多 179 日，decide() 的 2008 超期守卫（540-542）随之失效，实际已超期的案件能正常下决定——超期办案在复议诉讼和案卷评查里是程序违法点；更麻烦的是督办看板与临期提醒读的是同一个被污染的数字，本该发现此事的监督机制恰恰被一并静音，靠人工巡检基本发现不了。

【改动风险与收益】收益远大于风险。补的是一行、与 203 行 startAt 守卫完全对称、与已上线的 OversightService:224 规则字面一致，2032 码复用现有分支。留空 end_at 的语义早已成立：761/893 两处都带 `end_at is not null`，213-216 的重叠校验也已 `coalesce(end_at, current_date)`，无需任何配套改动。唯一的行为变化是"预登记已知未来结束日"的用法要改成事后补登，而这恰恰是正确的法定口径——只有实际已经过去的期间才谈得上不计入。建议按原文采纳该行校验，并注意三点：错误文案务必保留"尚未结束的请留空结束日期，事由结束后再补登"的引导，否则用户会卡在这里不知道怎么办；同步给前端两个 el-date-picker 补 :disabled-date 上界（后端为准、前端只为少一次往返）；上线前补一条存量排查。

### 6. [HIGH] 没收违法所得金额不计入任何"数额较大"阈值，罚款填0+巨额没收可跳过听证告知、法制审核、集体讨论
*核心服务 — `server/src/main/java/cn/ybcase/bureau/service/CaseService.java:385`*

**现状/复现**：对某定点机构 notify() 时 proposedFine=0，随后 decide({decisionType:"PUNISH", fineAmount:0, confiscateAmount:5000000, discretionReason:"…"})。逐条看守卫：(1) notify 385行 hearingEntitled 只比 proposedFine 与 hearing_threshold_* → false，当事人从未被告知听证权，之后 recordStatement 411行还会以 2039 拒绝其听证申请；(2) decide 511-513行 needReview 只比 fine 与 legal_review_fine_threshold，且 hearingHeldAt 为 null → 不触发 2005 法制审核（legal_review_mode 默认 THRESHOLD）；(3) decide 528-533行 2047 集体讨论阈值同样只比 fine → 不触发。NoticeReq（351行）里根本没有 proposedConfiscate 字段，所以没收额在告知阶段完全不可见。

**影响**：没收违法所得是行政处罚法第9条第(三)项的独立处罚种类，第63条把"没收较大数额违法所得"与"较大数额罚款"并列为听证范围。系统对 confiscateAmount 已有完整建模——CaseDecision:40 存储、ExecutionService:60 按 CONFISCATE 入账并封顶、fullyExecuted:913 计入结案条件、DocumentService:225 写进决定书、BureauStatsController:34 计入统计——唯独三个法定阈值守卫看不见它。结果是一份数百万元的没收决定可以不告知听证权、不经法制审核、不经集体讨论就作出，程序违法且属复议诉讼中的必撤事由；而同样金额若填在 fineAmount 里三道守卫全部触发，同一"数额较大"概念在处罚种类之间口径不一致。（追回基金 recoupAmount 不计入是对的——责令退回不是处罚，没收和它不是一类。）

**修法**：三处阈值统一改为按"处罚性金额"比对，即 fine.add(nz(confiscateAmount))：notify 的 NoticeReq 增加 proposedConfiscate 字段并在 385 行参与 hearingEntitled 判定（同时纳入 363-364 行 2077 的"不得加重"比对与 481-489 行 decide 的 minFine 比对），decide 512 行 needReview 与 532 行集体讨论阈值同样用 fine+confiscate。若担心口径由局方定，可加 `discretion_amount_includes_confiscate` 开关，默认 true。

> 核实：成立，且未被别处兜住；描述基本准确，只有一处复现细节需修正，另有两个漏报的同口径站点。

【逐条核实】
1. 听证（最硬的一条）。`C:\Users\drxuj\Projects\医保案件\server\src\main\java\cn\ybcase\bureau\service\CaseService.java:385` 是 `hearingEntitled` 在全仓的**唯一写入点**（grep `setHearingEntitled` 仅此一处），只比 `proposedFine`，没有任何人工置位的后门。`NoticeReq`（351行）确无 `proposedConfiscate`，前端告知弹窗（`frontend/src/views/case/CaseDetailView.vue:475-483`）也只有"拟罚款金额/拟追回基金"两个数字框，提示语原文就是"拟罚款达到听证标准将自动告知听证权利"——设计层面就是罚款单口径。随后 411 行以 2039 拒绝当事人听证申请，属实。
2. 法制审核。512 行 `needReview` 只有 fine / hearingHeldAt / mode 三个来源，默认 THRESHOLD + fine=0 → 不触发 2005，属实。
3. 集体讨论。529-533 行 `meetingThreshold` 只比 fine，属实。
4. 兜底排查全部落空：无任何守卫用 `amount_involved`（`OversightService:115-126` 只拿它做裁量建议档位，不设卡）；`ExecutionService:56-60` 只对没收做入账封顶；`MessageService:103`、`BureauStatsController:148` 只做缴款催办；控制器层无额外校验。没有第二道网。

【比审阅结论更严重的一点】`2007 不得加重` 的比对（481-489行）完全不含 confiscate，而告知端又无该字段可比——所以决定书里的没收额不是"阈值看不见"，是**相对告知书完全不受约束**：告知 0，决定 500 万没收，合法通过。审阅把这点写在"建议"里，其实它本身就是独立缺陷。

【审阅漏掉的两个同口径站点，必须一起改】
- `server/src/main/java/cn/ybcase/bureau/service/DocumentService.java:166-173` `legalReviewRequired()` 同样只比 fine，其注释自称"与 CaseService.decide() 的 needReview 同一套口径……两处若各写一遍，任何对审核范围的调整都会让'能不能决定'与'卷齐不齐'再次漂移"。只改 512 行会精确制造它警告的那种漂移（决定要审核，卷内却不把 LEGAL_OPINION 计入必备）。
- `server/src/main/java/cn/ybcase/bureau/web/BureauStatsController.java:92-97` `govRecordMissing`（重大处罚决定未报政府备案）也只比 fine_amount，500 万没收案永不上榜。这条依 辽54条 字面确系"罚款"，可列为低优先级/随开关走。

【描述需要修正的一处】复现路径不是"一路畅通"：`statement_wait_required` 默认 true + `statement_deadline_days=3`，2076 会挡到陈述申辩期届满或录入陈述/放弃留痕为止。这是时间闸不是金额闸，录一条"当事人放弃"即过，不改变结论，但"当天即可作出"的说法不准确。

【不是理论构造】种子法条 `server/src/main/resources/db/migration/V4__documents_archive.sql:102` 的 WC103（基本医疗卫生与健康促进法第103条）基础罚则就是"责令改正，没收违法所得"，罚款只在"情节严重"时并处——即 fine=0 + 巨额没收正是该案由的法定常态形态。系统自带的种子数据就包含这种案件形状。测试侧零覆盖：server/src/test 与 E2E 全文无 confiscate。

【法律依据强度需分档，别一刀切】听证一条最硬：行政处罚法第63条第(二)项把"没收较大数额违法所得"与"较大数额罚款"并列，罚款单口径直接违法，这一条不宜藏在开关后面（或开关默认 true 且不建议关）。法制审核与集体讨论两条偏口径：本仓 `legal_review_fine_threshold` 的库内注释自定义为"罚款数额较大"，辽54条字面也是"较大数额罚款"，纳入没收要靠第57条第2款"较重的行政处罚"/"重大执法决定"的解释，属局方口径，正适合 `discretion_amount_includes_confiscate` 开关。

【recoup 的排除】审阅的理由正确——责令退回是行政命令不是处罚种类。补一点：recoup 已参与 2007 不得加重比对，所以把 confiscate 加进同一处比对是与既有写法一致的，不是新增概念。

【影响措辞】"必撤事由"略重但方向对：剥夺法定听证告知在复议诉讼中通常导致撤销，情节重的可落入第38条无效。可保留。

> 核实：Real bug, confirmed line by line. NoticeReq (CaseService.java:351) has no confiscate field and case_notice (V2__bureau_case.sql:138-149) has no proposed_confiscate column, so 385行 hearingEntitled compares proposedFine alone; 487-489行 2007 不得加重 compares fine/recoup only; 511-513行 needReview and 528-533行 集体讨论阈值 both compare fine only. Meanwhile confiscateAmount is fully modeled everywhere else (CaseDecision:40, ExecutionService:41/56-62, OversightService:144/154, MessageService:103, BureauStatsController:34/148, DocumentService:225, frontend free numeric input at CaseDetailView.vue:535 with no gating). The asymmetry is an oversight, not a deliberate 口径. Legally 行政处罚法第63条 enumerates 没收较大数额违法所得 as its own listening-hearing category, so the gap yields 未告知听证权 = 违反法定程序 = 必撤 under 行政诉讼法第70条(三) — a procedural defect, not merely wrong data, and exactly the failure class this system exists to prevent. Sharper than the report states: because confiscate is absent from the 2007 comparison entirely, a decision can impose 没收 that appeared in no 告知书 at all, and the NOTICE 文书模板 (V4) plus the 卷内 NOTICE entry (DocumentService:195-210) render only 责令退回/罚款 while the 决定书 prints 没收. Two spots the report missed and that must move in lockstep: DocumentService.java:169 legalReviewRequired() repeats the fine-only test (its own comment demands one 口径 with decide(); otherwise 卷内齐全性 reports 齐全 with no 法制审核意见), and BureauStatsController.java:96 govRecordMissing filters on d.fine_amount alone. Frequency low but shape bad: of the 35 seeded 案由 and 9 法条 only WC103 (基本医疗卫生与健康促进法第103条, 药品采购投标) carries 没收违法所得, and at base severity it is 没收 with zero 罚款 — precisely the reported shape; 辽宁 口径 puts hearing_threshold_org at 10000, so any use of 没收 clears it. No unit/integration test and no step in tools/e2e-case.py exercises confiscateAmount. Not in 开发待办.md. On the fix: do not use fine.add(confiscate) for the hearing test — 第63条 lists 较大数额罚款 and 没收较大数额违法所得 as separate categories with their own standards, so summing over-triggers (fine 6万 + 没收 5万 would demand a hearing neither category requires); use max() or a hearing_threshold_confiscate_* pair defaulting to the same values. Summation is defensible for 集体讨论 (第57条第2款 is about overall severity) but not there. For 2007 keep per-type comparison (existing code is already per-type for fine/recoup); a summed test would let 告知罚款10万 → 决定没收10万 through, which is a change of 处罚种类. Cost is one additive migration plus three guard edits, DocumentService:169, the stats filter, one notice form field and template text; the real hazard is that pilot cases already in NOTIFIED carry no confiscate告知 and would start failing 2007 — legally the correct break, but it needs a line in the upgrade steps rather than a silent deploy.

### 7. [HIGH] basisText 的条文号过滤是死代码，命中一部法律就把该法全部条文正文塞进文书
*其余服务 — `server/src/main/java/cn/ybcase/bureau/service/DocumentService.java:353`*

**现状/复现**：外层条件已要求 refs.contains(law_name)；内层第三个析取项 refs.contains(shortRef) 中 shortRef = law_name.replace("中华人民共和国","") 必为 law_name 的子串，故外层为真时内层恒为真——art 匹配从未生效。用种子数据实测：执法事项 #1 的 basis_refs = '《医疗保障基金使用监督管理条例》第38条'（V4__documents_archive.sql:78），渲染 NOTICE/DECISION 时 law_basis 里 TL38/TL39/TL40/TL41 四条（同一 law_name）全部命中，{{basisText}} 里被拼上第三十八、三十九、四十、四十一条全文约 2000 字。执法事项 #2（第39条）同样得到这四条。

**影响**：行政处罚事先告知书与行政处罚决定书的“法律依据”段落引用了本案根本不适用的条文——第38条（造成基金损失，1-2倍罚款）的案子同时列出第40条（骗保 2-5 倍罚款），属适用依据错误，是复议/诉讼中撤销决定的实体瑕疵；且这两份文书是必备卷内件，错误会一路带进案卷与合成打印。

**修法**：把内层判断改成真正的“law_name AND article”合取，并做条文号归一：law_basis.article 用中文数字（第三十八条）而 basis_refs 用阿拉伯数字（第38条），需先把两侧统一（中文数字↔阿拉伯数字转换后比较），再要求 refs 中确实出现该条号才 append；同时删掉恒真的 refs.contains(shortRef) 分支和被外层完全包含的 refs.contains("《"+law_name+"》") 分支。

> 核实：成立，但描述有两处需要修正，且该 bug 还有被漏掉的另一半。

【死代码判定：完全成立，可形式化证明】
DocumentService.java:350-354。外层 `refs.contains(law_name) || refs.contains("《"+law_name+"》")` 中第二个析取项被第一个完全包含，故外层 ≡ refs.contains(law_name)。内层 shortRef = law_name.replace("中华人民共和国","")，无论是否发生替换，shortRef 恒为 law_name 的子串；外层为真时 refs ⊇ law_name ⊇ shortRef，故 `refs.contains(shortRef)` 恒真，`refs.contains(art)` 与 `refs.contains(art去掉第/条)` 两个分支从未被求值到有意义的地步。art 过滤确为死代码。

【实测（种子数据）核对】
执法事项 #1 basis_refs='《医疗保障基金使用监督管理条例》第38条'（V4:78），law_basis 中 TL38/TL39/TL40/TL41 同名（V4:94-97），四条全部 append。#2（第39条）同样四条全中。第38条（造成基金损失，1-2倍）的案子里被拼上第四十条（骗保2-5倍）全文，属实。

【修正一：字数夸大约 2 倍】
四条 content 实测 233+156+187+268=844 字，加 law_name+article+分隔符约 925 字，其中多余的三条约 670 字，不是“约2000字”。

【修正二：“一路带进案卷与合成打印”是有条件的】
render() 只在 CaseController.java:127 被调用一次，结果填进前端可编辑草稿（CaseDetailView.vue:838-843 → :429 的 6 行 textarea），并非自动落库；案卷与合成打印读的是已保存的 case_document.content。所以错误进卷的前提是承办人未删改直接保存——这是默认路径且 6 行文本框里 900 字要滚动才看得全，风险真实，但“一路带进”应表述为“默认路径下带进”。同理“复议撤销的实体瑕疵”是评审人的法律判断，处罚决定的实际内容取自 decisionContent，多列条文属依据段落瑕疵，定性为“文书依据段落错列条文”比断言必然撤销更稳妥。

【评审漏掉的另一半：同一处还有假阴性，且比假阳性影响更广】
shortRef 显然是为外层设计的（basis_refs 写《社会保险法》，law_name 写中华人民共和国社会保险法），却被误放进内层。后果是：SB84/SB87/SB88 三条的外层 refs.contains("中华人民共和国社会保险法") 恒为假，执法事项 #3/#4/#6/#10/#11/#13 一条正文都带不出来，basisText 只剩 refs 原文。#3/#5/#11/#13 里的简称《条例》同样永不匹配 law_name。

【对建议本身的告警：按字面照做会把功能整个打死】
建议“删掉 refs.contains(shortRef) 分支”——若只删不移，SB 系列仍然全废；且因 basis_refs 用阿拉伯数字（第38条）、article 用中文数字（第三十八条），`refs.contains(art)` 与 `refs.contains("三十八")` 双双为假，删除后所有执法事项的 basisText 都将退化为零条文正文。评审人确实提了“需先做中文/阿拉伯数字归一”，方向对，但必须强调这是硬前置条件而非可选优化，且 shortRef 应上移到外层而不是删除。仓库内无任何现成的中文数字转换工具（grep 三十八/cnNum/toChinese 全仓无命中），需新写。另建议一并处理《条例》类简称别名，否则 #3/#5/#11/#13 仍取不到条文。

【是否已被别处兜住：没有】
law_basis 仅 BureauLawController.java:29 另有一处只读列表接口，与本逻辑无关；server/src/test 下无任何针对 basisText/law_basis 的用例；开发待办.md 中无相关记录，不属已知重报项。

> 核实：真 bug，且描述的机制完全成立。DocumentService.java:350-357 外层已要求 refs.contains(law_name)，内层第三析取项 shortRef = law_name.replace("中华人民共和国","") 必为 law_name 子串，故内层恒真，art 过滤从未生效；外层第二析取项 refs.contains("《"+law_name+"》") 同样被第一项完全包含。按 V4 种子数据模拟 13 项执法事项 × 9 条 law_basis 的实际循环结果：事项 #1（第38条）与 #2（第39条）均带出 TL38/39/40/41 四条；事项 #12（《社会救助暂行办法》第57条，检查类）错误带出 JZ68 处罚条款；事项 #3/#4/#6（《社会保险法》…）因库中 law_name 存为“中华人民共和国社会保险法”而外层不匹配，一条正文都带不出——这恰恰说明 shortRef 本应放在外层名称判断里，被误写进了内层。规模比报告略小：TL38-41 正文合计 844 字，第38条案件多出约 665 字不适用条文，非 2000 字，但不影响结论。发生概率高且在主路径上：basisText 供 NOTICE/DECISION/FINAL_REPORT/ORDER_CORRECT 四个模板（V4:110/125/151/160），其中三份是必备卷内件；#1/#2 正是普通程序处罚的主力事项；渲染结果落在 6 行 textarea（CaseDetailView.vue:429）里承载约 2500 字，多出的条文在折叠区以下，承办人几乎不会删。后果是错误数据而非体验问题：第38条（1-2倍）案子的告知书/决定书同时列第四十条（骗保 2-5倍），违背行政处罚法第44/59条对“依据”的记载要求，属适用法律错误，是复议诉讼的可撤销实体瑕疵，并在当事人决定是否陈述申辩/申请听证时误导其法律风险。改动风险小收益大：单个私有方法、唯一调用点、无 schema 与持久化影响；无单测覆盖，E2E tools/e2e-case.py:398 只断言出现“责令改正”，而 TL38 正文本身含该词，修复后仍通过（这也是它熬过十轮审阅的原因）。对建议方案的两点修正：一是不能直接删掉 refs.contains(shortRef)，应把它移到外层名称判断，否则 #3/#4/#6 继续一条正文都带不出；二是条文号归一要处理复合引用（#3 的“第38/40条”）并防止同一 refs 串里跨引用串号（#5 同时含第68条与第2条且分属两部法），稳妥做法是先按 ；/;/、 切分引用片段，要求法名与条号在同一片段内共现；另外现有 art.replace("第","").replace("条","") 兜底会让“三十八”命中“一百三十八”，改成数值比较可一并消除。运维补充：修复不会回改试运行期间已保存的 case_document.content，需对关联事项 1/2/12 的案件人工排查一遍。

### 8. [HIGH] V22 给 case_installment 加了 kind 却没把唯一键 (case_id, seq) 一并放宽，同案两类款项的分期计划撞键
*其余服务 — `server/src/main/java/cn/ybcase/bureau/service/OversightService.java:167`*

**现状/复现**：V6__inspection_reward_discretion.sql:53 建表时 unique (case_id, seq)；V22__installment_kind.sql:7 只 add column kind，未改约束。对同一案件先建 FINE 第1期（seq=1），再建 RECOUP 第1期（seq=1）→ 23505 → DuplicateKeyException → 2101「该记录已存在，请勿重复提交」。前端 CaseDetailView.vue:1270 的 installmentForm.seq 只是本次会话内自增，openInstallment() 不重置、切案/刷新后回到 1（第 1357 行），所以刷新页面后换 kind 再录必撞。

**影响**：条例38条的典型医保案就是“责令退回基金 + 处1-2倍罚款”，经负责人批准暂缓分期后两类款项各自排期是常规做法，而 addInstallment 的累计封顶、payInstallment 的入账都已按 kind 分别计算——唯独建计划被旧唯一键挡住，且报的是“重复提交”这种完全误导的提示。绕开只能给第二类款项编 seq=4,5,6，于是 case_execution 备注写成“分期缴纳第 4 期”，分期计划表上的期数与实际不符。

**修法**：新增迁移：drop constraint case_installment_case_id_seq_key; create unique index uq_installment on case_installment (case_id, kind, seq);（升级前先按 case_id+kind 重排存量 seq）。同时把前端 openInstallment() 改为按当前 kind 取 max(seq)+1 预填，而不是全局自增。

> 核实：成立，无法证伪。六条事实全部逐字核实为真：

1. `V6__inspection_reward_discretion.sql:53` 确为 `unique (case_id, seq)`，不含 kind。
2. `V22__installment_kind.sql:7` 只有 `alter table ... add column if not exists kind`，未动约束。
3. 全仓（*.sql/*.java/*.sh/*.py）grep `drop constraint|case_id_seq_key|uq_installment` **零命中** —— 该唯一键从未被放宽，别处没有兜住。
4. `OversightService.java:167` 直接把客户端传来的 `req.seq()` 插入，服务端无任何按 kind 派生/校验 seq 的逻辑。
5. `BureauExceptionAdvice.java:69-70` 确将 `DuplicateKeyException` 映射为 2101「该记录已存在，请勿重复提交」——误导性提示属实。
6. 前端 `CaseDetailView.vue`：`installmentForm` 初值 `seq:1`(783)；`openInstallment()`(1256-1260) 只重置 dueAt/amount，**不重置 seq 与 kind**；`onAddInstallment` 会话内 `seq += 1`(1270)；`resetCaseLocalForms`(1357) 切案复位回 1。刷新/切案后换 kind 再录，默认值必撞。

**决定性佐证——仓库自己的 E2E 已经在绕开这个 bug：**
`tools/e2e-case.py:563-577` 正是同案双 kind 场景（d_case 在 471-488 行判了 fineAmount 5000 **且** recoupAmount 4000，即候选所说"退回基金+罚款"的典型结构，是本仓库的标准演示案）。FINE 排了 seq=1、2 之后，RECOUP 那期用的是 **seq=4** —— 而 seq=3 那次因超封顶被 2065 拒、根本没落库。也就是说 RECOUP 计划的第 1 期被编成了"第 4 期"，与候选预言的"绕开只能给第二类款项编 seq=4,5,6"完全一致。596 行更把 `f"第 {i['seq']} 期" in note` 断言了下来，把错误期数固化成了预期行为。**E2E 全绿不是因为没 bug，而是因为测试作者避开了它。**

且设计意图确实是"每类款项各自一套计划"：`addInstallment` 封顶按 `where case_id=? and kind=?` 分别累计(162)，`payInstallment` 按 kind 入账(190)——唯独建计划仍受 V22 之前的全局 seq 旧键约束。V22 属半程迁移。

**不属重复上报**：`开发待办.md` 中 installment 相关条目全是 installmentOverdue 预警前端未渲染，与本条无关；"勿重报"清单里的"分期缴纳入 case_execution 且按 kind 入账"正是 V22 的入账侧修复，与唯一键是明确的两半。

**须向评审者澄清的三处夸大/收窄（不影响成立）：**
- "必撞"偏绝对：期数是可手改的 `el-input-number`(668)，办案人填个不冲突的数即可绕过，属默认路径碰撞而非硬阻断。另外同一会话内不刷新时自增反而**不报错**，但会把 RECOUP 第 1 期静默编成"期数 2"——静默错号比报错更隐蔽。
- 频率被抬高：须同时满足"决定书两类金额均 >0"**且**"负责人已批准暂缓/分期"（无 deferApproved 时按钮禁用且 2065 拦截），比"典型医保案"窄。
- 后果未外溢到法律文书：`DocumentService.java:322` 只取 `paid_at, kind, amount`，**不取 note**；`ArchiveService` 完全不引用 installment。错误期数仅停留在 case_execution 备注与分期计划表内，不会污染打印的决定书/结案报告。

**修复建议基本正确**，两点补充：PG 对 V6 那句默认约束名确为 `case_installment_case_id_seq_key`；但存量按 case_id+kind 重排 seq 后，已写入的 case_execution 备注"分期缴纳第 N 期"会与新期数对不上，需一并回填或接受历史不一致。另 `req.seq()` 为可空 Integer，客户端漏传同样撞 `seq int not null` 报 2101，改成服务端按 kind 取 max(seq)+1 派生可一并消除。

> 核实：属实，但触发路径与报告的归因需要修正，且建议里有一条应当删掉。

【已核实的事实】
1. V6__inspection_reward_discretion.sql:46-54 建表时内联 `unique (case_id, seq)`（PG 自动命名 case_installment_case_id_seq_key）。全量迁移目录 grep：只有 V6、V14（加 idx_installment_case 普通索引）、V22 提到 case_installment，无任何 drop constraint / 重建表。V22__installment_kind.sql 整个文件只有一句 add column kind + 一句 comment。约束确实原封不动。
2. OversightService.java:167 `insert into case_installment (case_id, seq, due_at, amount, kind) values (?,?,?,?,?)`，seq 完全取自客户端 req.seq()，服务端对 seq 无任何校验或推导。同一 case_id 下 seq 重复必然 23505，与 kind 无关。
3. BureauExceptionAdvice.java:69-70 把 DuplicateKeyException 一律转成 2101「该记录已存在，请勿重复提交」。提示确实完全误导。
4. 前端在 frontend/src/views/case/CaseDetailView.vue（不是报告写的 web/ 路径）：installmentForm 初值 seq:1（783 行），onAddInstallment 成功后 installmentForm.seq += 1（1270 行），openInstallment() 只重置 dueAt/amount、不动 seq（1256-1260 行），resetCaseLocalForms() 把 seq 拍回 1（1357 行）。确认是会话内全局自增、切案/刷新归 1。
5. 无测试覆盖同案两类款项各自排期：GuardIntegrationTest「分期缴清后可结案」用的是 FINE seq=1,2 再拿 seq=4 试 RECOUP（且因决定书无 recoup_amount 被 2065 挡在插入之前，根本没走到约束）；「只退回基金」那条是纯 RECOUP seq=1。两条都恰好绕开了冲突面。

【归因要修正的地方】报告把「刷新后换 kind 再录必撞 → 报重复提交」算在 V22 头上，这半句不准确。seq 归 1 的重置和全局自增是 V22 之前就有的前端缺陷：单一款项的案件同样会撞——已建 FINE 1、2，刷新后再点添加分期，预填还是 1，撞 FINE 自己的 seq=1，报的还是那句「该记录已存在，请勿重复提交」。所以那条误导提示不是 kind 引入的，是预填逻辑本来就错。V22 真正欠下的是另一件事：它把 kind 变成了一等维度（addInstallment 按 kind 分别封顶、payInstallment 按 kind 入账、表格逐行显示款项列），却让 seq 仍锁在案件级唯一。

【约束该不该放宽：确实该放宽】唯一能让 unique(case_id, seq) 成立的读法是「全案一条交错的期序、每期只有一类款项」，但这个读法既没写在任何注释里，也不符合两种现实排法：(a) 两类款项各排各的、各自从第 1 期起；(b) 更常见的一份合并计划——第 1 期同日缴罚款 2500 + 退回 3000，那就是 seq=1 的两行不同 kind。两种都被现约束挡死。既然 V22 已经承认款项类型是维度，唯一键跟着变成 (case_id, kind, seq) 是补齐，不是改设计。

【发生概率】需要同时满足：决定书 fine_amount 与 recoup_amount 皆 >0（条例 38 条的主流形态，项目自述的典型案）、经负责人批准 defer_approved、且两类款项都排了分期。前两条常见、第三条是「当事人确有经济困难」时的常规做法，所以不是罕见组合；但也不是每案必到。一旦到了，撞不撞取决于操作时序：同一次不刷新的会话里，seq 会全局自增到 3、4，不报错但期数写歪；中间刷新或切案回来，预填归 1，必撞。

【后果定性：以流程受阻为主，数据错误是次生且轻】money 侧不受影响——金额、kind、ExecutionService 的封顶和 fullyExecuted 判定全都按 kind 各算各的，结案判定和法定时限不会被算错。真正的损失是两条：一是办案人被一句和事实毫无关系的「请勿重复提交」挡住，没有任何线索指向真因，多半要么放弃登记（那笔钱的分期计划不进库 → 督办看板的 installmentOverdue/「分期到期未缴」对它永远不告警，等于这笔钱脱离监督）、要么按报告说的编 seq=4,5,6 绕过；二是绕过后 payInstallment 写进 case_execution 的备注变成「分期缴纳第 4 期（自动入账）」，与分期计划表上这类款项的实际期次不符。case_execution.note 是自由文本备注、不参与任何计算，所以这是台账记述失真，不是账目错误。

【改动风险/收益】收益明确、风险很低：
- 迁移侧：把唯一键从 (case_id, seq) 放宽到 (case_id, kind, seq) 是严格更宽松的约束，存量数据不可能违反，建索引必成功；约束名由 V6 内联唯一确定，写 drop constraint if exists 即可兜底。
- 报告建议的「升级前先按 case_id+kind 重排存量 seq」应当删掉，这一步是净负风险：已缴分期在 case_execution 里留下的「分期缴纳第 N 期」备注是历史快照、不会跟着改，重排后计划表与已归档的执行记录互相打架，反而制造新的对不上。放宽约束本身不需要动存量。
- 前端侧：openInstallment() 与 kind 变更时从已加载的 installments 里按当前 kind 取 max(seq)+1 预填，是十几行的本地改动，顺带把上面说的「单一款项刷新后也撞」这个更日常的老毛病一并解决。
- 服务端建议再加一层：seq 为空时按 (case_id, kind) 服务端推导，或插入前显式查重并抛 2065 说明「该类款项第 N 期已存在」，这样即便前端预填失效也不会再冒出 2101 那句误导提示。
- 回归面：两条既有集成测试用的 seq 分别是 1/2/4 和 1，放宽约束后行为不变；新增一条「同案 FINE 与 RECOUP 各自从第 1 期排」的用例即可锁住。

### 9. [HIGH] 「简易程序备案」按钮嵌在 c.status==='DECIDED' 分支里，登记送达后入口永久消失，督办看板常亮无解
*前端 — `frontend/src/views/case/CaseDetailView.vue:59`*

**现状/复现**：该按钮位于 57 行 `<template v-if="c.status === 'DECIDED'">` 内，同一分支第 58 行就是「登记送达」。简易程序案件 decide() 后状态为 DECIDED（CaseService.java:571），当场处罚决定书当场交付，办案员通常先点「登记送达」→ CaseService.deliver 把状态改为 DELIVERED（CaseService.java:607）；DELIVERED 分支（65-72 行）没有备案按钮，CLOSED 也没有。此后前端再无任何入口调用 /summary-record，而 ProcedureService.summaryRecord（ProcedureService.java:147-156）本身对状态毫无限制，只要求 SUMMARY + decidedAt 非空 + 未备案。

**影响**：简易程序案件一旦先送达再备案，第51条的「决定后7个工作日内报本机关备案」在系统里永远无法完成；BureauStatsController 的 summaryRecordOverdue 查询（BureauStatsController.java:111-117）只按 `procedure_type='SUMMARY' and decided_at is not null and summary_record_at is null` 过滤、不看状态，这批案件会永久占据督办看板「简易程序备案超期」红条，且结案后也清不掉——监督看板出现一批无法处置的常亮告警。

**修法**：把这颗按钮从 DECIDED 分支里提出来，与 TERMINATED 立卷归档（63-66 行）同样独立成块：`<template v-if="c.procedureType === 'SUMMARY' && c.decidedAt && !c.summaryRecordAt"><el-button @click="onSummaryRecord">简易程序备案</el-button></template>`，与后端 summaryRecord 的实际前置条件对齐。

> 核实：成立，且描述偏保守而非夸大。核实：CaseDetailView.vue:59 的「简易程序备案」按钮确实嵌在 57 行 `<template v-if="c.status === 'DECIDED'">` 内（c = detail.caseFile，见 768 行），全仓库对 /summary-record 的唯一调用点是该按钮 → onSummaryRecord(1296 行)，DELIVERED(65-72)/CLOSED 分支均无入口，SupervisionView.vue:103-110 的超期卡片只是 row-click 跳详情页、无动作按钮，等于把用户送到按钮已消失的页面。三处反驳尝试全部失败：(1) 无别处兜底——grep summary-record/summaryRecord 仅命中按钮、ProcedureController:54、ProcedureService:147、BureauStatsController:111、CaseFile:118、V5 迁移；(2) 看板确实清不掉——BureauStatsController:111-117 只按 procedure_type='SUMMARY' and decided_at is not null and summary_record_at is null 过滤，无状态条件，与相邻 correctOverdue 明确带 `cf.status not in ('CLOSED','TERMINATED')` 形成对照，结案后红条永久常亮；(3) 不是边缘路径而是主干——CaseService.close 第690行 `if (!"DELIVERED".equals(c.getStatus())) throw 2011 "处罚决定书未送达，不能结案"`，PUNISH 类必须先走 deliver(583行，DECIDED→DELIVERED)，所以每个简易程序当场处罚案都必然穿过 DELIVERED；而第51条的备案是决定后7个工作日内的事后动作，当场处罚决定书是当场交付，真实顺序就是先送达后备案，恰好是 UI 无法服务的顺序（原文「通常先点登记送达」说轻了）。另一条出口 NO_NEED 决定可从 DECIDED 直接结案，同样吃掉按钮。后端 ProcedureService.summaryRecord(147-156) 无任何状态限制，属纯前后端前置条件错配。测试未覆盖：tools/e2e-case.py:726-733 直接调 API，decide 后立刻 summary-record，从不 deliver、也不走 UI。对建议的一处修正：不要加 status !== 'CLOSED' 的排除，否则试运行库里已卡住（已送达/已结案）的存量案件永远清不掉红条，后端是允许补备案的；另外该条件会让 TRANSFER_JUDICIAL 的简易案（TERMINATED 且 decidedAt 非空，CaseService:563-570）也渲染出按钮，后端接受、但与页面对 TERMINATED 隐藏动作按钮的惯例不一致。

> 核实：真实缺陷，报告的五条事实全部复核属实，且发生概率比报告估计的还高。

【复核结论】
1. CaseDetailView.vue:59 的「简易程序备案」按钮确在 57 行 `<template v-if="c.status === 'DECIDED'">` 块内，紧挨 58 行「登记送达」。
2. CaseService.deliver(:588-613) 要求 DECIDED 进入、退出时 setStatus("DELIVERED")；DELIVERED 分支(67-74)与 CLOSED 均无该按钮。
3. 全仓 grep：frontend/src 下 `/summary-record` 只有 CaseDetailView.vue:1296 一处调用，入口唯一，别无补录页面。
4. ProcedureService.summaryRecord(:146-157) 确实不看状态，只校验 SUMMARY + decidedAt 非空 + summaryRecordAt 为空。
5. BureauStatsController:111-117 的 summaryRecordOverdue 不含状态条件，CLOSED 案件照样出现在红条里。

【发生概率：高，不是偶发】
关键是我在 CaseService.close(:672-701) 找到的强制约束：PUNISH 决定的案件若非 DELIVERED 直接抛 2011「处罚决定书未送达，不能结案」。简易程序案件的决定类型必然是当场处罚(PUNISH)，所以每一件简易案要结案就必须经过 DECIDED→DELIVERED，而备案按钮只在 DECIDED 这个窗口里存在。更糟的是法定与实务顺序恰恰是"先交付、后备案"——第51条原文就是"当场交付决定书"+"决定后7个工作日内报本机关备案"，办案员从现场回来先点「登记送达」是正确操作，而这一点就永久失去入口。也就是说：按法定顺序办案的人 100% 踩坑，只有反着操作的人侥幸躲过。

【后果：错误数据 + 监督失效，两者都占】
(a) 数据侧：summary_record_at 永久为 NULL，案卷里表现为"本机关从未依第51条备案"。close() 对该字段无任何校验，案件照常结案归档，缺口被固化进已归档案卷。这是一项法定行为在系统中无法被记录，不是显示问题。
(b) 监督侧：SupervisionView.vue:103-104 已渲染该面板且行点击跳转 /case/detail/{id}——督办岗被告知去处理，跳过去却没有任何按钮。因 SQL 不过滤状态，结案后红条依然常亮，形成一批永远无法处置的告警，真实超期案件会被淹没。

【未被测出的原因】
tools/e2e-case.py:725-733 建简易案→decide→立刻 POST /summary-record，全程不调 deliver，正好避开了出问题的顺序；且它是 API 级用例，本就测不到按钮可见性。

【风险/收益】
收益明确：恢复一项法定动作的唯一入口，并让督办看板重新可清零。风险极低：纯前端模板搬块，无后端、无迁移；`c` 绑定的是 detail.caseFile 整个实体(CaseService:774)，procedureType/decidedAt/summaryRecordAt 三个字段(CaseFile.java:41/93/118)均无 @JsonIgnore，报告给的条件可直接用。建议照 TERMINATED 立卷归档(65-68)的写法独立成块。

【落地时需要拍板的一点】
报告给的条件不带状态过滤，按钮会在 CLOSED/TERMINATED 案件上也显示，与本页"结案/终止后隐藏操作"的惯例冲突。但如果为了守惯例而排除 CLOSED，那些已经结案且未备案的存量案件在看板上将永远清不掉——因为 BureauStatsController:111-117 同样不看状态。建议就按后端实际前置条件来（不过滤状态），这是唯一能清掉存量红条的写法；若局方坚持已归档案卷不可再动，那就把同一个状态过滤同时加到按钮和那条 SQL 上，两边必须保持一致，否则只是把死锁从"没按钮"换成"清不掉"。另：简易案走 TRANSFER_JUDICIAL 会被 decide 置为 TERMINATED 且 decidedAt 已写，届时按钮会出现——实务上极罕见，且与看板口径一致，无害。

### 10. [HIGH] 回避只提交 reason：主办（LEAD）人员回避 100% 被 2061 拒绝，当事人申请回避被静默记成主动回避
*前端 — `frontend/src/views/case/CaseDetailView.vue:1023`*

**现状/复现**：onAvoid（1021-1026）只发 `{reason: value}`。后端 CaseService.avoidOfficer(caseId, officerId, reason, applicant, decidedBy)（CaseService.java:144-166）由 CaseController.java:81-82 从 body 取 applicant/decidedBy，前端二者恒为 null（全仓库 grep 无 decidedBy/applicant 提交点）。于是：① 对 duty='LEAD' 的行，CaseService.java:156-158 `if ("LEAD".equals(duty) && (decidedBy==null||isBlank)) throw 2061 「主办人员回避须经负责人批准并记录批准人」` 必然触发——而办案人员表格（96-99 行）对所有未回避人员一律显示「申请回避」按钮，包括主办；② 非 LEAD 行虽能成功，但 CaseService.java:164 `applicant == null ? "SELF" : applicant` 把当事人申请的回避一律落库成 SELF（主动回避），avoid_decided_by 留空。

**影响**：主办人员的回避在系统里根本办不成，用户点一次报一次错、没有任何补救入口（第5条的分级批准通道在前端不存在）；协办人员的回避则把「当事人申请回避、负责人审查决定」伪装成「本人主动回避」，案卷里丢失第5条要求的申请人与批准人留痕——回避事由查起来无从判断合法性。

**修法**：onAvoid 改成先选申请主体再按需收集批准人：`const applicant = await 选择(SELF/PARTY)`；当 applicant==='PARTY' 或 row.duty==='LEAD' 时再 prompt 一次「负责人（批准人）」，最终 POST `{reason, applicant, decidedBy}`。

> 核实：成立，但两处描述需校准。

【核实通过】CaseDetailView.vue:1021-1026 onAvoid 只发 {reason}；CaseController.java:79-84 从 body 取 applicant/decidedBy（恒 null）；CaseService.java:155-158 LEAD + decidedBy 空 → 2061；:164 applicant==null?"SELF"。模板 96-98 对所有未回避行（含 LEAD）一律显示「申请回避」，385 行 duty 可自由选 LEAD。全仓库 frontend/src grep 无任何 decidedBy/applicant 提交点（ApprovalsView.vue:52 的 applicant 是审批单自己的申请人列，无关）。ApprovalService.KINDS 只有 FILE_CASE/EXTEND/SUSPEND/TERMINATE/DEFER，无 AVOID 审批通道——第5条分级批准在前端确实不存在，无别处兜底。

【为何十轮没抓到】grep -i avoid 在所有 .java/.sh/.md（排除这两个实现文件）中零命中：该端点无任何单元/集成/E2E 覆盖。

【校准1：「100%」有前置条件】CaseService.java:146-150 的 active<2 守卫先于 LEAD 检查执行。办案组恰为 2 人（法定下限、最常见配置）时，任何回避都先撞 2002「回避后执法人员将少于两人，请先补充办案人员」——这是正确且可操作的提示。2061 死路只在 ≥3 人时才浮现。所以应表述为「≥3 人办案组时 100% 被拒」，而非无条件。死路本身真实且无补救入口。

【校准2：留痕影响一处夸大、一处漏报】avoid_reason 是必填非空且在表格中显示的，「回避事由查起来无从判断合法性」不准确——事由留存了，丢的是申请主体与批准人。反过来：avoid_applicant/avoid_decided_by 是只写列，detail() 的 select * 会带回前端但无任何模板渲染、无任何后端逻辑/归档/打印读取，所以问题②今天没有用户可见症状，只在数据库/审计导出层面污染记录。①则是完全用户可见的硬阻断。

【额外发现】CaseService.java:152-153 的 "PARTY".equals(applicant) 守卫是彻底的死代码——applicant 恒为 null，该分支永不执行。后端分级批准逻辑两条臂都无调用方。

建议的修法方向正确。

> 核实：REAL — confirmed end to end by reading both sides; two distinct defects from one missing payload.

CONFIRMED FACTS:
- CaseDetailView.vue:1023 posts `{reason: value}` only. Grep over frontend/src for `decidedBy`/`applicant` returns zero senders (the only hit is an unrelated ApprovalsView table column). Only two `avoid` references exist in the whole frontend: line 96 (render) and line 1023 (post).
- CaseService.java:155-158 throws 2061 whenever duty='LEAD' and decidedBy is null/blank. Since decidedBy is null 100% of the time, 主办人员回避 is a deterministic dead end. Line 385 offers 主办(LEAD) in the add-officer form and line 97 shows 申请回避 for every non-avoided row including 主办, so the UI actively invites the failing action.
- CaseService.java:164 `applicant == null ? "SELF" : applicant` writes an affirmatively FALSE value: a 当事人申请 recusal is stamped 主动回避 with avoid_decided_by null. Worse than a blank. Silent too — detail() returns case_officer via `select *` json_agg (line 735) so the columns reach the client, but line 96 renders only avoid_reason, so nothing on screen ever contradicts the false SELF.

TWO AGGRAVATING FACTS NOT IN THE REPORT:
1. CaseController.java has ONLY two officer endpoints (POST /officers, POST /officers/{id}/avoid). There is NO remove-officer endpoint, so `avoid` is the sole mechanism for taking a 主办 off a case. Zero fallback: not a UX annoyance, a hard functional dead end with no alternate route short of DB surgery.
2. ZERO test coverage — `avoid` appears in no unit, integration, or E2E test. That is why a total frontend/backend contract mismatch survived ten review rounds: prior rounds reviewed the backend guard (correct in isolation), never the caller.

Note the symmetry: the same missing payload breaks both guards in opposite directions — the PARTY guard (152-153) is unreachable dead code because applicant is never 'PARTY', while the LEAD guard always fires.

NOT A DUPLICATE: the "勿重报" list covers the 回避 backend guard's existence; 开发待办.md:75 (人员变动) is about 离职停用/证件到期 account lifecycle, a different concern. The frontend never feeding the guard is previously unreported.

PROBABILITY / CONSEQUENCE: recusal is uncommon per case, but conditional on attempt the LEAD path fails 100%, and 医保 cases (办案人员 vs. local 定点医药机构) are precisely where conflicts of interest recur — that is why 第5条 and these columns exist. Over a deployed system's year this will arise. Consequence is BOTH: (a) a statutory procedure that cannot be performed at all, (b) wrong data silently written into the archived legal record, losing the 第5条 申请人/批准人 留痕 that makes recusal legality auditable. (b) is the more serious half because it is silent and permanent.

RISK/BENEFIT: fix is ~10 lines confined to onAvoid; V5 columns already exist (avoid_applicant, avoid_decided_by), backend already parses and validates both fields, endpoint has exactly one caller. Near-zero regression risk, high value.

CORRECTION TO THE PROPOSED FIX: prompting the 办案人员 to free-type a 批准人 reproduces the forgery hole this team already closed for 立案 — see the comment above CaseController.create(): "办案人员直接调用会写出 approver 是自己的假批准单，伪造第17条的负责人批准证据." Everywhere else approver derives from the authenticated principal (ApprovalService.decide(..., approver) fed auth.getName()) under @PreAuthorize; the avoid endpoint is the lone outlier trusting an ungated body string. Fix the frontend to collect applicant + approver AND gate decidedBy server-side (derive from principal, or require LEADER/ADMIN); `isLeader` already exists at CaseDetailView.vue:759. Otherwise the fix converts an impossible operation into a forgeable one.

### 11. [HIGH] 分期计划唯一约束未随 V22 的 kind 列扩展，第二类款项的分期建不出来
*数据层 — `server/src/main/resources/db/migration/V6__inspection_reward_discretion.sql:53`*

**现状/复现**：案件决定书判「罚款 5000 + 退回基金 4000」，批准暂缓分期后：先 POST /api/bureau/cases/{id}/installments {seq:1,kind:'FINE'}、{seq:2,kind:'FINE'}；再建退回基金分期第 1 期 {seq:1,kind:'RECOUP'}。CaseDetailView.vue:668 的「期数」是 el-input-number :min=1、切案时 reset 为 1，办案人自然会填 1。OversightService.java:167 直插 case_installment → 撞 case_installment_case_id_seq_key → BureauExceptionAdvice.java:69 转成 2101「该记录已存在，请勿重复提交」。tools/e2e-case.py:574 正是靠把退回基金分期写成 seq=4 才绕开这个约束。

**影响**：V22 专为「罚款/退回基金/没收各自分期」加的 kind 列在唯一约束上没落地：unique (case_id, seq) 让同一案件的第二类款项只能借用第一类的期数空间。结果二选一——要么直接报「重复提交」建不出计划（错误文案与真实原因无关，办案人只能靠猜换期数），要么按 e2e 的做法从 seq=4 起编号：此时 payInstallment 自动入账写进 case_execution.note 的是「分期缴纳第 4 期（自动入账）」、督办看板 installmentOverdue（BureauStatsController.java:99-102）显示的也是「第4期」，而它实际是退回基金分期的第 1 期。期数是第54条分期缴纳决定的法定要素，这两处都是进案卷和执行记录的内容。医保案「既责令退回基金又罚款」是常态，命中率高。

**修法**：新迁移里 alter table case_installment drop constraint case_installment_case_id_seq_key; alter table case_installment add constraint uq_installment_case_kind_seq unique (case_id, kind, seq);（存量行在同 case 内 seq 本就唯一，加列不会冲突）。同时把期数改为服务端定：addInstallment 里按 select coalesce(max(seq),0)+1 from case_installment where case_id=? and kind=? 取号，忽略请求体里的 seq，前端「期数」输入框改为只读展示。

> 核实：成立，但描述有三处需要更正（含一处事实错误），且建议的修法不完整。

【核实为真的部分】
1. 约束确未随 V22 扩展。V6__inspection_reward_discretion.sql:53 逐字是 `unique (case_id, seq)`；V22__installment_kind.sql 全文只有一条 `alter table case_installment add column if not exists kind ...` 和一条 comment，无任何 constraint 操作。V22 是最新迁移；对 case_installment 全仓 grep 所有 .sql，只命中 V6(建表)、V14:17(idx_installment_case 普通索引)、V22，无第二处约束定义。
2. 服务端不兜。OversightService.java:167 是裸 insert，直接把客户端 req.seq() 写进去；前置校验只有 defer_approved(2065)、kind 白名单、金额为正、以及按 kind 分组的计划总额封顶(:161-165)——没有任何 seq 取号或去重逻辑，也没有 on conflict。
3. 错误链属实。DuplicateKeyException → BureauExceptionAdvice.java:69 → 2101「该记录已存在，请勿重复提交」，与真实原因（期数被另一类款项占用）完全无关。
4. e2e 确在绕。tools/e2e-case.py:486-488 的 d_case 决定书正是「罚款 5000 + 退回基金 4000」，:566-570 建 FINE seq=1/2，:574-575 建 RECOUP 时写的是 seq=4 并靠 `i["seq"]==4` 取回——即评审说的场景在测试里存在，而 RECOUP 第 1 期编号为 1 的路径全套测试无一覆盖（GuardIntegrationTest:279-281 那条 RECOUP seq=4 是被另一道守卫 2065 挡掉的，不构成覆盖）。
5. 下游文案属实。OversightService.java:191 写入 case_execution.note 的是 "分期缴纳第 " + seq + " 期（自动入账）"；BureauStatsController.java:99-102 的 installmentOverdue 输出含 i.seq。

【更正一：「进案卷」是错的】
评审称「这两处都是进案卷和执行记录的内容」——案卷那一半不成立。系统里根本没有分期缴纳决定书：DocumentService.java:27-32 的 ARCHIVE_ORDER 23 个 doc_type 无对应项；archiveEntries()(:105-124) 只取 case_document，recordEntries()(:177+) 只渲染 biz_approval / case_notice / case_decision / case_review / case_meeting / case_hearing / case_delivery，case_installment 与 case_execution 两张表都不进卷内目录、也不进合成打印。DocumentService.java:322 的 timeline 虽读 case_execution，但只 select paid_at, kind, amount，note 被丢弃。ExecutionService.java:126-132 的 approveDefer 只翻 defer_approved 标志位，连一条 biz_approval 都不产生，所以整个分期批准在案卷里没有任何纸面。错误期数的可见范围实为两处屏显：案件详情「执行」表的备注列(CaseDetailView.vue:373) 与督办看板。「期数是第54条分期缴纳决定的法定要素」在本系统没有落点，属拔高。

【更正二：复现路径挑错了主线】
评审说「切案时 reset 为 1，办案人自然会填 1」——只说对一半。CaseDetailView.vue:1270 在每次添加成功后执行 `installmentForm.seq += 1`，而 openInstallment()(:1256-1260) 只复位 dueAt 和 amount、不碰 seq。所以同一次会话里连排完 FINE 1、2 后切到 RECOUP，默认值是 3，会插入成功——落地的是编号错误的静默脏数据，不是 2101 报错。要撞约束需要刷新页面或切案（:783 初值、:1357 复位均为 1）后再建。两条路都真实，但评审把概率较低的那条当成了主线；真正更常发生的是静默写错期数。

【更正三：这个前端缺陷比描述的更宽，与 kind 无关】
弹窗从不按已加载的 installments 回填 seq。次日新会话打开一个已有「罚款第1期」的案件，添加第2期时默认仍是 1 → 同样 2101。这一条反而加强了「期数改由服务端取号」的建议（同类款项撞车是约束在正常工作、只是文案烂；跨类款项撞车才是约束错了）。

【建议不完整之处】
按 kind 取号改完后仍有两个尾巴必须一起动，否则改完更糊：
- BureauStatsController.java:99-102 只 select i.seq 不 select i.kind，SupervisionView.vue:111-118 渲染的是 案号/期次/金额/应缴日期。改约束后看板会出现两行都写「期次 1」且无从区分款项，比现在的「第4期」更难判读。须补 i.kind 与款项列。
- OversightController.java:103 是 `order by seq`，改后 FINE 1 / RECOUP 1 / FINE 2 / RECOUP 2 会交叉排列，须改 `order by kind, seq`。
迁移本身的两点判断是对的：存量行在同 case 内 seq 已唯一，加列不会冲突；`case_installment_case_id_seq_key` 是 PG 对 create table 内联 `unique (case_id, seq)` 的确定性默认名，drop by name 可行；V14:17 已有 (case_id) 普通索引，丢掉唯一约束不损失查询路径。

【定级】中。医保案「既罚款又责令退回基金」确是常态（e2e 自己的样例案就是），命中率高；但不是硬阻断（换个未占用期数即可建成），无法定文书被写错，无不可逆后果。属数据正确性 + 错误文案误导的缺陷，不是致命项。

> 核实：真实缺陷，代码逐环确认，非已修复项、非已记录待办项。

【确认的事实链】
1. C:\Users\drxuj\Projects\医保案件\server\src\main\resources\db\migration\V6__inspection_reward_discretion.sql:53 建表时写的是 `unique (case_id, seq)`。对全部 22 个迁移文件 grep `case_installment`，除 V14 的 `idx_installment_case` 索引和 V22 的 `add column kind` 外无任何其他 DDL——V22 加了 kind 列，唯一约束确实没跟着扩展。
2. server/src/main/java/cn/ybcase/bureau/service/OversightService.java:167 `insert into case_installment (case_id, seq, due_at, amount, kind) values (?,?,?,?,?)` 直接用 req.seq()，无服务端取号、无按 kind 的期数校验。讽刺的是同一方法第 162 行的「计划总额不超决定额」守卫**已经是按 kind 分开算**的（`and kind = ?`），即金额守卫认为三类款项各有各的计划，而唯一键仍认为一个案件只有一套期数——两处口径相互矛盾。
3. server/src/main/java/cn/ybcase/bureau/common/BureauExceptionAdvice.java:69-70 把 DuplicateKeyException 转成 2101「该记录已存在，请勿重复提交」，文案与真实原因（期数空间被另一类款项占用）完全无关，办案人无从判断该改什么。
4. 前端 frontend/src/views/case/CaseDetailView.vue:668 期数为 `el-input-number :min="1"`；openInstallment()（1256 行）只复位 dueAt/amount，不复位 seq；切案复位（1357 行）把 seq 置回 1。
5. tools/e2e-case.py:574 确实把唯一一条 RECOUP 分期写成 seq=4 并按 `i["seq"] == 4` 回查——绕开约束的痕迹坐实。
6. 该场景无任何测试覆盖：GuardIntegrationTest.java:255-315 两个分期用例都是单一款项类型；第一个用例里那条 RECOUP 在 2065（决定书未判退回基金）就被挡下，根本走不到 insert。混合款项分期是测试盲区。

【对报告的一处修正】报告称「办案人自然会填 1」不完全准确：CaseDetailView.vue:1270 每次添加成功后会 `installmentForm.seq += 1`，所以同一次操作中 FINE 第1、2 期之后切到 RECOUP，表单预填的是 3，直接提交**不报错但把退回基金计划的第一期记成第3期**；硬报 2101 出现在刷新页面/切案回来（seq 复位为 1）或办案人把明显不对的 3 改回 1 时。报告的「二选一」结论成立，只是触发点略有出入。

【后果定性：错误数据，不只是体验问题】错号会落到三处持久/展示内容：case_installment.seq 本身；OversightService.java:191 写入 case_execution.note 的「分期缴纳第 N 期（自动入账）」（执行记录正文）；BureauStatsController.java:99-102 → SupervisionView.vue:113 督办看板「期次」列。期数是第54条分期缴纳决定的法定要素。补充一点报告没提的：督办看板那张表只有案号/期次/金额/应缴日期，**没有款项类型列**，所以即便修好约束，两套计划各自的「第1期」在督办视角仍无法区分——修复应连带 select i.kind 并加一列。另外 OversightController.java:103 的列表查询是 `order by seq`，改成按 kind 分套后两类会交错，需改为 `order by kind, seq`。
注：错号只进数据库与页面，不进打印文书——DocumentService.java:322 的时间轴只取 case_execution 的 paid_at/kind/amount，不取 note；报告「进案卷」的说法偏重。

【概率】需同时满足：决定书既判罚款又判退回基金（医保案常态，属实），且经负责人批准暂缓/分期（第54条以「确有困难」为条件，非常态）。两条件相乘后单案命中率中偏低，但一旦命中办案人是被彻底卡住且拿到一句误导性提示，只能靠猜换期数。

【风险/收益】收益明显大于风险。迁移安全：存量行受 unique(case_id,seq) 约束，同案内 seq 本就唯一，扩展为 (case_id, kind, seq) 不可能冲突（drop 时用 `if exists` 兜住约束名差异即可）。服务端取号会改变接口语义，影响面已确认可控：tools/e2e-case.py:574-576 的 seq=4 断言必须同步改；GuardIntegrationTest 两个用例虽显式传 seq，但只按 id 回查，不受影响。建议两项一起做——只改约束不改取号，办案人仍可手填出跨类跳号/断号；只改取号不改约束则第二类款项照样撞键。总改动量为一个迁移 + 服务端十余行 + 前端期数改只读，属高性价比修复。

### 12. [HIGH] 静态资源无 Cache-Control + SPA 回退把缺失的 js 返回成 index.html，升级后老用户白屏
*工程与性能 — `deploy/Caddyfile:29`*

**现状/复现**：Caddyfile:27-31 的 `handle` 块只有 `root`/`try_files`/`file_server`，全站没有任何 Cache-Control（顶部 header 块只设了 X-Frame-Options 等安全头）。file_server 只发 ETag + Last-Modified，浏览器对这类响应走**启发式缓存**：新鲜期 ≈ 0.1 × (Date − Last-Modified)。index.html 若是 20 天前部署的，用户取到后浏览器会认为它 2 天内新鲜、根本不回源校验。而 server-build.sh:35 每次部署 `rm -rf "$DEPLOY/dist"/*` 把旧产物全删，Vite 的 chunk 名又是内容哈希（如 assets/index-Bg4-NOiY.js）。复现：用户 A 今天打开过站点 → 今晚发版 → 明天 A 刷新：浏览器直接用缓存里的旧 index.html，去请求已被删除的 /assets/index-<旧哈希>.js，命中 Caddyfile:29 的 `try_files {path} /index.html` → 服务端返回 **HTTP 200 + Content-Type: text/html** 的 index.html。Chrome 报 “Failed to load module script: Expected a JavaScript module script but the server responded with a MIME type of text/html”，页面全白，除非用户自己 Ctrl+F5。

**影响**：每次发版后，凡是在发版前不久访问过的用户都会白屏，且持续到启发式新鲜期结束（发版间隔越长、新鲜期越长）。用户侧看到的是“系统坏了”，运维侧后端健康检查全绿、Caddy 访问日志全是 200，完全无从定位。次要成本：因为没有强缓存，每次冷导航都要对 dashboard 路由用到的十来个 hash 化 chunk（vendor 264KB、index.css 49KB、MainLayout 46KB、el-table-column 116KB 等）逐个发条件请求换 304。

**修法**：在 handle 块里加两条：`header /assets/* Cache-Control "public, max-age=31536000, immutable"` 与 `header /index.html Cache-Control "no-cache"`；同时把资源目录从 SPA 回退里摘出去，让缺失的哈希文件老老实实 404 而不是伪装成 HTML —— 在现有 handle 之前加 `handle /assets/* { root * /srv/dist; file_server }`。改完 Caddyfile 后注意它由 server-build.sh:41 同步、需 `docker compose up -d` 重建 caddy 才生效。

> 核实：部分成立：根因与修复方向对，但描述的故障机理和影响被明显夸大，须改写后再采纳。

【核实为真】Caddyfile:27-31 确实只有 root/try_files/file_server，全仓库 deploy/ 无任何 Cache-Control；file_server 只发 ETag+Last-Modified，启发式缓存确实生效。dist/index.html 用 type=module crossorigin 引 /assets/index-Bg4-NOiY.js，且 nosniff 已设，HTML 冒充 js 必被硬拦。server-build.sh:35 每次 rm -rf dist/*，旧哈希必删。确未被别处兜住：无 service worker、无 router.onError、无 app.config.errorHandler、无 unhandledrejection、__APP_VERSION__ 仅在 MainLayout.vue:138 当文字显示、Caddy 是唯一边缘（无 nginx）、开发待办.md 无此条。

【机理被驳倒】server-build.sh:36 是 `cp -r ... dist/.` 无 -p，实测本地产物 index.html / vendor-67QM-Wco.js / index-Bg4-NOiY.js 的 mtime 同为 2026-08-31 13:10:47（同秒）。即 index.html 与入口 chunk、vendor、css 的 Last-Modified 完全相同，又在同一次加载中同时取回，Date−Last-Modified 相同 → 启发式新鲜期完全相同、同时刻过期。index.html 永远是一次加载中最早取回的资源，不可能出现「index.html 还新鲜、它引用的同代入口 chunk 已过期」。所以「拿着旧 index.html 去请求被删掉的入口 js」这个状态在正常情况下根本不成立——旧 index.html 必然配着一整套同样被缓存的旧 chunk，浏览器全部命中缓存，旧版前端照常跑起来，不白屏。要出现所述白屏，需 Chrome 恰好淘汰了 264KB 的 vendor 却留下 500 字节的 index.html，对总量约 1.4MB 的资源集不是常态。

【其它夸大】(1)「除非用户自己 Ctrl+F5」错：Chrome 普通 F5 对主资源就发条件请求，一次刷新即自愈，谈不上「持续到启发式新鲜期结束」。(2)「每次冷导航都要逐个发条件请求换 304」错：新鲜期内是零请求；过期后约十几个条件请求在同一条 h2 连接上复用≈1 个 RTT，收益不可观测，这条次要成本不成立。

【真实存在、值得修的是另一条】try_files 那半是真缺陷，但受害者是懒加载路由 chunk 而非入口包。router/index.ts 全部路由都是 () => import()，用户此前没访问过的 chunk 不在缓存，发版后必然 404 → Caddyfile:29 回退成 200 text/html → 动态 import reject；因无 router.onError，导航静默中止。可见症状：点没打开过的菜单（移送/检查/奖励/统计）毫无反应；api/client.ts:98 的 401 走 router.push('/login') 而 LoginView 也是懒 chunk，于是弹「登录已过期，请重新登录」但登录页永不出现。这条完全不依赖缓存——发版时开着标签页的用户同样中招，光加缓存头修不掉。此外真正更严重、原文没点出的后果是：旧 index.html 让用户在 0.1×发版间隔内静默地用旧前端打新后端，对一个靠发版补法定守卫、改 payload 形状的系统，这是无声的正确性风险。

【建议本身有 bug】`header /index.html Cache-Control "no-cache"` 无效：Caddy 的 header 指令排序在 try_files 之前，匹配器看到的是原始请求路径，而用户访问的是 /、/dashboard、/case/list，从不请求字面量 /index.html，这些导航仍拿不到头、启发式缓存照旧。须改用否定匹配器，如 `@spa not path /assets/*` + `header @spa Cache-Control "no-cache"`，再配 /assets/* 的 immutable 头和独立 `handle /assets/*` 让缺失哈希老实 404。

> 核实：真 bug，机制逐环核实无误，但报告的复现路径和建议补丁各有一处需要修正，照抄会发一个空补丁。

## 一、机制全部证实

1. **无任何缓存头**：deploy/Caddyfile:27-31 catch-all handle 只有 root/try_files/file_server；站点级 header 块(:8-14)只设 X-Frame-Options/nosniff/Referrer-Policy/CSP。全仓 grep `cache-control|max-age|immutable|no-cache`（*.sh/*.yml/Caddyfile/*.ts/*.java/*.md）**零命中**。Caddy file_server 只发 ETag+Last-Modified，浏览器据 RFC 9111 §4.2.2 走启发式新鲜期 0.1×(Date−Last-Modified)——报告的算式正确。
2. **旧产物被删**：server-build.sh:35 `rm -rf "$DEPLOY/dist"/*` → :36 覆盖。
3. **全部内容哈希**：frontend/dist/assets 95 个文件，按 `name-HASH.ext` 正则校验**无一例外**；dist 根目录只有 index.html，无 public/ 目录。所以每次构建哈希必变，且 `/assets/*` 下不存在任何非哈希文件。
4. **回退伪装成 HTML**：dist/index.html 是 `<script type="module" crossorigin src="/assets/index-Bg4-NOiY.js">`，缺失哈希文件命中 try_files → index.html → 200 + text/html；ES module 严格 MIME 校验直接拒绝（Caddyfile:10 的 nosniff 更堵死嗅探兜底），入口 chunk 一行不执行，`<div id="app">` 空 → 白屏。
5. **无任何兜底**：frontend/src grep `onError|dynamically imported|ChunkLoadError|location.reload|serviceWorker` 零命中；main.ts 无 router.onError。
6. **未被记录过**：开发待办.md 无缓存/白屏条目（唯一 "缓存" 命中是 ConfigChangedEvent），不属于禁报清单。

## 二、两处需修正报告

**(A) "明天 A 刷新" 恰恰是会自愈的那条路径。** Chrome/Edge/Firefox 对普通 F5/Ctrl+R 会给顶层文档打 validate-cache 标志强制回源，用户按 F5 就拿到新 index.html——**不需要 Ctrl+F5**。真正中招的是走缓存的导航：书签、桌面快捷方式、地址栏输入、会话恢复、新标签页、链接跳转。政务内部系统正是以书签/快捷方式为主，所以 bug 成立；但这也解释了报告说的"运维无从定位"——一线答复会收敛成"按下 F5 就好了"，永远查不到根因，同时也**把爆炸半径封顶**：没人会被卡满两天，卡到有人告诉他刷新为止。

**(B) 严重度随发版间隔放大，"20 天"不是当前节奏。** server-build.sh:36 是 `cp` 未带 `-p`，mtime=部署时刻，故 Last-Modified 年龄=距上次发版时长，新鲜期=0.1×该值。当前 git 节奏 1-3 天（8/28、8/29、8/31），新鲜期仅 2.4-7 小时：**夜间发版基本无害**（隔夜 12h+ 已超期自动回源），**工作日白天发版**才会打到发版前几小时活跃的用户。危险区恰是试运行→生产要进入的状态：间隔拉到 2-4 周时新鲜期涨到 2-4 天，报告描述的"昨天用过的人全白屏"才完全成立。**这是一个随系统稳定而变严重的 bug**，现在改成本只有 4 行配置。

## 三、判定

- **概率**：某些发版必然触发；受影响=发版前 0.1×间隔 内活跃且在该窗口内回访的用户。范围有界、可自愈、但每次发版复发。
- **后果**：纯可用性/体验，**不是错误数据**。入口模块整体不执行=全有全无，无半加载状态、无脏写，属最安全的失败形态。间接风险仅在于这是法定时限系统，"系统坏了"的认知会推迟告知期/听证期/7日内公开等动作，但 F5 可自愈使多日中断不现实。
- **收益**：消除复发白屏；把 200 变回 404 让访问日志能定位；砍掉冷导航对 dashboard 路径十余个 chunk 的条件请求（vendor 264KB、el-table-column 116KB、index.css 49KB、MainLayout 47KB+27KB css、入口 47KB），政务外网延迟下首屏提升可感知。
- **风险**：低。`immutable`+1年 的唯一前提（/assets 全哈希、根目录无非哈希文件）已实测成立。

## 四、建议补丁必须改两处，否则等于没改

**坑 1：`header /index.html Cache-Control "no-cache"` 大概率无效。** header 匹配的是**请求路径**，而 Caddy 默认指令序中 `header` 排在 `rewrite`/`uri`/`try_files` 这组"incoming request manipulation"**之前**，匹配器看到的是回退前的原始路径。用户请求的是 `/` 和 `/dashboard`，从不是 `/index.html`——这些响应仍然一个缓存头都没有，启发式缓存这个**真正病根原样保留**，只有 assets 拿到了一年强缓存。结果是只发出去了"可诊断"那一半，却以为修完了。改成在两个 handle 块内各写一条**无匹配器**的 header，彻底绕开匹配/排序问题：

```
handle /assets/* {
    root * /srv/dist
    header Cache-Control "public, max-age=31536000, immutable"
    file_server
}
handle {
    root * /srv/dist
    header Cache-Control "no-cache"
    try_files {path} /index.html
    file_server
}
```
（回退块只可能吐 index.html——dist 根目录再无他物——所以无匹配器的 no-cache 正好精确。）

**坑 2：改完会同步到服务器但永远不被加载。** 报告说"需 docker compose up -d 重建 caddy 才生效"，而 server-build.sh:46 确实跑了 `sudo docker compose up -d`——但对 caddy 是**空操作**：Caddyfile 是 bind mount（docker-compose.yml:61），compose 只在**服务定义**（image/env/volumes/command）变化时重建容器，**绝不感知 bind mount 的文件内容变化**，而 Caddy 本身无配置热监听。upgrade.sh:27 只 `--force-recreate app`，其 :28 注释还明确写着 caddy 无需重启。所以纯 Caddyfile 提交会落到 /opt/ybcase/Caddyfile 然后被彻底忽略，**部署日志上没有任何迹象**。须给 server-build.sh 补 `sudo docker compose restart caddy`，或更稳的 `sudo docker compose exec caddy caddy reload --config /etc/caddy/Caddyfile --adapter caddyfile`（先校验配置、不断连接）。

坑 2 是既有问题且范围更大：**任何**纯 Caddyfile 改动都会静默失效。git 已证实这种提交真实发生过——4e7a985 单独改 Caddyfile（+9 行安全头）未动 compose；当前安全头之所以是生效的纯属偶然，因为后来的 e533c37 同时改了 compose（给 caddy 加 TZ/日志）才强制重建。而本次建议的修复恰恰又是一次纯 Caddyfile 提交。

## 五、修完仍存的残口（可选，小）
已开着页面跨版本的用户，点菜单触发懒加载 chunk 时会拿到 404（修复后是诚实的 404 而非白屏），vue-router 无 onError 处理 → 点击无反应。补一句 `router.onError(e => { if (/dynamically imported module|Failed to fetch/.test(e.message)) location.reload() })` 即可闭合，约 1 行。

### 13. [HIGH] 督办看板 16 类预警全部无上界，数据量上来后页面不可用
*工程与性能 — `server/src/main/java/cn/ybcase/bureau/web/BureauStatsController.java:55`*

**现状/复现**：本机灌入市级医保局五年量级数据（3102 案件 / 10014 线索 / 20 万审计）后实测：GET /bureau/stats/supervision 中位数 493ms（其余全部接口均在 100ms 内），响应体 932.8 KB，16 类合计 6434 行（clueOverdue 3293、publishOverdue 1166、summaryRecordOverdue 536、caseNearDeadline 424…）。前端 SupervisionView 用普通 el-table 全量渲染、无分页无虚拟滚动：浏览器实测 6434 行表格、46296 个 DOM 节点、约 17 秒页面才渲染稳定。逐条 SQL 单独跑均为 1-3ms，耗时不在数据库，而在无上界的结果集序列化与前端布局。

**影响**：局方最看重的页面在真实数据量下需十几秒才能用，且随案件量线性恶化。督办的目的是看最紧急的若干条，全量返回既无必要也不可用。

**修法**：每类加上界（如 supervision_top_n 默认 100，各查询已按紧急度 ORDER BY，LIMIT 即保留最紧急的）；多取一条判断是否截断，前端提示「共 N 条，显示最紧急 100 条」。仓库已有同类先例（线索状态筛选的 Top200）。

> 核实：本机实测，非代码推断。

### 14. [MEDIUM] 停用账号登录返回 500「系统内部错误」并触发前端一键故障报告
*core 模块 — `core/src/main/java/cn/ybcase/core/web/AuthController.java:47`*

**现状/复现**：admin 停用某账号（PUT /api/system/users/{id}/enabled?enabled=false），该用户随后登录 POST /api/auth/login：DaoAuthenticationProvider 的 preAuthenticationChecks 在校验口令之前抛 DisabledException（AuthenticationException 子类，不是 BadCredentialsException），AuthController:47 只 catch BadCredentialsException → 异常逃逸到 BureauExceptionAdvice.handleAny(Exception)，返回 R.fail(500,"系统内部错误，请联系管理员（详情见服务端日志）")，并打一条 ERROR 全栈。前端 client.ts:73-79 把 code=500 当系统级故障，弹「点顶栏"反馈"可一键报告」并派发 ybcase-api-500 事件。E2E 第 1240 步只验了「停用后旧令牌失效」，没验停用后再登录，所以一直没暴露。

**影响**：每个离职/调岗被停用的人员登录都被系统当成崩溃：用户看到的是内部错误而不是「账号已停用」，还会被引导去提交故障反馈；服务端日志被 ERROR 堆栈刷屏，真实故障淹没其中。

**修法**：把 catch 拆成两段：先 catch DisabledException（或 AccountStatusException）返回 R.fail(1005,"账号已停用，请联系管理员")，再 catch BadCredentialsException 走现有失败计数与锁定逻辑；注意子类分支必须写在前面。

> 核实：成立，且我用项目实际依赖版本做了实证，不是纸面推演。反驳未能成立，但描述里有两处夸大、建议里有一处技术性错误。

【实证：机制确认】
在 `C:\Users\drxuj\.m2\repository\org\springframework\security\spring-security-core\6.5.3` 上跑了一个探针（AuthUser 同款构造：enabled=false，其余三个状态位 true + BCryptPasswordEncoder），结果：
- 口令正确 → `org.springframework.security.authentication.DisabledException`，层级 `DisabledException <- AccountStatusException <- AuthenticationException`，`instanceof BadCredentialsException = false`
- 口令**错误** → 同样是 DisabledException
两者一致，证明 preAuthenticationChecks 确实先于口令校验执行。pom.xml:24 spring-boot 3.5.5 → spring-security 6.5.3，版本对得上。

【链路逐环核实，无人兜住】
1. `core/.../security/SecurityConfig.java:46-48` AuthenticationManager 取自 `AuthenticationConfiguration`；全仓无任何自定义 AuthenticationProvider bean（grep 只命中 JwtAuthenticationFilter 的一行注释和这一行），DbUserDetailsService 是唯一 UserDetailsService bean + 有 PasswordEncoder bean → InitializeUserDetailsBeanManagerConfigurer 装配 DaoAuthenticationProvider。E2E 能正常登录，反证该 provider 确实在位。
2. `core/.../security/AuthUser.java:18` `super(username, password, enabled, true, true, true, authorities)` —— enabled 如实传入，未被抹平。
3. `core/.../security/DbUserDetailsService.java:20-27` 不按 enabled 过滤、不抛 UsernameNotFoundException，disabled 用户照常返回。
4. `core/.../web/AuthController.java:37-43` 登录前只判 lockedUntil，无 enabled 判断；:47 只 catch BadCredentialsException。
5. `server/.../common/BureauExceptionAdvice.java:11` `@RestControllerAdvice(basePackages = "cn.ybcase")` —— 确实覆盖 `cn.ybcase.core.web`（跨模块，core 与 server 同一个上下文）。逐个 @ExceptionHandler 比对：BizException / IllegalArgumentException / AccessDeniedException / MaxUploadSize / 四个 BadRequest / DateTimeParse+NumberFormat / OptimisticLocking / DataAccessException —— DisabledException 一个都不匹配，只能落到 :86 `handleAny(Exception)`，`log.error("未处理异常", e)` + `R.fail(500, "系统内部错误…")`。
6. `core/.../common/R.java` 是纯 POJO，无 @ResponseStatus → HTTP 200 + body.code=500。`frontend/src/api/client.ts:73-79` 正是判 `r.code === 500`。`frontend/src/stores/auth.ts:41` 登录走的就是这个 client 实例。
7. 覆盖缺口属实：`tools/e2e-case.py:1240-1244` 只断言「停用后旧令牌失效」；全仓 `server/src/test`、`core/src/test` 里 grep `auth/login` **零命中** —— 登录的失败分支（1001 错密码、1002 锁定）根本没有任何测试，不止 disabled 这一条。
8. `core/.../web/SysUserController.java:85-96` PUT /api/system/users/{id}/enabled 端点与路径、admin 例外、token_version 自增均与描述一致。
9. 全仓 grep `DisabledException` / `AccountStatusException`：零命中。确无别处兜底。

【描述夸大之处（两点，应修正后再提）】
A. **「触发前端一键故障报告」不成立**。`frontend/src/router/index.ts:6` `/login` 是独立路由，不在 MainLayout 之下；而 `ybcase-api-500` 的唯一监听者 `MainLayout.vue:236,243` `on500 = () => { fb.kind='BUG'; fbVisible.value=true }` 只在 MainLayout onMounted 时注册。登录页上 MainLayout 未挂载 → 事件派发了但**无人监听，反馈弹窗不会自动弹出**。实际后果是：用户只看到一条 6 秒红色 toast「系统内部错误，请联系管理员（详情见服务端日志）（点顶栏"反馈"可一键报告）」，而登录页压根没有顶栏、没有那个"反馈"按钮 —— 是一句指向不存在控件的胡话提示，不是"被引导去提交故障反馈"。
B. **「日志刷屏 / 真实故障淹没其中」是修辞**。每次尝试一条 ERROR 全栈属实，但离职人员一般试一两次；且 `server/.../common/BureauAuditFilter.java:57` 显式 `!uri.equals("/api/auth/login")`，登录被排除在审计表之外，**sys_audit_log 不会被污染**。只有应用日志多几条 ERROR。

【建议里的技术性错误（一点）】
「注意子类分支必须写在前面」在本例是错的：DisabledException 与 BadCredentialsException 是 AuthenticationException 之下的**兄弟**（探针输出的层级链已证实），互不可赋值，Java 里两者 catch 顺序随便写都能编译、语义也一样。这句话只有在还想加一个 `catch (AuthenticationException)` 兜底时才成立 —— 若照抄进代码评审意见，会让人误以为存在不存在的陷阱。

【核实后可加进修复的三条】
1. 只需 catch `DisabledException` 即够：`AuthUser.java:18` 把 accountNonExpired / credentialsNonExpired / accountNonLocked 全部硬编码为 true，LockedException / AccountExpiredException / CredentialsExpiredException 在本系统不可能发生。catch `AccountStatusException` 更保险但非必需。
2. 业务码 1005 确实空闲（全仓 grep 1005/1006 零命中），可直接占用。
3. 安全提示：探针证明**口令错误时也抛 DisabledException**，所以返回「账号已停用」等于在未验证口令的前提下泄露"该账号存在且被停用"。不过 AuthController:40-42 的 1002「账号已锁定」早已在 authenticate() 之前泄露了同等信息，属项目既有取舍，保持一致即可，不必因此否决修复。
4. AuthController:84 的 change-password 有同一个裸 catch，但 disabled 用户到不了那里（SysUserController:93 停用时自增 token_version + JwtAuthenticationFilter:35 显式判 `!user.isEnabled()`），**不是第二处待修点**；只是一旦有人删掉那个 token_version 自增，它就会浮出来。

结论：bug 真实、可复现、无兜底、无测试覆盖，值得修。但报告应把"自动弹出一键故障报告弹窗"降级为"toast 指向登录页上不存在的反馈按钮"，把"日志刷屏、审计污染"降级为"每次一条 ERROR 全栈，审计表不受影响"，并删掉"子类分支必须写在前面"这句错误提示。

> 核实：真实缺陷，已逐环核实，建议修复（低风险小改动）。

【核实链路，每一环都在代码中确认】
1. DbUserDetailsService.java:26 把 user.getEnabled() 传入 AuthUser；AuthUser 构造为 super(username, password, enabled, true, true, true, authorities)，停用后 isEnabled() 确为 false。
2. SecurityConfig.java:47-49 用的是 config.getAuthenticationManager()，即标准 DaoAuthenticationProvider，其 preAuthenticationChecks 在校验口令之前抛 DisabledException。
3. AuthController.java:47 只 catch BadCredentialsException；DisabledException 与之是 AuthenticationException 下的兄弟类，不匹配，异常逃逸。
4. BureauExceptionAdvice 为 @RestControllerAdvice(basePackages = "cn.ybcase")，确实覆盖 core.web；全文无 AuthenticationException/DisabledException 处理器，落到 handleAny(第84行)：log.error("未处理异常", e) + R.fail(500,"系统内部错误…")。
5. frontend/src/api/client.ts 对 r.code===500 走系统级分支，追加「点顶栏"反馈"可一键报告」并 dispatch ybcase-api-500。
6. tools/e2e-case.py:1240-1244 停用后只断言旧令牌失效（401/403），随即进入下一步，且账号保持停用状态——确实从未验证「停用后再登录」。

【对报告的两处修正】
- change-password(第84行)虽是同样的窄 catch，但停用用户根本走不到：JwtAuthenticationFilter:36「if (!user.isEnabled()) throw」已在过滤器层拦掉，按匿名处理返回 401。故受影响面仅限 /api/auth/login，范围比报告所述更小。
- 「子类分支必须写在前面」这条提醒不成立：DisabledException 不是 BadCredentialsException 的子类，二者是兄弟（BadCredentialsException 与 AccountStatusException 同为 AuthenticationException 直接子类）。两个 catch 谁先谁后都能编译、行为一致，不存在顺序陷阱。

【发生概率】条件触发但确定性 100%：只要账号被停用，该用户任何一次登录尝试必然 500，无随机性。触发频率取决于人事变动（离职/调岗），属偶发但必然重复发生的事件；且被停用者往往不知情，习惯性登录，几乎一定会撞上至少一次。

【后果性质：体验+运维问题，不是错误数据】
无任何数据损坏——异常在写库之前逃逸，失败计数与锁定逻辑根本没执行（这反而是正确的：不该给停用账号累计锁定）。鉴权结果也没错，账号仍被拒绝，无安全绕过。实际代价有三层：
1. 用户看到「系统内部错误」而非「账号已停用」，无法自助判断，只能打电话问管理员。
2. 更糟的是被前端主动引导去提交故障反馈——系统在批量制造自己的假故障工单，污染反馈渠道。
3. 服务端 ERROR 全栈刷屏。这是真正的运维成本：ERROR 级别是排障时的第一过滤条件，可预期的业务状态混进去，会稀释信号，真实故障被淹没（狼来了效应）。

【风险收益权衡：明显值得改】
改动风险极低——在既有 catch 上方（或下方，顺序无所谓）加一段 catch，不触碰失败计数与锁定逻辑，不改前端（1005 走 client.ts 的普通 else 分支，直接 ElMessage.error(r.message)，无需联动修改），错误码 1005 当前未被占用（1001-1004 已用，1005 全仓无引用）。
建议 catch AccountStatusException 而非仅 DisabledException：它同时覆盖将来可能启用的 locked/expired 状态；同时务必不要笼统 catch AuthenticationException——InternalAuthenticationServiceException（如数据库故障）是真服务端故障，应当继续 500，不能被伪装成业务提示吞掉。
一处需要知会的取舍：向未登录调用方返回「账号已停用」会确认该用户名存在（枚举风险）。但这不构成新增泄露——第40-42行现有的锁定检查同样在校验口令之前就返回 1002，仓库已确立该惯例；且当前 500 与 1001 的差异本身就已经暴露同一事实。故修复不劣化安全态势。
配套建议：e2e-case.py:1244 之后账号已处于停用态，追加一次登录并断言 code==1005 仅需 2-3 行，成本几乎为零，正好补上这个一直没被覆盖的口子。

### 15. [MEDIUM] 登录成功与失败全程不留痕；core 侧 R.fail 也进不了审计的业务码列
*core 模块 — `server/src/main/java/cn/ybcase/bureau/common/BureauAuditFilter.java:58`*

**现状/复现**：过滤器第 56-58 行的条件把 /api/auth/login 显式排除，全仓再无第二处登录留痕（无 sys_login_log 表，AuthController 里也没有任何 log 语句）。场景：某账号被连续试密 5 次锁定 15 分钟（AuthController:51），用户报障时查 sys_audit_log 一条记录都没有——试了几次、从哪个 IP、什么时候，全都查不到；同理谁在何时登录过系统也无记录。第二个问题在同一处：过滤器第 63-64 行只有 BizException 经 BureauExceptionAdvice.handleBiz 才会写入 BIZ_CODE_ATTR（server 侧 244 处都这么走），而 core 的 15 处 R.fail（1101 用户名已存在 / 1102 口令不合规 / 1104 停用内置管理员 / 1302 授权被拒 / 1401 配置项不存在）是直接返回的，审计里一律记 http_status=200。

**影响**：账号爆破、口令被盗后无法溯源登录来源，等保三级「审计覆盖到每个用户的重要行为」与局令第4号全过程记录都缺登录这一环；同时账号/角色/参数这类最敏感的操作（正是 AuditQueryController 敏感筛选专门盯的 /system/users、/roles、/menus 路径）在审计里失败与成功完全同形，与过滤器第 61-62 行自己写下的设计意图相反。

**修法**：去掉 !uri.equals("/api/auth/login") 排除项——过滤器只记 method/path/status/ip/request_id，不碰请求体，没有凭据泄露风险；同时在 AuthController 的失败分支加 request.setAttribute(BureauAuditFilter.BIZ_CODE_ATTR, 1001/1002)，否则失败登录仍记 200。更彻底的做法是让 core 的控制器改抛 BizException（或在 core 提供等价异常）统一走 advice，把这条口径与 server 拉齐。

> 核实：成立，但建议不完整、第二半严重度被夸大。

【反驳失败：登录零留痕属实，且比描述更彻底】
逐条堵死了兜底可能：(1) BureauAuditFilter.java:58 的 `!uri.equals("/api/auth/login")` 自过滤器诞生（78ffc56）就在，无注释无 ADR，非有意决策；(2) 全仓无 sys_login_log，V1__bureau_core.sql:67 只有 sys_audit_log；(3) grep `AuthenticationSuccessEvent|AuthenticationFailure|AuthenticationEventPublisher|@EventListener` 零命中，Spring Security 事件监听这条兜底不存在；(4) deploy/Caddyfile 无 `log` 指令 —— Caddy v2 默认不写访问日志，反代这条兜底也不存在；(5) application-pilot.yml:29-34 root=INFO，而 DaoAuthenticationProvider 的凭据失败只在 DEBUG 打，落盘日志里同样没有；(6) SecurityConfig:33 确认 /api/auth/login 是 permitAll 且过滤器能到达；(7) 开发待办.md 20 条标题里无登录相关条目，不是重报。「244 处」这个数字也是准的：grep `new BizException` 恰好 244。

【建议有缺陷：照做仍答不出"哪个账号被锁"】
只删排除项会写出 username=null 的行。AuthController 全程没调 SecurityContextHolder.setAuthentication，permitAll 又让 JwtAuthenticationFilter 不填充上下文，所以过滤器第 60 行拿到的 auth 必为 null。结果是"某 IP 试了 5 次登录"可查，"哪个账号被锁"仍不可查 —— 正是这条结论开篇举的那个场景。真正的最小修法要三件：删排除项 + AuthController 把 attempted username 塞进 request attribute 供过滤器读 + 失败分支设 BIZ_CODE_ATTR。顺带，"没有凭据泄露风险"这句在补记 username 后不再完全成立（用户把口令误敲进用户名框是常见情形），值得在实现时对该字段做长度/内容裁剪。

【第二半：事实对，但归因和严重度要下调】
- 「与过滤器 61-62 行自己写下的设计意图相反」站不住。该注释针对的是"越权尝试与成功同形"，而越权这条恰恰是被兜住的：core 四个系统控制器（SysUserController:23、SysRoleController:49、SysConfigController:36、SysDeptController:31/43/56）全是 @PreAuthorize("hasRole('ADMIN')")，非 ADMIN 会抛 AccessDeniedException → handleDenied 写入 403。那 15 处 R.fail 只在"已通过鉴权的 ADMIN 撞业务规则"时触发。
- 1302 被误标为「授权被拒」。SysRoleController.java:54 原文是 `R.fail(1302, "内置管理员角色不可修改授权")`，是保护内置角色的业务规则，不是访问控制拒绝。
- 「/menus」是凑数：SysMenuController 零处 R.fail，敏感筛选的 /menus 路径不受本问题影响。
- 15 处里真正值得进状态列的只有 2 处：1104（停用内置管理员，PUT /api/system/users/{id}/enabled 记 200）和 1302（PUT /api/system/roles/{id}/menus 记 200）—— 审计等于断言"内置管理员被停用了"而实际没有，是假阳性记录，比漏记更坏。其余 13 处（用户名已存在/科室不存在/角色不存在/配置项不存在/口令不合规）是校验噪声。

【结论漏掉的一条，反而使问题更广】
"只有 BizException 经 handleBiz 才会写入 BIZ_CODE_ATTR"低估了范围：BureauExceptionAdvice 自己的 handleIllegal(2000)、handleUpload(2054)、handleBadRequest(2100)、handleBadValue(2100)、handleOptLock(2102)、handleData(2101) 六个处理器同样不设该属性 —— 也就是说 server 侧抛出的参数不合法、重复键、乐观锁冲突在审计里也一律记 200。所以"让 core 改抛 BizException 拉齐口径"不是对症的修法（要改 15 处控制器），把 setAttribute 收敛到 advice 的公共出口是一行改动且覆盖面更大。

【附带发现（不在本条范围，供参考）】
BureauAuditFilter 是默认 LOWEST_PRECEDENCE 的 @Component，而 Security 过滤器链在 -100，故审计过滤器嵌套在安全链内部 —— 被安全层直接拒绝的请求（令牌失效/过期返回 401）根本到不了审计过滤器，这类认证失败同样零留痕。修登录留痕时应一并考虑该位置。

定级：安全/合规缺陷成立，挂等保三级 8.1.4.3「审计覆盖到每个用户的重要行为」是对的；但挂"局令第4号全过程记录"是误引 —— 第4号令的全过程记录约束的是行政执法行为（启动/调查取证/审核决定/送达执行）的文字与音像记录，不含系统认证事件，这半句是修辞加码。建议按"删排除项 + 记 attempted username + 失败分支写业务码 + advice 公共出口统一 setAttribute"实施，工作量约半天。

> 核实：两半都属实，读码逐条证实。

半一（登录零留痕）：BureauAuditFilter.java:56-58 的 `&& !uri.equals("/api/auth/login")` 是唯一原因——该过滤器是裸 @Component OncePerRequestFilter，无 FilterRegistrationBean 收窄路径，POST 在 WRITE_METHODS 内，且未配 server.servlet.context-path，URI 精确命中。全仓无第二处留痕：所有迁移中无 sys_login_log（V1__bureau_core.sql:67 的 sys_audit_log 是唯一审计表），两模块 0 个 AuthenticationSuccess/FailureBadCredentialsEvent 监听器，core/.../web/AuthController.java 与 core/.../security/* 内 0 条日志语句。故 AuthController.java:39-58 的 5 次锁定 15 分钟完全无痕：试了几次、来源 IP、时间全无；成功登录同样无记录。git log -L 显示该排除项自过滤器创建（78ffc56）就在，从无理由说明。

半二（core 的 R.fail 进不了状态列）：仅 BureauExceptionAdvice 的 handleBiz(:16)、handleDenied(:28)、handleAny(:86) 会写 BIZ_CODE_ATTR；core 15 处 R.fail 均直接返回，一律记 http_status=200。

发生概率 100%、每日发生。后果两半不同，报告者混为一谈了：登录侧是「数据缺失」；R.fail 侧是「错误数据」，更严重——SysRoleController.java:54 拒绝修改内置 ADMIN 角色授权时，审计里却记 `PUT /api/system/roles/1/menus 200`，即肯定地宣称最敏感的权限变更成功了；而这恰是 AuditQueryController.java:29 敏感筛选专盯的 /system/users、/roles、/menus 路径，与过滤器 :61-62 自述的设计意图直接矛盾。

对报告的两处修正：(1) 第二半严重性被夸大——handleDenied 已盖 403，真正的越权尝试是可区分的，残留的只是业务规则拒绝（1101/1102/1104/1301/1302/1401）；(2) 建议的补丁编译不过——server/pom.xml:19 显示 server 单向依赖 core，core 里的 AuthController 无法引用 server 里的 BureauAuditFilter.BIZ_CODE_ATTR，须先把该常量（或整个过滤器）下沉到 core。

「直接删排除项」还有一个坑：过滤器 finally 执行时登录请求的 SecurityContextHolder 认证为 null（login 是 permitAll、尚无 JWT），只会得到 username=null——有 IP/时间/次数，但查不到被试的是哪个账号、谁登录了。须由 AuthController 经 request attribute 传入用户名；且传入时要设防（仅在 userOpt.isPresent() 时记录或截断），否则用户把口令误敲进用户名框就会被当作「用户名」持久化进 sys_audit_log。

风险收益明显偏向修复：删一个 && 子句加几处 setAttribute，审计 insert 本就包在吞异常的 try/catch 里、不会拖垮登录；唯一成本是表增长，可忽略。对面是等保三级「审计覆盖到每个用户的重要行为」完全缺登录环节，且敏感操作审计正在主动记录与事实相反的结果。

### 16. [MEDIUM] publish() 用 deliveredAt 非空判"已送达"，公告送达时该值是未来日期，可提前60天公开处罚决定
*核心服务 — `server/src/main/java/cn/ybcase/bureau/service/CaseService.java:624`*

**现状/复现**：deliver({method:"ANNOUNCE", deliveredAt: 今天})：596-598行按辽60条把法定送达日算为公告日+announce_deliver_days（默认60），即 deliveredAt = 今天+60（未来日期），608-609行同时把状态直接置为 DELIVERED。紧接着调用 POST /bureau/cases/{id}/publish：publish() 624行只判 `cf.getDeliveredAt() == null`，此时非空 → 守卫放行，决定书在法定送达日之前60天就被登记公开。

**影响**：publish() 621-625行的注释写明该守卫的目的是"未送达即公开会侵害当事人陈述救济权"，而公告送达恰恰是当事人下落不明、最依赖公告期保障知情权的场景——守卫对它完全失效。同一份未生效送达还会让 close() 690行的 `!"DELIVERED".equals(status)` 判断通过（公告期未满即可结案）。注意 ExecutionService:112/140 与 MessageService:86-95 用的是 deliveredAt 的日期算术（deliveredAt + payment_days），未来日期在那里是正确的，所以问题不在 deliver() 存未来日期，而在 publish()/close() 拿"非空/状态"当"已送达"的代理——同一概念在同一个文件里两套判法。

**修法**：publish() 624行改为 `if (cf.getDeliveredAt() == null || cf.getDeliveredAt().isAfter(LocalDate.now())) throw new BizException(2042, "处罚决定书尚未送达（公告送达自公告之日起满" + config.intVal("announce_deliver_days", 60) + "日视为送达，法定送达日 " + cf.getDeliveredAt() + "），不得先行公开（辽56条）");`。close() 690行的"已送达"判断同样应从状态改为 `c.getDeliveredAt() != null && !c.getDeliveredAt().isAfter(LocalDate.now())`。

> 核实：核心机制成立，但修复建议是错的，close() 那一半被严重夸大。

【成立部分——逐行核对无误】
C:\Users\drxuj\Projects\医保案件\server\src\main\java\cn\ybcase\bureau\service\CaseService.java
- 597-598：ANNOUNCE 时 deliveredAt = 公告日 + announce_deliver_days（V8__hardening.sql:30 播种 60）
- 608-609：无条件 setStatus("DELIVERED") + setDeliveredAt(未来日期)
- 624：确认只有 `cf.getDeliveredAt() == null`
未被别处兜住，四处都查过：CaseController.java:228-231 的 /publish 无 @PreAuthorize、无额外守卫；前端 CaseDetailView.vue:314-315 的"公开（7日内）"按钮只按 `decision.published` 取反渲染，不看送达；tools/e2e-case.py:862 的公告用例故意取 `ann_date = today - 61天`，算出的送达日已是昨天，唯一的 ANNOUNCE 覆盖恰好绕开了未来日期窗口。所以"公告期内可登记公开"确实可复现。

【建议不能照抄——会和系统自己的 7 日 SLA 死锁】
这是审阅完全没提的：V3__province_params_guards.sql:23 播种 `decision_publish_days=7`，注释"处罚决定作出后公开期限"；BureauStatsController.java:130-135 的 publishOverdue 判据是 `d.decided_at + 7 < current_date`——从**作出日**起算，不是送达日。公告送达下 deliveredAt ≥ decidedAt+60。若按建议把 publish() 卡到 `!deliveredAt.isAfter(today)`，该案 60 多天内一律拒绝公开，而 SupervisionView.vue:87 的"处罚决定公开超期"看板从第 8 天起就把它永久标红，办案人无任何合法动作可做。审阅说"同一概念两套判法"，实际是三个时钟（decided_at 的公开期 / deliveredAt 的日期算术 / status 代理），它的一行修复让前两个直接对撞。真正要定的是辽56条的公开期到底挂哪个起点，并同步改 publishOverdue 或对 ANNOUNCE 豁免——不是加一个 if。

【close() 那一半基本已被兜住，描述夸大】
690 行确实放行，但 PUNISH 分支紧接着 691-698 还要过 courtEnforceApplied 或 fullyExecuted。ExecutionService.java:139-142 的 applyCourtEnforce 用的正是审阅承认"正确"的日期算术：`today > deliveredAt + payment_days`，公告场景下即公告+75 天，公告期内根本申请不了——未来日期在这里恰好起了保护作用。于是只剩 fullyExecuted（CaseService.java:910-914，罚款/退回/没收三类全额缴清）。也就是说"公告期未满即可结案"要求一个"下落不明"的当事人把每一分钱都主动缴清——既与前提自相矛盾，真发生了也正是第56条(一)执行完毕的正当结案情形。把它与 publish() 并列为等重缺陷不成立。

【危害表述也偏了】
621 行注释所称"侵害陈述申辩救济权"：陈述申辩是决定前的权利（告知前置守卫已覆盖），决定后的救济是复议/诉讼，其期间自实际/视为送达起算，不因公开而缩短；且公告送达本身就是把同一份决定张贴公示。说"守卫对它完全失效"是拿一个本就不贴合该场景的立法目的做放大。站得住的危害是另一件事：把尚未生效、期间内仍可能被撤销变更的决定提前推进 OversightController.java:140-148 的 publish-export 信用公示导出。

【附带发现，审阅漏了】604-606 行把算出的未来日期写进 case_delivery.delivered_at，真实公告日（req.deliveredAt）不落库、receiptSignedAt 为空，DocumentService.java:269 打印的"送达日期"因此是拟制日，公告日只活在 note 自由文本里。审阅建议的报错文案想回显公告日，只能用 deliveredAt 减参数反推，而"参数变更无历史"已在开发待办里——反推会错。

> 核实：属实（publish 部分确认；close 部分技术上成立但价值低、不建议照改）。

【已核实的代码事实】
- CaseService.java:596-598 —— ANNOUNCE 分支 deliveredAt = 公告日 + config.intVal("announce_deliver_days", 60)（V8__hardening.sql:30 种子值 60），609 行把这个未来日期写进 case_file.delivered_at，608 行同时把状态直接置为 DELIVERED。
- CaseService.java:624 —— publish() 的守卫只有 `if (cf.getDeliveredAt() == null)`。公告送达登记完成当天该字段已非空，守卫直接放行。621 行注释白纸黑字写明这条守卫的目的是"应在决定书送达后（未送达即公开会侵害当事人陈述救济权）"，而它对最依赖公告期保障知情权的场景完全失效。同一文件里"已送达"存在两套判法（非空 / 状态），这是内部不自洽，与法条解释争议无关也成立。
- 报告人对 ExecutionService:112(lateFeeQuote)、:140-141(applyCourtEnforce)、MessageService:86-95、BureauStatsController:153-190 的判断正确：那些地方全是 delivered_at + payment_days 的日期算术，未来日期在那里恰恰是对的。所以问题不在 deliver() 存未来日期，定位准确。

【发生概率：场景稀少，但落进场景后几乎必然触发】
医保处罚相对人多为定点医药机构、地址固定，公告送达主要用于参保人骗保、机构失联等少数案件，占比低。但一旦是 ANNOUNCE + PUNISH，前端和看板会主动把办案人往坑里推：
- frontend/src/views/case/CaseDetailView.vue:313-315 的按钮文案就是"公开（7日内）"，只要 decision.published 为假就显示，完全不看送达状态；
- BureauStatsController.java:130-135 的 publishOverdue 用 `d.decided_at + 7 < current_date and published = false`，从决定后第 8 天起把该案挂成红色超期。
办案人为消掉这条督办红项去点"公开"，服务端放行——系统等于在指导用户违规。条件概率高，不是理论可能。

【后果：错误数据 + 真实法律后果，且不可回滚】
不是体验问题。publish() 写入 published=true / published_at=今天；OversightController.java:140-148 的 publish-export 把它导出成对外公示清单，由人工搬到政府网站/信用渠道。行政处罚决定书自送达之日起对当事人生效，公告送达的拟制送达日未到，复议 60 日、诉讼 6 个月救济期尚未起算——此时把处罚信息推入公示与联合惩戒渠道，是公开一个尚未生效的决定。写坏的数据还改不回来：627-628 行的幂等守卫（前几轮特意加的，防止刷洗"7日内公开"的超期证据）让重复调用直接抛错，系统没有撤销公开的入口，published_at 早于 delivered_at 的记录会永久留在案卷与公示导出里。

【close() 那半条：成立但别照抄】
690 行确实用状态而非日期判"已送达"，公告期未满可通过。但危害路径基本被下游堵死：PUNISH 分支要么 courtEnforceApplied（ExecutionService:140-141 用日期算术，公告日+75 天前根本申请不了），要么 fullyExecuted（CaseService.java:910-913，须有真实入账）。唯一能走通的是"公告期内当事人主动缴清"——钱已收讫，此时拦下来反而把案件卡死 60 天，属过度纠偏。建议只改 publish()，690 行维持现状或另行讨论。

【改动风险与收益】
收益明确、风险很低：publish() 是单点方法，只加一个日期条件；历史数据中正常送达的 delivered_at 都是过去日期，不受影响；server/src/test/java/cn/ybcase/bureau/GuardIntegrationTest.java:241-253 的既有测试只覆盖 NO_PUNISH 分支，不会被打破；E2E 脚本里没有 ANNOUNCE 用例。报告人给的改法（`|| cf.getDeliveredAt().isAfter(LocalDate.now())`）方向对，错误文案带上法定送达日也对——否则办案人会以为系统坏了。
配套要补一条，否则是拿静默错误换永久假红：publishOverdue（BureauStatsController.java:130-135）应排除 delivered_at 为空或在将来的案件，或对公告送达按法定送达日起算 7 日。注意这个假红其实现在就已存在（决定后 7 天内尚未送达的案件同样是想公开也公开不了却被标超期），本次修复只是把它从"未送达"扩展到"公告期内"，不是新引入的问题类别，但既然动这块就一并收掉。

### 17. [MEDIUM] addMeeting/recordStatement 无归档封卷守卫，已立卷案件仍可被写入集体讨论与陈述申辩记录
*核心服务 — `server/src/main/java/cn/ybcase/bureau/service/CaseService.java:433`*

**现状/复现**：对一件已 close() 的案件（status=CLOSED、archive_no 已生成）调用 POST /bureau/cases/{id}/meetings {heldAt, attendees, record, conclusion}：addMeeting 434行只有 `get(caseId)`，既无 requireActive 也无归档判断，记录直接入 case_meeting，并被 DocumentService:246-251 渲染进案卷材料、DocumentService:320 写入案件大事记时间轴。POST /bureau/cases/{id}/statement 同样：recordStatement（398行）只按 notice 定位，对案件状态无任何校验，可在 DECIDED/DELIVERED/CLOSED 之后改写 statement、statementWaived、hearingRequested、hearingHeldAt。两个接口在 CaseController:213/218 均无 @PreAuthorize。

**影响**：封卷口径在同一层里被写了两遍又漏了两处：addDocument 176-179行明确以 `"CLOSED".equals(status) || archiveNo != null` 抛 2031 "案件已立卷归档，不可新增文书"，EvidenceService.crossExam:88-90 特意照抄了同一判法（连注释都写了"同 CaseService.addDocument"），而同属卷内法定材料的集体讨论记录和陈述申辩记录没有。后果是第57条"一案一卷"的卷宗在归档后仍可被追加/篡改材料——集体讨论记录还是 2009 继续延期与 2047 较大数额罚款两道守卫的证据基础（hasValidMeeting:902），事后补录会让审计无法区分"决定前讨论"与"事后补卷"。

**修法**：把 addDocument 176-179 行的封卷判断提取成 CaseGuards 里的 `requireNotArchived(CaseFile c)`（CaseService、EvidenceService 已各写一份，正好三处复用），在 addMeeting 434行与 recordStatement 399行前调用；recordStatement 另建议再限定 `List.of("NOTIFIED").contains(c.getStatus())`，因为它唯一的合法时机就是告知之后、决定之前。

> 核实：成立，但描述有夸大也有遗漏，且遗漏的部分比夸大的部分更严重。

【核实为真的部分】
- CaseService.java:433-435 addMeeting 确实只有 `get(caseId)`，无 requireActive、无 archive 判断；CaseService.java:398-399 recordStatement 确实只按 notice 定位，对 CaseFile 状态零校验。
- 对照组属实：addDocument 176-179 与 EvidenceService.crossExam:88-90 都写了 `"CLOSED".equals(status) || archiveNo != null`，后者注释明写"同 CaseService.addDocument"。同层同口径写两遍、卷内法定材料漏两处，事实成立。
- 确实进卷：DocumentService:245-252 把 case_meeting 渲染成 MEETING_RECORD 卷内材料，:193-208 把 statement/statementReview/statementWaived/hearingHeldAt 渲染进告知与陈述申辩材料，:320 写入大事记。
- 确实无别处兜住：CaseScopeInterceptor 只管数据范围（SELF），不看状态；无 DB 触发器/规则；无 @PreUpdate 监听；server/src/test 与 tools/e2e-case.py 均无结案后写入的用例。
- 案卷本身确实留不下痕迹：V2__bureau_case.sql:151-158 的 case_meeting 无 created_at，held_at 完全由入参决定（`req.heldAt() != null ? req.heldAt() : now`），可任意回填到决定之前。

【夸大之处（四点）】
1. "可在 CLOSED 之后改写 statement、statementWaived、hearingRequested、hearingHeldAt"——四个字段里有两个默认配置下已被拦住：`statement` 命中 CaseService.java:401-404 的 2046（now > statementDeadline，V3 seed statement_deadline_days=3）；`hearingRequested=true` 命中 :414-417 的 2046（notifiedAt+hearing_request_days，默认 3）。实际可写的只有 statementReview、statementWaived、hearingHeldAt，以及 hearingRequested 置 false（该分支不走 2046）。注意这层保护是配置相关而非结构性的：statement_deadline_days 配成 0 时 statementDeadline 为 null，`statement` 的保护整个消失。
2. "集体讨论记录是 2009/2047 两道守卫的证据基础，事后补录"——暗示可绕守卫，不成立。extend() 在 :268 先 requireActive；decide() 在 :464/:473 要求 INVESTIGATING/NOTIFIED，且 :453 的 2040 挡住二次决定。归档后补录的讨论记录永远喂不到这两道守卫。危害只是卷宗完整性，不是守卫绕过。
3. "审计无法区分"过头。BureauAuditFilter 把每个 POST（含 /cases/{id}/meetings）连用户名、真实服务端时间、IP、request_id 写进 sys_audit_log，审计员从日志能查出这是结案后写的。准确说法是：卷宗这一法定载体上查不出来（无 created_at + held_at 可回填），而不是"审计查不出来"。
4. "两个接口均无 @PreAuthorize"是凑数。CaseController 28 个写接口只有 10 个带 @PreAuthorize，录入集体讨论/陈述申辩本就是办案人员职责，不加角色限制是设计而非缺陷。（行号也偏了，映射在 210 和 215。）

【遗漏之处（三点，比夸大更要紧）】
1. 漏了第三处该改的地方：GoLiveController.java:88-97 signMeeting 用裸 jdbc 把 `sign_confirmed = true` + sign_names 写进去，同样无状态、无归档判断。而 sign_confirmed 恰恰是 hasValidMeeting(CaseService.java:902-907) 唯一认的字段、也是 DocumentService:251 打印"已签字确认"的依据。建议里的 requireNotArchived 复用点应是四处（addDocument / crossExam / addMeeting / signMeeting）加 recordStatement，不是三处。
2. 陈述申辩这一半根本不需要构造 POST，走正常 UI 就能改：CaseDetailView.vue:52 的 `<el-button @click="dlg.statement = true">陈述申辩/听证</el-button>` 没有任何 v-if/:disabled，而同屏 76 行的集体讨论按钮写了 `v-if="!['CLOSED','TERMINATED'].includes(c.status)"`。296 行 el-table 里的"签字确认"按钮同样无门禁。所以已结案案件在页面上就能开框提交、能给旧讨论记录补签字；反倒是 addMeeting 因为前端挡住了，才需要审阅者说的直连 POST。可达性比原描述高。
3. statementWaived 的破坏力被低估：DocumentService:206-207 是 `statement_waived ? "明确放弃" : statement`——归档后翻这一个布尔值，就能把当事人已录入的陈述申辩内容从打印卷宗里整个换成"明确放弃"。也就是说审阅者关于"改写 statement"的结论在字段层面错了，在卷宗呈现层面反而是对的。项目自己也知道这字段危险：CaseDetailView.vue:1350-1351 的注释就写着漏复位 statementWaived 会在别的案卷里"写下当事人放弃陈述申辩的假事实"。

【结论】按第57条一案一卷的口径，这是真缺口且值得修：归档后卷内法定材料仍可增、可改、可补签，且卷宗自身不留痕。收敛后的准确表述应是"归档封卷守卫漏了三处（addMeeting、recordStatement、signMeeting），其中两处从正常 UI 即可触达；影响是卷宗完整性与事后篡改，不涉及法定守卫绕过，审计日志仍可溯源"。审阅者建议的 requireNotArchived 提取方向正确，只需补上 signMeeting；recordStatement 另加 `NOTIFIED` 限制也站得住——notify 置 NOTIFIED、decide 要求 NOTIFIED、2075 又要求听证先于决定，简易程序无 notice 会先被 2006 挡掉，不存在其他合法时机。

> 核实：Confirmed by reading the code. CaseService.java:433-441 addMeeting opens with a bare `get(caseId);` — no requireActive, no archive check — and inserts into case_meeting unconditionally. CaseService.java:398-425 recordStatement resolves only the latest case_notice and never inspects the CaseFile; its internal guards are partial (the deadline check fires only when statement != null, the 3-day check only for hearingRequested), while statementWaived and statementReview have no time or state bound at all and can be rewritten at DECIDED/DELIVERED/CLOSED. The duplicated closure rule is real: CaseService.java:176-179 and EvidenceService.java:87-90 (whose comment says "same as CaseService.addDocument") each carry `"CLOSED".equals(status) || archiveNo != null`. Downstream is as claimed: DocumentService.java:245-252 renders MEETING_RECORD into archive material, :194-207 renders the notice statement/statement_waived, :320 puts meetings on the timeline, and hasValidMeeting (:902) is the evidence base for guards 2009 and 2047.

Three corrections/additions to the report. (1) It missed a third unguarded write path: GoLiveController.java:88-98 signMeeting has no case-state check either, and it is the write that flips sign_confirmed — the only column hasValidMeeting keys on — so patching addMeeting alone still leaves an archived case's discussion record (re)signable. tools/e2e-case.py:1352-1356 exercises exactly that today: case cid is CLOSED at line 244 and re-signed at 1353. (2) Severity is aggravated by schema: case_meeting (V2__bureau_case.sql:152-159) has no created_at, only user-supplied held_at, so a post-archive insert is indistinguishable inside the case file; BureauAuditFilter.java:66 does log the HTTP call to sys_audit_log, but AuditQueryController.java:27-32's sensitive filter lists /decide, /close, /executions and not /meetings or /statement. (3) Probability is much lower than implied: CaseDetailView.vue:76 already hides the 集体讨论记录 button for CLOSED/TERMINATED and :52 renders 陈述申辩/听证 only under status === 'NOTIFIED', so the UI already enforces both rules. The gap is server-side defense-in-depth, reachable only by direct API call from an authenticated case-scoped user (no @PreAuthorize on CaseController:213/218, consistent with /notice and the other case-work endpoints) or by a future UI change.

Consequence is wrong data, not UX: falsified 卷内材料 after 一案一卷 closure, with no in-file way to tell "discussed before the decision" from "backfilled after archiving". Fix is cheap and low risk: extract requireNotArchived (CLOSED || archiveNo != null) into CaseGuards for addMeeting and signMeeting — it matches the frontend's existing visibility rule exactly, so zero UI breakage; only e2e-case.py:1352 must move to a non-archived case. For recordStatement prefer CaseGuards.requireActive over the proposed NOTIFIED-only list: it blocks every post-decision rewrite at issue without dead-ending the case where 告知 is followed by 中止 (suspend() accepts NOTIFIED) and the party's 申辩 then arrives with no override path. All seven E2E statement calls sit at NOTIFIED, so neither variant costs test churn. Not a duplicate: the ten-round fixed list covers 封卷按 archive_no 判 for addDocument/crossExam only, and 开发待办.md has no entry for these two (three) sites.

### 18. [MEDIUM] 把封顶写进 insert 的 where 并不能在 READ COMMITTED 下阻止并发超额入账
*其余服务 — `server/src/main/java/cn/ybcase/bureau/service/ExecutionService.java:72`*

**现状/复现**：PostgreSQL 默认 READ COMMITTED 下，语句内的子查询看到的是本语句开始时刻的快照，看不到并发事务尚未提交的行，且没有任何锁把两笔入账串起来。两个并发请求（注释中自己点名的“双击提交、超时重试”）同时执行这条 INSERT ... SELECT ... WHERE (select sum(amount) ...) + ? <= ? 时，双方读到的 sum 都是旧值，两条都通过 where、都插入、都提交。LATE_FEE 分支（第 88-96 行）同构。receipt_no 唯一索引不兜底：空/未填票据号被归一为 null，分期自动入账更是显式传 null。

**影响**：注释声称“由数据库一次性判定”，实际并未成立；而超时重试恰恰是首个事务仍未提交的场景，正是最容易撞上的一种。落库后 sum(FINE/RECOUP/CONFISCATE) 超出决定书金额，close() 的 fullyExecuted 判定“执行完毕”而实际多收，或 LATE_FEE 累计超过罚款本金（第55条硬上限）。

**修法**：在 addExecution 入口按案件加行锁把同案入账串行化：jdbc.queryForObject("select id from case_file where id = ? for update", ...) 之后再做预检与插入；或对 case_execution 按 (case_id, kind) 建汇总表并用 update ... where 汇总+新增 <= 决定额 的受影响行数判定。只靠 INSERT ... SELECT ... WHERE 子查询在 READ COMMITTED 下不构成互斥。

> 核实：成立，但严重度应下调，且两处描述需修正。

【核心机制核实：反驳失败】
ExecutionService.java:72-79 与 88-95 的 `insert ... select ... where (select sum(amount) ...) + ? <= ?` 在 PostgreSQL READ COMMITTED 下确实不构成互斥：子查询按语句快照读，看不到并发未提交行，且对"不存在的行"不取谓词锁。逐条排除了所有可能的兜底：
- 隔离级别未抬高：全仓无 @Transactional(isolation=)、无 default_transaction_isolation、deploy/docker-compose.yml 无 postgres command 覆盖 → 默认 READ COMMITTED。
- 无行锁：全仓 grep 无任何 `for update`；CaseGuards.java:14 的 get() 是裸 findById，addExecution 全程不 save CaseFile，CaseFile 实体亦无 @Version 字段 → 插入前该事务不持有任何既有行的锁。
- 无库端约束：case_execution（V2__bureau_case.sql:187）只有 PK/FK 与票据号唯一索引，无 CHECK、无 EXCLUDE、无触发器。
因此 69-71 行注释"由数据库一次性判定"是**事实错误**，这是本条最值得修的部分——它会让下一位审阅者直接跳过。

【部分被兜住（审阅者低估了两处）】
1. 票据号唯一索引其实能兜一大半：V21 的 uq_execution_receipt 是部分唯一索引，唯一索引在 READ COMMITTED 下**确实**串行化（后插入者阻塞在前者未提交的索引项上，前者提交后报唯一冲突）。真实"双击提交"重发的是同一个 receiptNo，只要经办人填了票据号就会被挡住。暴露面仅限**留空票据号**的登记（前端 CaseDetailView.vue:1003 默认 receiptNo:''，归一为 null；仅 ONSITE 强制要求票据，见 line 52）。
2. 分期自动入账路径**并不暴露**：OversightService.payInstallment 有 `update case_installment set paid_at = ? where id = ? and paid_at is null` + n==0 抛错，这是真正的行锁+最新版本重判，同一分期的并发重复缴纳被串行化；且 addInstallment 已把计划总额封顶 ≤ 决定额。审阅者以"分期显式传 null"暗示该路径同样裸奔，这一句是错的。

【描述夸大之处】
- "超时重试恰恰是首个事务仍未提交的场景"：全仓无任何自动重试（frontend/src/api/client.ts:11 只有 timeout:15000，无 retry 拦截器），需人工在 15 秒超时后手动重点，而这条 INSERT 事务是毫秒级——只有服务端/库严重卡死时窗口才成立，远非"恰恰"。
- "close() 的 fullyExecuted 判定执行完毕而实际多收"表述自相矛盾：超额入账会让 fullyExecuted 为真恰恰是因为记多了。真实危害应改述为：多出的执行记录**无删除接口可更正**，并直接污染 BureauStatsController 的 fineCollected/recoupCollected 与督办看板口径。
- LATE_FEE 分支还有个附带小问题（本条未提，不必单列）：d.getFineAmount() 为 null 时 where 恒为 NULL → n=0 → 抛"加处罚款累计不得超出罚款数额"，提示词不对症，但不会崩。

【与"勿重报"清单的关系】
清单正确性一栏有"执行封顶原子化"。本条不是重报未修项，而是指出**该已记录的修复并未真正生效**（注释宣称的原子性不成立），属于对既有修复的有效证伪，可以计入。

【结论】
真实缺陷，但等级建议为中低而非高：触发需同案同 kind 的真并发 + 票据号留空，概率低；修复成本极低。建议按原文第一种方案，在 addExecution 入口 `select id from case_file where id = ? for update` 串行化同案入账，并同步把 69-71 行的错误注释改掉。

> 核实：真 bug，判定成立，且值得修——但报告对暴露面的估计偏高，需要修正两处。

【机制核实，成立】
`ExecutionService.java:72-79` 与 `:88-95` 的 `INSERT ... SELECT ... WHERE (select sum(...)) + ? <= ?` 在 READ COMMITTED 下确实不构成互斥。PG 的 RC 是语句级快照：子查询看不到并发事务未提交的行，INSERT 之间只有互相兼容的 RowExclusiveLock，子 SELECT 是纯 MVCC 读，不加行锁也不阻塞。T1/T2 各自读到旧 sum、各自通过 where、各自插入、双双提交，累计即超决定额。全仓 grep 无 isolation/SERIALIZABLE/LockModeType/for update 任何配置或用法，Spring 默认走库默认 = RC，确认前提成立。

值得点名的是作者为何会写错：同文件 `OversightService.payInstallment`（`:187`）的 `update case_installment set paid_at = ? where id = ? and paid_at is null` 在 RC 下**是**原子的——UPDATE 拿到行锁后会用最新版本重新求值 where（EvalPlanQual），第二笔必然拿到 0 行。作者把这个正确的 UPDATE 模式外推到了 INSERT...SELECT，而 INSERT 的子查询没有这个重求值。所以 `:70-71`「由数据库一次性判定」这句注释断言了一个不成立的安全性质。注意「执行封顶原子化」在已修清单里——本报告不是重报，而是指出那次修复本身是错的，这恰恰是它的价值。

【报告高估的两点，需修正】
1. 「分期自动入账显式传 null」不构成额外暴露面。`payInstallment` 入口已被上面那条原子 UPDATE 串行化，同一期并发双击第二笔必然抛错、根本走不到 addExecution；不同期并发则总额受分期计划封顶，不会溢出。
2. `uq_execution_receipt`（V21，谓词已含空白排除）对**填了票据号**的那部分是真兜底——ONSITE 强制填（`:52-53`），银行缴付有财政票据时通常也填。真正裸奔的是：手工登记 + 非 ONSITE + 票据号留空 + 同案同 kind 并发两笔。

【概率：低，但非零】
- 触发需同案同款项类型的两笔并发。市局单点部署、办案人员十几人、执行登记是低频手工动作，两人同秒撞同一案件很少见。
- 报告点名的「超时重试」路径确实通：`frontend/src/api/client.ts` 是 `timeout: 15000` 且**无自动重试**，`CaseDetailView.vue:1007-1018` 的 `submitting` 防双击在 `finally` 里复位——超时后按钮重新可点，而首个事务仍在服务端跑。但 addExecution 只有几条短查询，跑满 15s 在这个体量下不现实。所以需要人为再点一次，属于小概率而非常态。

【后果：错误数据，且不可自愈】
不是体验问题。落库后 case_execution 累计超决定额，或 LATE_FEE 突破第55条罚款本金硬上限——正是这段代码存在的唯一目的被绕过。更关键的是 `CaseController.java:244` 只有 POST，全仓无 `delete from case_execution`、无作废/冲正入口：一旦写进去，应用层没有任何办法改回来，只能 DBA 手工改库。行政处罚案卷是要接受审计的，卷内出现「累计收缴超出决定书金额」是硬伤。

【风险收益：明显划算，建议修】
按建议在 addExecution 入口加 `select id from case_file where id = ? for update`（在 `:35` CaseGuards.get 之后、预检之前），两行，无 schema 变更，单请求路径行为完全不变，只把同案入账串行化，锁持有仅毫秒级。死锁风险实测为零：`close()` 读 case_execution 走的是普通 SELECT，不加行锁、不进锁等待图，与 addExecution 的 case_file 行锁构不成环；`approveDefer`/`applyCourtEnforce` 只是被短暂阻塞，这本身也是想要的。唯一提醒：这将是全仓第一处悲观锁，需留注释说明为何 INSERT...SELECT 在 RC 下不够，否则下一个人还会踩。同时把 `:70-71`、`:86-87` 那两段失实注释改掉。

【顺带同类，一并处理】
`OversightService.java:161-168` 的分期计划封顶是同一形态的先查后插（sum 后 insert，中间无锁）。同案两条分期并发录入可让计划总额超决定额。后果轻得多（计划而非台账，且可改），但同一个 for update 覆盖即可，不必单开一轮。

### 19. [MEDIUM] 延期审批单 payload.days 为 null 时批准报 500，单据永远批不下来
*web 控制器 — `server/src/main/java/cn/ybcase/bureau/service/ApprovalService.java:121`*

**现状/复现**：办案员在案件详情「申请审批」里选 EXTEND，把「延长天数」输入框清空（el-input-number 的 valueOnClear 默认就是 null，见 node_modules/element-plus/es/components/input-number/src/input-number.vue_...mjs:127），填理由提交。CaseDetailView.vue:920 发出 payload {"days":null}；FormalizationController.java:85 的 POST /api/bureau/approvals → ApprovalService.apply() 对 EXTEND 的 payload 不做任何校验（只校验 kind/reason/caseId），单据正常入库。负责人在「待办审批」点批准 → decide() 走到 ApprovalService.java:121 的 ((Number) p.getOrDefault("days", 0)).intValue()：HashMap.getOrDefault 在 key 存在而值为 null 时返回 null，不返回默认值 0 → NPE。BureauExceptionAdvice.java:53 刻意不接 NPE/CCE，落到 handleAny → R.fail(500, "系统内部错误")。API 客户端传 "days":"30"（字符串）同理得 ClassCastException → 同样 500。

**影响**：decide() 是 @Transactional，500 后抢占 update 回滚，单据仍是 PENDING——这张延期审批单永远批不下来，负责人每点一次批准就 500 一次，只看到「系统内部错误，请联系管理员」，办案员端毫无提示。第45条延期是办案期限届满前唯一的合法延长通道（办案员自己没有 /cases/{id}/extend 权限，必须走这条审批流），只能靠驳回重报绕开。同一条路由对 FILE_CASE 走 objectMapper.convertValue 能自动转型，唯独 EXTEND 是裸强转，问题只在这一支。

**修法**：在 ApprovalService.apply() 里对 kind=EXTEND 提前用 ReqValues.reqInt(req.payload(), "days", "延长天数") 校验，提交时就以 2071/2100 报错；execute() 第121行改用同一取值方式（或至少 p.get("days") instanceof Number n ? n.intValue() : 0）。前端 el-input-number 补 :value-on-clear="1" 兜底。

> 核实：成立，机制逐环核实并实测复现。(1) ApprovalService.apply():38-44 对 EXTEND 的 payload 零校验，单据正常入库；(2) 全仓无 default-property-inclusion/NON_NULL/自定义 ObjectMapper（application.yml 只配 jackson.time-zone），{"days":null} 原样落 biz_approval.payload（text 列，无约束）；(3) 本地 java T.java 实测确认 LinkedHashMap{days=null} 上 ((Number)m.getOrDefault("days",0)).intValue() 抛 NPE "because the return value of Map.getOrDefault is null"，days="30" 抛 CCE；(4) BureauExceptionAdvice.java:49-52 注释明确写"刻意不含 NullPointerException/ClassCastException"，落 handleAny → 500；(5) decide() @Transactional 回滚抢占 update，单据回到 PENDING，可无限重现。前端触发路径确认：CaseDetailView.vue:616 el-input-number 无 :value-on-clear，element-plus input-number.mjs:81-88 默认 valueOnClear=null 且 handleInput 清空瞬间即 emit null；onApply():915 只校验 reason；dlg.apply=true 打开弹窗不重置表单（重置只在切案的 :1359），清空值会持续存在。无 @Valid、无 DB 约束、无前端兜底，E2E tools/e2e-case.py:923 只跑 {"days":5} happy path，测不到——确未被别处兜住。反向佐证强化判定：同一动作的直接路由 CaseController.java:180 写的正是 int days = reqInt(body,"days","延长天数")，是仓库既定写法；全仓其余 ((Number)...) 裸强转（CaseService:103/139/781/855、MessageService:105、OversightService:181-182）全作用于 JDBC 结果行定型列，只有 ApprovalService:121 作用于用户 JSON，"问题只在这一支"属实。夸大一处：称"第45条延期是唯一合法通道、只能靠驳回重报绕开"——踩 500 的正是负责人本人，而 CaseController:176 的 POST /cases/{id}/extend 是 @PreAuthorize LEADER/ADMIN，前端 :37/47/54 对 isLeader 显示「延长期限」按钮（onExtendCase:1090 用 inputPattern ^\d+$，并 recordDirect("EXTEND") 留痕），法定延期本身不被卡住，只是这张单批不下来（驳回可清理）；且触发需办案员主动清空一个默认预填 30 的输入框。另有一处描述未提的对照：payload 整体为 null 时 fromJson 返回 Map.of() → getOrDefault 得 0 → extend 抛 2009「延长天数须为正数」，报错可读，唯独 days:null 这一支落 500。定级：真 bug，低-中危（无数据错乱/无安全影响/有绕行），但修复两行且与仓库既有约定一致，advice 注释本身即声明此类 NPE 不应被伪装，建议照原议修（apply() 用 reqInt 前置校验 + 第121行同法取值 + 前端 :value-on-clear 兜底）。

> 核实：真实缺陷，链条逐环实测确认，建议照单采纳。

【实证结果】
1. 前端确能发出 null。frontend/src/views/case/CaseDetailView.vue:615 是裸的 `<el-input-number v-model="applyForm.days" :min="1" :max="90" />`，未设 value-on-clear。已核对本仓 element-plus 2.14.3 源码 input-number.mjs:81-88，valueOnClear 默认值确为 null；vue_type_script_setup mjs 的 verifyValue 里 `value === "" && valueOnClear === null → return null` 在 min 钳制之前返回，所以 `:min="1"` 挡不住。且 handleInput 在清空的那一刻就 emit 了 null（不必等 blur）。CaseDetailView.vue:920 原样发出 `{"days": null}`。
2. 入库无校验。ApprovalService.apply() 第38-45行只校验 kind/reason，以及非 FILE_CASE 时的 caseId，EXTEND 的 payload 完全不看，单据正常入库。
3. JSON 往返保留 null 键。实跑了 jackson-databind 2.19.2（本仓依赖版本）：`{"days":null}` 序列化后原样存，反序列化回 LinkedHashMap 后 containsKey=true、value=null。仓库内 grep 无 default-property-inclusion / NON_NULL / JsonInclude 任何配置，默认 ALWAYS，null 不会被丢掉。（这一环是关键：若 Jackson 丢键，getOrDefault 就会拿到 0，被 extend() 的 `days<=0` 干净拦下，缺陷不成立。实测不丢。）
4. 第121行确实 NPE。实跑确认 HashMap.getOrDefault 在「键存在、值为 null」时返回 null 而非默认值 → `((Number) null).intValue()` 抛 NPE；`"days":"30"` 字符串则抛 ClassCastException。两种都复现。
5. 落到 500。BureauExceptionAdvice.java:48-57 的注释明写「刻意不含 NullPointerException/ClassCastException」，故走第83行 handleAny → R.fail(500, "系统内部错误，请联系管理员")。
6. 单据卡死可复现。decide() 是 @Transactional，第63行带状态条件的抢占 update 先执行、第72行 execute() 抛 NPE，整个事务回滚 → status 退回 PENDING，负责人每点一次批准就 500 一次。

【最有力的旁证】同一个仓库的兄弟路由 CaseController.java:179 早就写的是 `int days = reqInt(body, "days", "延长天数");` —— 正是报告建议的那个 helper。也就是说安全取值的规范在本项目已经确立，只有审批分支这一支漏掉了。这不是设计取舍，是遗漏。同理 execute() 里 FILE_CASE 走 objectMapper.convertValue 能自动转型，唯独 EXTEND 是裸强转，报告对「问题只在这一支」的判断准确。

【对维护者三问的回答】
发生概率：中低。days 默认 30、每次开窗（CaseDetailView.vue:1359）都重置为 30，必须办案员主动清空才触发，不是主路径。但「全选删除想重填、结果直接点了提交」是很自然的操作，且清空瞬间 v-model 就变 null；试运行系统多人长期使用下属于迟早会遇到。CCE 那一支只对非浏览器客户端有意义，权重低，但修复顺带覆盖。

后果：**不是错误数据，是被卡死的合法操作 + 误导性报错**。这点要说清楚——事务干净回滚，期限没被写错，没有半截状态，数据完整性无损。但严重度被三件事抬高：一是办案员没有 /cases/{id}/extend 权限（CaseController.java:176 限 LEADER/ADMIN，已核实），审批流确是其第45条延期的唯一通道；二是办案期限在此期间照常流逝，卡到届满就是超期办案的法律后果；三是负责人只看到「系统内部错误」，对真因零提示。

需要修正报告一处措辞：并非完全无解。execute() 只在 approve=true 时调用（第71行已核实），所以**驳回是能成功的**，负责人还可用自己的 /cases/{id}/extend 直接延期。所以是「可绕开但要靠猜」，不是绝对死局。另外 ApprovalsView.vue:47 会渲染成「延长  日」（中间空白），有微弱视觉线索，但极易忽略。

改动风险/收益：收益明显大于风险，建议直接改。reqInt 已同时处理 null 和数字字符串两种情形，两处各一行，且是向 60 行外已有代码看齐，不引入新语义。apply() 加校验把报错前移到提交时刻、由能改的人（办案员）看到 2100「请填写延长天数」；第121行同步改则保护库里已存在的存量 PENDING 行——注意这类存量行不会因此变得可批准（days 本来就没有），但 500 会变成可读的 2100，负责人由此知道该驳回，这正是期望行为。前端补兜底建议用 `:value-on-clear="min"`，与既有 `:min="1"` 自洽，比硬编码 1 更贴切（纯风格差异）。

【测试缺口】server/src/test/java/cn/ybcase/bureau/GuardIntegrationTest.java:141 只覆盖了 `Map.of("days", 10)` 的正常路径（Map.of 本身还装不下 null 值），null/字符串分支无任何覆盖。修复时建议补一条断言 apply() 对 `days=null` 抛 2100 的用例。

### 20. [MEDIUM] 写接口用请求体里的 caseId 定位案件，绕过 CaseScopeInterceptor 的数据范围
*web 控制器 — `server/src/main/java/cn/ybcase/bureau/web/FormalizationController.java:84`*

**现状/复现**：case_view_scope=SELF 下，办案员 A（既不承办也不参办案件 9）直接 POST /api/bureau/approvals {"kind":"TERMINATE","caseId":9,"reason":"x"} → 200 建单。CaseScopeInterceptor 只按路径变量 id/caseId 判定且只注册在 /api/bureau/cases/**（BureauWebMvcConfig.java:16），这条路由既不在该前缀下、caseId 又来自请求体，一次范围校验都不过；ApprovalService.apply() 也不校验案件是否存在、是否在申请人范围内。负责人在待办审批列表里看到的是 pending() join 出来的案号+案件名，与正常申请完全同形，顺手批准即 caseService.terminate(9) 生效（SUSPEND/EXTEND/DEFER 同理）。同一形态还有 POST /api/bureau/transfers（ProcedureController.java:76，TransferReq.caseId 来自请求体，直接 insert case_transfer）与 POST /api/bureau/transfers/{id}/confirm（ProcedureController.java:82）——而同一张表的 POST /api/bureau/transfers/{id}/receipt（FormalizationController.java:212）却专门写了 scopeByChild 补范围校验，两边口径相反。

**影响**：SELF 数据范围在写侧留了口子：范围外的办案员可对任意案件 id 发起中止/终止/延期/暂缓审批与移送登记。终止/中止一经批准就改写他人案件状态并在 biz_approval 留下「申请人=A」的记录，事后复盘会把责任落在被越权的案件上；移送登记还会计入 /bureau/stats/report 的 transfer_total。这正是 scopeByChild 那条注释想堵的「兄弟资源路由」，只堵了 receipt 与 agreement-actions 两条。

**修法**：ApprovalService.apply() 中当 req.caseId() != null 时先 caseService.assertInScope(req.caseId(), applicant, privileged)（顺带补案件存在性校验，现在不存在的 caseId 只会撞外键返回 2101）；ProcedureController.addTransfer/confirmTransfer 按 FormalizationController.scopeByChild 同样补上（confirm 按 case_transfer.id 反查 case_id）。

> 核实：成立，反驳失败。逐条核实（全部命中）：

1) 拦截器覆盖面：BureauWebMvcConfig.java:16 全仓唯一一处 addInterceptor，只注册 `/api/bureau/cases/**`；grep 全仓 HandlerInterceptor/WebMvcConfigurer 只此一个。CaseScopeInterceptor.java:32-37 只读 URI_TEMPLATE_VARIABLES 里的 caseId/id，路径变量拿不到就 `return true` 放行。`/api/bureau/approvals` 与 `/api/bureau/transfers` 都不在该前缀下，一次都不进。

2) 服务层无兜底：ApprovalService.apply()（:37-56）只校验 kind∈KINDS、reason 非空、非 FILE_CASE 时 caseId 非空，随即 insert biz_approval，全程没有 assertInScope、也没有案件存在性查询。CaseService.assertInScope 全仓 17 处引用（grep 已穷举），无一在 apply() 路径上。ProcedureService.addTransfer(:164-186)、confirmTransfer(:189-193) 同样零校验。SecurityConfig 只有 `anyRequest().authenticated()`，FormalizationController.apply 与 ProcedureController.addTransfer/confirmTransfer 均无 @PreAuthorize —— 任何已登录用户可达。种子角色只有 ADMIN/HANDLER/LEGAL/LEADER（V1__bureau_core.sql:79-83），privileged() 放过其中三个，所以受 SELF 约束的正是 HANDLER，也正是能打这两个洞的人。

3) 口径相反属实且更明显：同一张 case_transfer，`/transfers/{id}/receipt` 走 scopeByChild（FormalizationController.java:212），`/transfers` 与 `/transfers/{id}/confirm` 不走。同模式的第三处补丁在 OversightController.java:113-119（payInstallment 内联反查 case_installment.case_id）—— 审阅说"只堵了 receipt 与 agreement-actions 两条"略有遗漏，但这只加强结论：三处已按"兄弟资源路由"补过范围校验，approvals/transfers 是漏网的。

4) 下游影响属实：pending()（ApprovalService.java:97-102）left join case_file 带出 case_no/case_name，越权单与正常单同形；decide() 批准即 execute() → caseService.terminate/suspend/extend/executionService.approveDefer，terminate(:250-261) 自身无范围校验。GoLiveController.java:61 `select count(*) from case_transfer where extract(year from sent_at)=?` 确为 transfer_total。biz_approval.case_id、case_transfer.case_id 均为 references case_file(id)（V9:31、V5:24），不存在的 id 撞 FK → BureauExceptionAdvice.java:71 返 2101，与"顺带补存在性校验"的建议吻合。E2E（tools/e2e-case.py:1039-1108）的 2080 断言全部打在 /bureau/cases/** 上，approvals/transfers 一条未测；开发待办.md 无对应条目（:156 只记了线索/检查不在拦截器口径内）。

描述略有夸张/需补的三点（不影响成立，但影响定级）：
(a) case_view_scope 默认 'ALL'（V10__v12_todo_scope.sql:24），全仓除 e2e 脚本外无任何部署脚本或迁移把它置为 SELF —— 这是管理员显式开启后才存在的洞，属于"已发布的安全控件不完整"，不是默认可利用。
(b) 审批这一半需要 LEADER/ADMIN 在 /approvals/{id}/decide（有 @PreAuthorize）点批准才会改状态，是 confused deputy；单凭越权请求本身产生的直接后果是一条 applicant=A 的 PENDING 单 + 向全体 LEADER 发通知。真正"一步到位、无第二道闸"的是移送那一半（addTransfer 直接 insert、confirmTransfer 直接 update），标题锚在 approvals 上反而挑了弱的一半。
(c) 修复建议要连读侧一起定调：GET /api/bureau/transfers（ProcedureController.java:67-73）现在就把全库移送台账连 case_no 一起返给任何登录用户、无任何范围过滤。只给两个写接口补 assertInScope 会造出"读得到、写不了"的新矛盾，应当一次性决定移送台账到底是不是全局账。

> 核实：Confirmed in code, every link. BureauWebMvcConfig.java:16 registers CaseScopeInterceptor only on /api/bureau/cases/**, and the interceptor reads only URI_TEMPLATE_VARIABLES (caseId/id) — a body-carried id is structurally invisible to it. SecurityConfig.java:34 is .anyRequest().authenticated() and FormalizationController.java:84 has no @PreAuthorize, so any logged-in officer reaches it. ApprovalService.apply() (ApprovalService.java:48-70) validates kind/reason/caseId-non-null then inserts — no assertInScope, no existence check. ProcedureService.addTransfer:164 inserts case_transfer straight from req.caseId(); confirmTransfer:189 updates by transfer id; neither checks scope — while transferReceipt (FormalizationController.java:212) calls scopeByChild on that same table, with the comment at :197 naming exactly this "兄弟资源路由" gap. I enumerated every non-/cases/{id}/ write in bureau/web: those three are the complete set (/inspections takes no caseId), so the report is precise rather than a shotgun.

Probability: low and not accidental. case_view_scope seeds as ALL (V10__v12_todo_scope.sql:24), so the hole exists only in SELF deployments, and no UI path produces an out-of-range body caseId — this is a hand-crafted request by authenticated staff or a scripted integration, not a misclick.

Consequence: wrong data, and the two halves differ. Approvals is gated (apply() only creates PENDING; a LEADER must approve, and LEADER is privileged anyway) — the harm is a normal-looking request in their queue, applicant=A the only tell. Transfers has no gate: an immediate insert on another officer's case, counted in GoLiveController.java:61 transfer_total. Impact is worse than the report states on one point: CaseService.java:256 terminate() also runs "update case_evidence set sealed=false, register_hold=false where case_id=?", so an approved spurious TERMINATE releases 强制措施 on someone else's case.

Risk/benefit favors fixing: assertInScope returns immediately when scope isn't SELF (CaseService.java:811), so the patch is inert under the shipped ALL default; three call sites, existing house pattern. Four cautions for the patch: guard on caseId != null (transfers legitimately carry clue-only rows, as scopeByChild:202 already handles); confirmTransfer needs the reverse lookup and both ProcedureController methods must gain an Authentication parameter; assertInScope alone does NOT fix the 2101/nonexistent-caseId complaint because under ALL it returns without touching the DB, so an explicit count check is needed (precedent FormalizationController.java:144-145, code 2043); and scopeByChild is private to FormalizationController, so it must be lifted to a shared place rather than copied — a second copy of the scope predicate is what CaseScopeInterceptor.privileged's comment warns against. Not covered by the ten prior rounds or 开发待办.md.

### 21. [MEDIUM] 封卷闸门漏掉附件上传与送达登记，已归档案卷仍可追加卷内材料
*web 控制器 — `server/src/main/java/cn/ybcase/bureau/web/AttachmentController.java:50`*

**现状/复现**：1) 负责人 POST /api/bureau/cases/{id}/close 立卷归档，archive_no 生成、status=CLOSED。2) 范围内任一账号 POST /api/bureau/cases/{id}/attachments 上传文件 → 200，附件入 case_attachment 并出现在案件附件页；AttachmentController.java:50 的唯一前置只有 select count(*) from case_file，没有任何封卷判断。对照同一时刻 POST /cases/{id}/documents 会被 CaseService.addDocument:178（"CLOSED".equals(status) || archiveNo != null）以 2031「案件已立卷归档，不可新增文书」挡回，POST /cases/{id}/evidences 被 requireActive 挡回，EvidenceService.crossExam:89 也有同一守卫。3) 同一账号 POST /api/bureau/cases/{id}/doc-deliveries（FormalizationController.java:139-168，同样没有封卷判断）新增一条 case_delivery → DocumentService.recordEntries:263 把它渲染成「送达回证（系统记录）」虚拟卷内件，GET /cases/{id}/archive-catalog 与 /cases/{caseId}/archive-full 的卷内目录立刻多一件、seq 整体重排。

**影响**：3be5e73 刚建立的封卷规则（CLOSED 或 archive_no 非空即不得再往卷里加东西）只覆盖了 case_document 和 case_evidence 两张表，而 case_attachment（笔录扫描件/执法音像）与 case_delivery（送达回证，现已是卷内必备件）这两个控制器直连 JdbcTemplate 的写入口一个都没挡。结果是同一个案卷号先后两次调 /archive-full 打印出来的卷内目录条目数和页序可以不一致；附件又没有作废通道（开发待办已记「附件无作废」），追加即永久。

**修法**：把 CaseService.addDocument:176-179 的判定抽成公共守卫（如 CaseGuards.requireNotArchived(CaseFile)），在 AttachmentController.upload 把第50行的 count(*) 换成查 status/archive_no 后调用它，并在 FormalizationController.deliverDocument 第144-145行的案件存在性检查处一并调用。

> 核实：结论成立，反驳失败；且实际缺口比描述的更大。

逐条核实：
1. AttachmentController.java:38-75 upload()：前置只有 file.isEmpty、category 白名单、大小限额，第50行 `select count(*) from case_file where id=?` 仅判存在，随后直接 insert into case_attachment。全类无 status/archive_no 读取，无 @PreAuthorize（只受 CaseScopeInterceptor 的数据范围拦截 + authenticated）。属实。
2. FormalizationController.java:139-168 deliverDocument()：只校验 method 白名单、案件存在（第143-144行同样是 count(*)）、documentId 属本案、ELECTRONIC 需 e_delivery_consent，随即 insert into case_delivery。无封卷判断。属实。
3. 对照守卫确实只在两处：CaseService.addDocument:174-179（`"CLOSED".equals(status) || archiveNo != null` → 2031）和 EvidenceService.crossExam:87-90（同一判定，注释明写"同 CaseService.addDocument"）。CaseGuards.java 全类只有 get() 和 requireActive()，并无 requireNotArchived——建议里的公共守卫确不存在。
4. 卷内目录确为实时计算、无快照：DocumentService.archiveEntries:106-126 每次现查 case_document + recordEntries()，排序后 `int seq=1; for(...) d.put("seq", seq++)`；recordEntries:263-273 把 case_delivery 每行渲染成 "送达回证（系统记录）"（id=null）。archiveCatalog:130-139 与 FormalizationController.archiveFull:279-291 用的是同一份 archiveEntries。所以归档后新增一条 case_delivery，两次打印的条目数与 seq 必然不同。属实。
5. 无别处兜住：CaseScopeInterceptor 只做 assertInScope，不看状态；SecurityConfig 仅 `anyRequest().authenticated()`，无这两个路由的角色限制；db/migration V1-V9 无任何 trigger；server/src/test 与 e2e 里没有任何用例覆盖 attachments / doc-deliveries 的封卷行为。
6. 前端也没兜：CaseDetailView.vue:141-142 的"制作文书/模板生成"有 `:disabled="c.status === 'CLOSED'"`，而 :174 的"上传附件"和 :159 的"送达"按钮没有任何禁用条件——即已归档案件在正常 UI 上就能点，不需要绕过前端直调 API，复现比描述的更容易。

描述的准确性（唯一需要收口的一处）：附件并不进卷内目录与合成打印——archiveFull 只返回 caseFile/documents/signatures，archiveEntries 不查 case_attachment（开发待办.md:222 的既有核实结论也已确认这点）。所以"卷内目录条目数和页序不一致"这半句完全由 case_delivery 承担；case_attachment 的危害是"已归档案卷的附件清单事后仍可增、且无作废通道"。原文其实是分开归因的（明确说目录变动来自 case_delivery），只是标题句把两者合成"卷内材料"，宜加一句限定，不构成夸大。

反而是低估：同一封卷规则还漏了至少三个同样进卷宗打印的写入口——FormalizationController.sign:45-63（insert doc_signature + update case_document set signed=true，签章行会出现在 archive-full 的 signatures）、CaseService.addMeeting:433-441（只 get(caseId)，case_meeting 进目录）、ProcedureService.scheduleHearing:58-87（无封卷判定，case_hearing 进目录）。所以修复不宜只补两处，应按建议抽出 CaseGuards.requireNotArchived(CaseFile) 后统一覆盖 case_attachment / case_delivery / doc_signature / case_meeting / case_hearing 这五个入口。

> 核实：真缺陷，判定成立，但报告的因果链有一处错误，且缺口比报告说的更大。

【已核实的事实】
1. server/src/main/java/cn/ybcase/bureau/web/AttachmentController.java:50 唯一前置确为 `select count(*) from case_file where id = ?`，之后直接 insert into case_attachment，无 status/archive_no 判断。
2. server/src/main/java/cn/ybcase/bureau/web/FormalizationController.java:143-144 deliverDocument 同样只有存在性 count，随后 insert into case_delivery（:158-166），无封卷判断。
3. 对照守卫确实存在：CaseService.addDocument:176-179（"CLOSED".equals(status) || archiveNo != null → 2031）、EvidenceService:88 同款注释同款判定。
4. 卷内目录确实会变：DocumentService.recordEntries:264-274 把 case_delivery 每行渲染成 DELIVERY_RECEIPT 虚拟件；archiveEntries:105-124 每次调用都按 ARCHIVE_ORDER 排序后 `int seq = 1; for (...) d.put("seq", seq++)` 重编号；/cases/{id}/archive-catalog（CaseController:133）与 /cases/{caseId}/archive-full（FormalizationController:280-291）共用同一份 archiveEntries。DELIVERY_RECEIPT 在 ARCHIVE_ORDER 中排第 17（DocumentService:31），位于 CLOSE_REPORT（末位、结案时必然已生成）之前——所以归档后每加一条送达，结案报告及其后所有件的 seq 都会后移。「同一案卷号两次打印目录条目数与页序不一致」成立。
5. 不是 API-only 路径：frontend/src/views/case/CaseDetailView.vue:141-142「制作文书/模板生成」带 :disabled="c.status === 'CLOSED'"，而同表第 160 行的「送达」按钮和第 174 行的「上传附件」按钮都没有任何 disabled——已归档案件点开文书页就能按。
6. 无解封通道：全仓仅 CaseService:669 与 :717 两处 setArchiveNo，没有任何撤销归档/拆卷接口。

【报告说错的一处，须更正】附件根本不进卷内目录与合成打印。archiveEntries 只查 case_document + recordEntries（biz_approval/case_review/case_meeting/case_hearing/case_delivery），archiveFull 只返回 caseFile/documents/signatures，case_attachment 一处都不参与。这一点开发待办.md:222 上一轮已核实并记录过。因此「打印目录条目数和页序不一致」完全由 case_delivery 单独造成，附件那半只影响案件附件页，与打印无关——报告把两件事并成了一条因果链。

【比报告说的更大】封卷缺口不止这两个入口。喂给卷内目录的 5 类结构化记录里，还有两处服务层同样无封卷判断：CaseService.addMeeting:432-441 只 get(caseId) 就 insert into case_meeting（MEETING_RECORD 在 ARCHIVE_ORDER 第 15 位，同样会移位）；ApprovalService.apply:37-51 也无归档判断（FILING_APPROVAL/APPROVAL_FORM 进卷）。这两处目前只被前端 CaseDetailView.vue:76-77 的 :disabled 挡住，服务端全敞。反过来 CaseService.deliver:584 有 `!"DECIDED".equals(status)` 硬拦，结案后走不到——所以 case_delivery 的两个写入口里，恰恰只有泛化送达那个是敞开的。

【发生概率】分两档。附件：高。「先结案再补扫描件」是日常动作，按钮无禁用、无提示，几乎必然发生。送达：中。/doc-deliveries 是「任意文书的送达与回证」泛化入口，补登一条漏记的告知书/听证通知回证是常见补录动作；TERMINATED 案件立卷后再登记终止决定书送达也是自然顺序（前端 :66 的立卷按钮并不强制先送达）。都不需要恶意，正常点按即触发。

【后果】是错误数据，不是体验问题。卷内目录是第57条一案一卷的法定产物，3be5e73 建立封卷规则的全部意义就是归档后目录定型；现在同一案卷号先后两次打印可以给出不同的件数与页序，案卷评查、复议应诉时这是证据完整性问题，且全程无痕——附件又无作废通道（开发待办.md:213 已记），追加即永久。

【改动风险与收益】收益明确，改动本身小，但报告给的补丁位置不够且有一个编译层面的坑：CaseGuards 是 `final class`（包私有，位于 cn.ybcase.bureau.service），两个控制器在 cn.ybcase.bureau.web，照报告直接调用编译不过。更省事的做法是在 CaseService 上加一个 public assertNotArchived(Long)，内部走 get(caseId) 复用实体与 2031 码——AttachmentController:32 与 FormalizationController 都已注入 caseService，顺带能把两处重复的裸 count(*) 一并删掉（前端已认识 2031 文案）。同时建议把 CaseService.addMeeting 与 ApprovalService.apply 一并补上，否则封卷规则仍是半覆盖。
真正要权衡的是两个副作用：其一，系统没有拆卷通道，加了守卫后归档案件的送达回证补登就永久无路可走——文书/证据已经接受了这个单向性质，送达跟上是一致的，但要在发布说明里点明「补登须在立卷前完成」；其二，「结案后补传扫描件」是试运行中很可能正在用的流程，一刀切禁用附件上传对已部署的 pilot 是可感知的功能回退。稳妥的分法是：送达（影响法定目录）按封卷规则硬挡，附件（不进目录）要么同挡并同步在前端把上传按钮置灰给出明确文案，要么留一条 LEADER 补传通道，不要只在后端静默 403 让办案人对着可点的按钮撞墙。

### 22. [MEDIUM] 「电子送达确认书」入口同样只在 DECIDED 分支，决定作出前的文书电子送达永远无法登记确认书
*前端 — `frontend/src/views/case/CaseDetailView.vue:60`*

**现状/复现**：该按钮在 57 行 `<template v-if="c.status === 'DECIDED'">` 内。文书页的「送达」（onDeliverDoc→onDocDeliverSubmit，1057-1063 行）在任意状态可用，送达方式下拉直接遍历 DELIVERY_METHOD 含 ELECTRONIC；后端 FormalizationController.deliverDocument（FormalizationController.java:152-157）对 ELECTRONIC 校验 case_file.e_delivery_consent，未签确认书抛 2054。而 ProcedureService.eDeliveryConsent（ProcedureService.java:227-242）对案件状态毫无限制，任何状态都能登记。复现：案件处于 REPORTED/NOTIFIED，对告知书或听证通知选「电子送达」→ 报「当事人未签订电子送达确认书（第59条）」→ 页面上找不到任何登记确认书的按钮（它只在 DECIDED 才渲染）。

**影响**：电子送达在真正用得上的阶段完全不可用：处罚决定书本身受辽61条参数开关限制多半不走电子送达，需要电子送达的恰恰是决定前的告知书/听证通知/协查函，而这些文书所在状态下确认书入口不存在，办案员只能反复试错后放弃该送达方式。

**修法**：把这颗按钮移出 DECIDED 分支，与集体讨论记录/提交审批申请同级：`<el-button v-if="!c.eDeliveryConsent && !['CLOSED','TERMINATED'].includes(c.status)" @click="dlg.eConsent = true">电子送达确认书</el-button>`。顺带在 labels.ts 的 DOC_TYPE 补 `E_DELIVERY_CONSENT: '电子送达确认书'`——eDeliveryConsent 会写入该类型文书，目前文书列表只能显示原始英文码。

> 核实：证伪失败——缺陷成立，且比描述的还宽一点。

核实过的事实链：
1. `frontend/src/views/case/CaseDetailView.vue:60` 的「电子送达确认书」按钮确实包在 `:57` 的 `<template v-if="c.status === 'DECIDED'">` 里。
2. 全仓 grep `e-delivery-consent` / `e_delivery_consent` / `eConsent`，前端唯一的调用点是 `CaseDetailView.vue:1308`，唯一的触发入口就是第 60 行那颗按钮。没有第二条路径（无独立送达页、无当事人信息页入口、无立案时录入）。**别处没有兜住。**
3. 文书页的「送达」按钮 `:160` 无任何状态守卫（同 tab 的「制作文书」「模板生成」只在 CLOSED 时禁用），弹窗 `:630` 直接遍历整个 `DELIVERY_METHOD`（含 ELECTRONIC）。后端 `FormalizationController.java:152-156` 对 ELECTRONIC 查 `case_file.e_delivery_consent`，未签抛 2054。`ProcedureService.java:227-242` 登记确认书对案件状态零限制。复现链完整。
4. 状态机是单向的（`CaseFile.java` 类注释：INVESTIGATING→REPORTED→NOTIFIED→DECIDED→DELIVERED→CLOSED），REPORTED/NOTIFIED 阶段不可能回退到 DECIDED 去补签。
5. **未在 开发待办.md 记录**：该文件里 `E_DELIVERY_CONSENT` 只在归档条目 `:69` 作为「case_document 四个写入点之一」被顺带提及，不是本问题。

反而是描述**低估**了范围：DELIVERED 分支（`:68-74`）同样没有这颗按钮。执行阶段的催告书（URGE_LETTER）走电子送达一样撞 2054 且无处补签。所以按钮实际只在 DECIDED 这一个状态存在，不是「决定前不可用」而是「只有 DECIDED 可用」。

现有 E2E 恰好是活证据：`tools/e2e-case.py:946-950`「送达泛化：告知书电子送达无确认书拒（2054）」先撞 2054，然后**改用 DIRECT 绕过**——脚本里写死的正是审阅者预言的那条放弃路径；而 `:1200-1206` 单独登记确认书时是直接打 API，从不经过 UI，所以这个 UI 死路一直没被测出来。

描述有两处小瑕疵，不影响定性：
- 行号错了：`onDeliverDoc`/`onDocDeliverSubmit` 在 **899-909**，不是 1057-1063。（第 60 行、FormalizationController:152-157、ProcedureService:227-242 三处均准确。）
- 「处罚决定书受辽61条参数开关限制多半不走电子送达」是臆测：`V3__province_params_guards.sql:24` 里 `delivery_electronic_decision_allowed` 种子值就是 `'true'`，`CaseService.java:589` 兜底也是 true，默认配置下决定书电子送达是放行的。这只削弱动机叙述，不削弱缺陷本身。

**补充一个会让建议的补丁打折的独立问题**：`c.eDeliveryConsent` 其实永远是 undefined。`CaseFile.java:122` 字段 `eDeliveryConsent` 经 Lombok `@Getter` 生成 `getEDeliveryConsent()`（已 javap 确认编译产物签名），Jackson 默认 legacy mangling 把 `EDeliveryConsent` 折成 **`edeliveryConsent`**；`application.yml` 的 jackson 段只配了 `time-zone`，没有 property-naming-strategy；私有字段默认不可见所以不参与命名。而 `CaseService.java:774` 是把实体本身 `m.put("caseFile", c)` 交给 Jackson 序列化的，前端 `:768` 直接读 `detail.caseFile.eDeliveryConsent`。构建产物 `frontend/dist/assets/CaseDetailView-Og_uczfE.js` 里也只有 `eDeliveryConsent` 一种拼写。后果：`!c.eDeliveryConsent` 恒为真，按钮签完确认书也不消失；而 `ProcedureService.eDeliveryConsent` 没有幂等守卫，重复点会反复插入 E_DELIVERY_CONSENT 文书。E2E 无一处断言过 caseFile 上这个布尔（`grep eDeliveryConsent tools/ server/src/test/` 空），所以从未暴露。建议的补丁 `v-if="!c.eDeliveryConsent && !['CLOSED','TERMINATED'].includes(c.status)"` 照抄会退化成纯状态判断——修的时候要么读对 key（或给实体加 `@JsonProperty("eDeliveryConsent")`），要么在后端加已签则拒的守卫。

`labels.ts:26-35` 的 DOC_TYPE 确实缺 `E_DELIVERY_CONSENT`，顺带补的建议属实（`:147` 有 `|| row.doc_type` 兜底，所以只是显示英文码，不崩）。

> 核实：Confirmed by reading the code. CaseDetailView.vue:60 is inside `<template v-if="c.status === 'DECIDED'">` (lines 57-63) and is the only caller of /bureau/cases/{id}/e-delivery-consent in the entire frontend (line 1308). The 文书 tab's 送达 button (line 160) has no status gate and its dialog (lines 629-630) iterates all of DELIVERY_METHOD including ELECTRONIC; FormalizationController.java:152-157 rejects ELECTRONIC without e_delivery_consent (2054). ProcedureService.java:227-242 has no status restriction (tools/e2e-case.py:1201-1206 already registers consent on a non-DECIDED case), so the dead end is purely a frontend gating mistake and the fix needs no backend change. labels.ts:26-35 DOC_TYPE indeed lacks E_DELIVERY_CONSENT, and both the 文书 tab (line 147) and the 案卷目录 dialog (line 727) fall back to the raw code, so a printed legal catalog shows English.

Assessment: probability is 100% on attempt and the attempt is natural, since 第59条 electronic delivery is aimed precisely at the pre-decision 告知书/听证通知/协查函. (One premise in the report is wrong but harmless: delivery_electronic_decision_allowed at CaseService.java:589 defaults to true, so the decision letter is not actually blocked by default.) The consequence is a blocked workflow, not bad data — the backend guard rejects correctly. Risk of the fix is near zero (a template move plus one label entry); benefit is high.

One additional defect the report missed, which matters because the suggested one-liner carries it into a more prominent place: CaseFile.eDeliveryConsent has Lombok getter getEDeliveryConsent() (see CaseService.java:592), and Jackson's legacy name mangling serializes that as "edeliveryConsent", not "eDeliveryConsent". Verified empirically by compiling and running the identical getter shape against jackson-databind 2.19.2 from the local .m2: output is {"edeliveryConsent":false}. detail() puts the raw entity into the payload (CaseService.java:772) and client.ts applies no key transform, so c.eDeliveryConsent is always undefined and `v-if="!c.eDeliveryConsent"` is always true. So the button never disappears after registration, and every extra click inserts another E_DELIVERY_CONSENT row (ProcedureService.java:236) that DocumentService.archiveEntries deliberately does not dedup (comment at lines 108-112), producing duplicate 确认书 and shifted 序号 in the 卷内目录. The fix should read c.edeliveryConsent, or better add @JsonProperty("eDeliveryConsent") on the entity; making eDeliveryConsent() skip the document insert when the flag is already true would close it server-side. Minor extras: E_DELIVERY_CONSENT is also missing from DocumentService.ARCHIVE_ORDER (lines 27-32) so it sorts last, and eDeliveryConsent() writes case_document via raw jdbc, bypassing addDocument's archive_no seal check (CaseService.java:177-179).

### 23. [MEDIUM] 线索期限扣除把历次事由累加进 varchar(255) 的 extend_reason，溢出后整笔回滚
*数据层 — `server/src/main/java/cn/ybcase/bureau/service/ClueService.java:64`*

**现状/复现**：线索先走一次核查延期（CluesView.vue:139 的 prompt 无 maxlength，特殊情况说明约 80 字，写入 extend_reason）；再按辽15条连续登记期限扣除（CluesView.vue:133），每次 addExclusion 追加「；期限扣除{days}日：{reason}」≈ 7+len(reason) 字。事由各 60 字时第三次即 80+70+70+70=290 > 255 → PG 22001 value too long for character varying(255)（V2__bureau_case.sql:24 extend_reason varchar(255)）→ DataIntegrityViolationException → BureauExceptionAdvice.java:71 返回 2101「数据校验未通过：请检查关联对象是否存在、必填项是否完整、日期格式是否正确」。单步也能触发：延期理由直接写超过 255 字。

**影响**：事务回滚意味着同一方法里的 setExcludedDays / setDeadlineAt(plusDays) 一并作废——鉴定、检验期间根本登记不进去，线索的 deadline_at 不顺延，督办看板 clueOverdue 与 MessageService 每日提醒继续把它当超期件推送，办案人被迫在「已依法扣除期限」的情况下背超期。而返回的错误文案与真实原因（字段超长）毫无关系，用户只会原样重试。全前端仅 MainLayout.vue:69 一处 maxlength，服务端也无长度校验；本处又是整个数据层唯一「服务端把历次文本串接后写回同一定长列」的写法，所以随使用次数必然逼近上限，而非偶发。

**修法**：迁移里 alter table case_clue alter column extend_reason type text（与同表 content/verify_result 一致，无存量风险）；更彻底的做法是把线索期限扣除拆成子表（对齐 case_period_exclusion 的 start_at/end_at/reason），不再往「延期理由」列里追加字符串——现在扣除事由和延期理由混在同一列，且 extended 标志仍为 false，语义本身也是错位的。另在 ClueService.extend/addExclusion 入口按列宽做长度校验并抛明确的 2026/2023 提示。

> 核实：成立，证伪失败——但描述里有两处需要修正，且候选漏掉了同两行里更严重的一个缺陷。

【逐条核实全中】
1. 列宽属实且从未改过：`server/src/main/resources/db/migration/V2__bureau_case.sql:24` `extend_reason varchar(255)`，全仓 22 个迁移里对 case_clue 的 alter 只有 `V3__province_params_guards.sql:51`（加 excluded_days），extend_reason 从未 alter。
2. 写入无任何长度守卫：`ClueService.java:64` 追加、`:50` 直接赋值，两处都不校验长度；`ClueController.java:35/46` 用 `Map<String,String/Object>` 原样透传，`ReqValues.java` 只有 reqInt/optLong/optDate/reqDate，没有任何字符串长度函数；全仓无 `@Size`（core/pom.xml 虽引了 spring-boot-starter-validation，但 Hibernate 不会把 `@Column(length=255)` 当校验约束，实体 `CaseClue.java:52` 只有 length=255 这个纯映射声明）。
3. 前端 maxlength 的说法准确：`grep -rn maxlength frontend/src --include=*.vue` 全仓只有 `MainLayout.vue:69` 一处；`CluesView.vue:126-136` 的 onExclusion 与 `:138-142` 的 onExtend 两个 ElMessageBox.prompt 都只有 `inputPattern: /\S+/`，无 inputAttrs/maxlength。按钮确实挂着（`CluesView.vue:42/43`）。
4. 错误链路准确：PG 22001 → Hibernate DataException → DataIntegrityViolationException（DataAccessException 子类）→ `BureauExceptionAdvice.java:66-72`，非 DuplicateKey 即落到 `:71` 返回 2101「数据校验未通过：请检查关联对象是否存在、必填项是否完整、日期格式是否正确」——与「字段超长」毫无关系。
5. 回滚范围属实：`addExclusion` 整方法 `@Transactional`，`setExcludedDays(:62)`、`setDeadlineAt(plusDays)(:63)`、`setExtendReason(:64)` 同一个持久化上下文，提交时 flush 失败三者一并作废。
6. 下游影响链真实：`BureauStatsController.java:58-60` clueOverdue = `status='PENDING' and deadline_at < current_date`，不含 excluded_days 修正项；`MessageService.java:53-61` 每日 8 点提醒同口径。deadline_at 没顺延，看板与站内信就继续按超期推。
7. 无测试兜底：`tools/e2e-case.py` 只调 `/bureau/cases/{id}/exclusions`（:161、:1168-1180），从未调用 `/bureau/clues/{id}/exclusions`；线索侧只测了 extend 的 2022 重复守卫（:104-105）。
8. 「整个数据层唯一的串接回写定长列」属实：全仓 service 层 grep `set*(get*() +` 只命中 ClueService:62/64，其余全是 caseNo/archiveNo 之类的一次性拼装。
9. 修复方案安全：`application.yml:16` 是 `ddl-auto: validate`，但 Hibernate 6 的列类型校验比的是 JDBC type code（PG 的 text 与 varchar 同为 Types.VARCHAR），所以 `alter column extend_reason type text` 后即使实体不改也不会启动失败；同表 content/verify_result 已是 `columnDefinition="text"` 的现成先例，PG 的 varchar(n)→text 也不需要重写表。

【描述需修正的两处】
A. 触发频率被夸大了。PG 的 varchar(255) 按字符计不按字节计，所以是 255 个汉字。候选的算式本身没错（80 + 3×约 68 ≈ 284），但「一条线索在 15 个工作日核查期内登记三次鉴定/检验」在业务上是罕见的；真正现实的是它自己提的单步路径——从 Word 粘一段超过 255 字的特殊情况说明进 onExtend 的 prompt。所以结论应是「偶发但确定会撞、且撞上后无从自救」，不是「随使用次数必然」。
B. 「办案人被迫背超期」是有条件的：换一段短理由重试就能成功，真正持久的伤害是那句答非所问的 2101 让用户只会原样重试。

【候选漏掉的、同两行里更实的缺陷 —— 建议合并修】
它把「extended 仍为 false」当语义错位，这条恰恰是最站不住的子主张：扣除本就不是延期，extended 保持 false 是对的，否则会误耗掉 `:47` 第14条「只能延长一次」的守卫。但同两行有个它没看到的真问题：`extend()` 在 `:50` 是**直接赋值**而非追加。所以「先登记期限扣除、再申请核查延期」这个完全合法的顺序，会把之前累积的全部扣除事由文本**静默覆盖清空**（excluded_days 数字还在，事由文本没了）。
另外补一条对收益判断有影响的事实：`grep -rn "extend_reason\|extendReason"` 全仓只有实体声明和建表语句两处命中——没有任何查询、文书模板或前端绑定读它。这一列是纯只写列，串接逻辑目前换不来任何用户可见价值，却背着全部溢出风险。这反而让「改 text + 入口按列宽校验并抛 2026/2023」成为性价比最高的收敛方案，而候选提的拆子表（对齐 case_period_exclusion）在没有任何读取方之前先不急。

> 核实：真 bug，逐环核实全部成立，但报告的概率和后果定性都需要修正。

【已核实】
1. ClueService.java:64 确实把历次事由串接回同一列：`setExtendReason((getExtendReason()==null?"":getExtendReason()+"；") + "期限扣除"+days+"日："+reason)`。全 server 层 grep 确认这是唯一一处「服务端把已有列值拼接后写回」的写法，报告此点准确。
2. V2__bureau_case.sql:24 `extend_reason varchar(255)`，V3–V22 无任何 alter 加宽；实体 CaseClue.java:52 `@Column(length = 255)` 与之一致。同表 content/verify_result 确为 text。
3. 无任何长度守卫：ClueController.java:44-48 直收 raw Map，service 只校验 blank；前端 grep 全仓 maxlength 只有 MainLayout.vue:69 一处，而 ElMessageBox.prompt 有 33 处。
4. 溢出链路成立：PG 22001 → DataIntegrityViolationException → BureauExceptionAdvice 的 DataAccessException 分支 → 2101「请检查关联对象是否存在、必填项是否完整、日期格式是否正确」。文案与真因（字段超长）完全无关，用户无法自诊断，只会原样重试再失败——这是本 bug 真正的杀伤点。
5. 回滚确实带走同方法内的 setExcludedDays/setDeadlineAt；BureauStatsController:58-60 的 clueOverdue 与 MessageService:54 都按 deadline_at 判定，该线索会继续被当超期件推送。

【报告夸大处】
「随使用次数必然逼近上限」不成立。累加是按线索行的，不是全局单调增长：每条线索的 extend_reason 从空开始，且 PENDING 窗口只有 15+15 个工作日，办结后按钮即消失。实际需要同一条线索上 3 次以上长事由扣除，或单次粘贴 >255 字的理由。单条线索概率低；一年运行量下总会命中，值得修但不是「必然」。
后果也不是脏数据：事务原子回滚，库内保持一致。真实后果是「合法操作被挡 + 错误文案误导」，以及在扣除登记不上期间线索被误报超期。

【报告漏掉、同一方法】
ClueService.java:49 extend() 的 `clue.setExtendReason(reason)` 是无条件覆盖：先扣除后延期会静默抹掉此前追加的全部扣除事由。这条 100% 必现，比溢出更确定。

【削弱严重性的事实】
extend_reason 全仓从未被读取。grep `extend_reason|extendReason` 覆盖 .java/.vue/.ts 只有实体字段一处命中：无 SELECT、无列表列、无文书模板。excludedDays 同样未展示。该列目前是只写的，累加的文本不服务于任何用户可见用途——这既降低了「保住累加内容」的价值，也说明它本就不该以散文形式存在。

【改动风险/收益】
alter column extend_reason type text 正确且几乎零成本：该列无索引，PG 中 varchar(255)→text 二进制兼容，不重写表，仅瞬时 ACCESS EXCLUSIVE 锁。但必须同时改 CaseClue.java:52 为 columnDefinition="text"，因为 application.yml:16 是 ddl-auto: validate，只改库不改实体有启动校验失败风险。
拆子表（对齐 V2:113-120 的 case_period_exclusion）语义上确实正确（现在扣除事由混进「延期理由」列且 extended 仍为 false），但要新增表+实体+接口+UI，而目标列当前无人读取——应作为后续项，不是本次修复。
入口长度校验值得加，但要认识到这是一类问题的一个实例：33 处无界 prompt 对 16+ 个 varchar(255) 列，extend_reason 只是其中额外带累加的那一个。

### 24. [MEDIUM] 普通处罚案件的封存永远解除不了，督办看板“封存到期”只增不减
*工程与性能 — `server/src/main/java/cn/ybcase/bureau/service/EvidenceService.java:71`*

**现状/复现**：1) 给案件登记 sealed=true 的证据（EvidenceService.java:45 写入 seal_expire_at = obtained+30）。2) 案件走普通处罚出口：CaseService.decide() 只在 decisionType=TRANSFER_JUDICIAL 时清封存（CaseService.java:566），其余分支直接 `c.setStatus("DECIDED")`（:571），close() 全程不碰 sealed；全仓 grep `sealed` 只有 CaseService:256(terminate)、CaseService:566(移送司法)、EvidenceService:78 三处会置 false。3) 案件到 DECIDED 后调 `POST /bureau/cases/{id}/evidences/{eid}/seal?extend=false`：EvidenceService.java:71 `CaseGuards.requireActive(...)` 只放行 INVESTIGATING/REPORTED/NOTIFIED，直接抛 2044「案件当前状态（DECIDED）不允许该操作」。于是 sealed 永久为 true。4) BureauStatsController.java:194-197 的 sealExpiring 是 `where ev.sealed = true and ev.seal_expire_at < current_date + 5`，**没有案件状态过滤**（同一方法里的 correctOverdue:124-128 就写了 `and cf.status not in ('CLOSED','TERMINATED')`，口径不一致）。

**影响**：每一件带封存证据的处罚案件，结案后都会在 LEADER/LEGAL 的督办看板「封存到期」里永久占一行，前端无任何按钮可以消解。按一年 200 件处罚案、每件 2 件封存证据估算，看板每年多 400 条僵尸行，三年后近千行，真正需要处置的封存被淹没，这块预警实际作废。同时数据本身是错的：结案多年的案卷在库里仍记录“证据处于封存中”，与第31/33条（封存不超过30日、可延长一次、不需要时应解除）直接冲突，复议应诉调卷时是硬伤。

**修法**：两侧都补：(a) 让封存有终态 —— 在 CaseService.close()（以及 decide() 转 DECIDED 时）比照 terminate() 执行 `update case_evidence set sealed = false, register_hold = false where case_id = ?`，或把 EvidenceService.updateSeal(extend=false)/disposeHold 的 requireActive 放宽到「未归档即可」（与 crossExam 用的 archive_no 判定同口径，EvidenceService:89）；(b) 看板兜底 —— 给 BureauStatsController:194-197 的 sealExpiring 和 :86-89 的 holdOverdue 补上 `join case_file cf ... and cf.status not in ('CLOSED','TERMINATED')`，与同方法内 correctOverdue 对齐。

> 核实：成立。逐条复核后四步全部对上，且我没能找到任何兜底；但描述有三处需要修正（一处说反、一处夸大、一处反而说轻了），另有一处漏报。

【核实：机制完全属实】
- EvidenceService.java:45 `sealed ? obtained.plusDays(30) : null` 写入封存期；:71 `updateSeal` 第一行 `CaseGuards.requireActive(...)`。CaseGuards.java:20-22 白名单只有 INVESTIGATING/REPORTED/NOTIFIED，其余一律 2044。
- 全仓（排除 frontend/dist 与 target）grep `sealed` 置 false 只有三处：CaseService.java:256（terminate）、:566（TRANSFER_JUDICIAL）、EvidenceService.java:78。CaseService.java:571 `c.setStatus("DECIDED")` 与 close()（:673-720/:711-718）全程不碰 case_evidence。
- BureauStatsController.java:194-197 sealExpiring 确无案件状态过滤；同方法 :124-128 correctOverdue 写了 `and cf.status not in ('CLOSED','TERMINATED')`，口径确实不一致。

【无逃生口（新增核实）】
- terminate() CaseService.java:251-252 对 DECIDED 直接 2035；suspend() :228 仅收 INVESTIGATING；resume() :241 仅收 SUSPENDED。全仓仅一处 `setStatus("INVESTIGATING")`（:243，resume 内），DECIDED 无法回退。
- 全仓只有一个封存端点 CaseController.java:107，无管理员修复接口。唯一 @Scheduled 是 MessageService.java:45，其查询不涉及 sealed。除直连 SQL 外确实解不开。
- 测试零覆盖：tools/e2e-case.py 只在 :303（终止）和 :853（移送）断言解除。更糟的是主流程 :138-144 给 ev2 封存并延长到 +60 天后再没解除，:244 直接结案——每跑一次全新库 E2E，就在库里留下一条 CLOSED 案件挂 sealed=true 的记录，正是这个 bug 的实例，却没人断言。
- 系统开发计划.md:48 自己写的是「✅ 延长一次守卫(2030)、终止自动解除」，从没声称覆盖处罚出口，佐证这条路径当初就没设计。

【修正一：描述说反了】「前端无任何按钮可以消解」不对。CaseDetailView.vue:132 的「解除」按钮只由 `v-if="row.sealed"` 控制，没有任何状态 disable（对比同文件 :141「制作文书」就写了 `:disabled="c.status === 'CLOSED'"`）。按钮一直在，点下去 onSeal(:1059) 发 POST 后弹 2044「案件当前状态（DECIDED）不允许该操作」。是死按钮，不是没按钮——实际比没按钮更糟（办案员会反复点、以为系统坏了），但原文这句是错的。

【修正二：量级夸大】「一年 200 件处罚案 × 每件 2 件封存证据 = 400 条」无据。封存是强制措施，只在扣账册/封药品等少数案件用得上，「每件 2 件」是拍脑袋。方向对，数量级不可信，建议改成定性表述（单调只增、无自然出口）。

【修正三：反而说轻了】SQL 是 `order by ev.seal_expire_at` 升序，僵尸行的 seal_expire_at 是最早的，会稳定排在「封存临期/到期（5日内）」面板最顶端，把真正待处置的封存挤到下面。「淹没」比原文说的更直接。

【原文漏报：同一把锁还卡着先行登记保存】EvidenceService.java:52 `disposeHold` 用的是同一个 requireActive。案件带 register_hold=true 走到 DECIDED 后，PRESERVE/SEAL/RELEASE 三个处理都做不了，而 BureauStatsController.java:87-89 的 holdOverdue 同样没有状态过滤。原文只把 holdOverdue 当成「看板兜底(b)」，其实它是和封存同构的单向锁死，修 (a) 必须一并放宽 disposeHold，否则只是把脏数据从看板藏起来。

【为什么不是「办案员自己忘了解」】封存物在告知、听证、决定全程都还是定案依据，第33条要求的是「不需要时解除」——自然解除时点恰恰在决定作出之时或之后（返还/没收随决定确定）。而 requireActive 正是在 decide() 那一刻关门。所以合规办案员想在决定后依法解封也做不到，不是疏忽问题。

【严重性边界（利于定级，别报成 P0）】MessageService 不推送封存提醒，所以不会有每日消息轰炸；影响面是①督办看板该项预警实际作废、②已归档案卷详情页 CaseDetailView.vue:129-130 永久红字显示「封存至 YYYY-MM-DD」，与第31/33条冲突，复议应诉调卷时确是硬伤。属于正确性缺陷（数据错 + 功能不可达），不是 optimization。

【建议采纳，但两项都要落】(a) 把 updateSeal(extend=false) 与 disposeHold 的 requireActive 换成 crossExam 同款判定（EvidenceService.java:89 `"CLOSED".equals(status) || archiveNo != null`），并在 close() 转 CLOSED 时比照 terminate() 兜一条 `update case_evidence set sealed=false, register_hold=false where case_id=?`；注意别在 decide() 转 DECIDED 时就强制清——那会把「决定后依法解封并留痕」这个动作从台账里抹掉，且与第33条要求的「解除应有据可查」相悖，正确顺序是先放开手动解除、再由 close() 兜底。(b) sealExpiring/holdOverdue 补 `and cf.status not in ('CLOSED','TERMINATED')`，与 correctOverdue 对齐。(c) 顺手在 e2e-case.py 主流程 :144 后补一次 extend=false 解除并断言，否则同样的状态还会被测试固化下来。

> 核实：真缺陷，代码逐条核实成立，建议基本可采纳（其中一处事实描述有误，且报告漏掉了一个更严重的分支）。

【核实结论】
1. EvidenceService.java:71 `CaseGuards.requireActive(...)` 确在 updateSeal 入口，而 CaseGuards.requireActive 白名单只有 INVESTIGATING/REPORTED/NOTIFIED（CaseGuards.java:21-23），DECIDED/DELIVERED/CLOSED 一律抛 2044。属实。
2. 全仓把 sealed 置 false 只有三处：CaseService.java:256(terminate)、:566(decide 且 decisionType=TRANSFER_JUDICIAL)、EvidenceService.java:78(updateSeal extend=false)。decide() 的 else 分支只有 `c.setStatus("DECIDED")`（:571）；close()（CaseService.java:674-719）与 archiveTerminated 全程不碰 case_evidence。属实。
3. 没有任何定时任务或触发器在 seal_expire_at 过期后清 sealed —— sealed 只降不升的唯一出口就是上面三处，普通处罚案件走到 DECIDED 后确实成为死锁：数据永久 sealed=true。
4. BureauStatsController.java:194-197 sealExpiring 只有 `where ev.sealed = true and ev.seal_expire_at < current_date + 5`，无案件状态过滤；同方法 correctOverdue:124-128 明确写了 `and cf.status not in ('CLOSED','TERMINATED')`。口径不一致属实。holdOverdue:86-89 同样缺过滤，且 disposeHold（EvidenceService.java:51）用的是同一个 requireActive，缺陷同构。

【报告的一处错误】"前端无任何按钮可以消解"不成立。CaseDetailView.vue:132 就有 `<el-button ... @click="onSeal(row, false)">解除</el-button>`，且没有 :disabled。真实症状不是"没按钮"，而是"按钮在，点了报 2044「案件当前状态（DECIDED）不允许该操作」"——用户看得见操作却被守卫拒绝，比没按钮更容易被当成系统故障来报障。这也意味着修复只需放宽后端守卫，前端零改动。

【报告漏掉的更重分支】SUSPENDED 也不在 requireActive 白名单里。案件中止调查期间（第46条，可能持续数月）封存到期同样无法解除，而第31条封存上限 30 日是硬期限——中止期间封存超期比结案后留一行看板僵尸行更接近真实违法。同理 disposeHold 在中止期间也调不动，而第26条先行登记保存只有 7 个工作日。

【概率与后果】
概率：条件触发但一旦触发即确定性发生——只要案件登记过 sealed=true 的证据、且办案人没在案件仍处 INVESTIGATING/REPORTED/NOTIFIED 时点过"解除"，走普通处罚/不予处罚出口就 100% 锁死。且时间窗对用户不利：封存 30 日上限 vs 办案期限 90 日，绝大多数案件在决定作出前封存早已到期，看板从第 25 天起就开始报，全靠办案人自觉去点解除；漏点一次就是永久。报告估算的 400 条/年是拍的（取决于本局实际用不用封存开关），但关键不是绝对数量而是结构：这批行零衰减、只增不减，三年后必然把真正需要处置的封存淹没。
后果：数据错误 + 体验劣化，且数据错误是主要的。已归档多年的案卷在 case_evidence 里仍记录"强制措施处于生效中"，与第31条（不超过30日、可延长一次）、第33条（不需要时应当及时解除）正面冲突；这类记录还会同时呈现 sealed=true 且 seal_expire_at 早已过期的自相矛盾状态，复议应诉调卷或上级卷宗评查时是硬伤。看板失效是次生后果。

【改动风险与收益】收益明显大于风险，但两侧的性价比不同：
(a) 放宽 EvidenceService.updateSeal(extend=false) 与 disposeHold 的守卫到"未归档即可"（照 crossExam:88-89 的 `"CLOSED".equals(status) || archiveNo != null` 同口径）——首选，风险最低。它只放行"解除/处理"这一个方向的写操作，不放行 extend（延长是调查阶段措施，第31条，必须仍受 requireActive 约束，报告这一点分寸拿捏正确），改动 2 行，前端按钮立即可用，且解除动作仍由办案人显式作出、可审计，法律语义比系统代劳干净。顺带把 SUSPENDED 一并解开。
(b) close()/decide() 转 DECIDED 时比照 terminate() 批量 `update case_evidence set sealed=false, register_hold=false where case_id=?`——作为兜底可加在 close()，语义是"归档时强制措施终了"，与 terminate():256 同形，风险也低。但不建议放在 decide() 的 else 分支：决定作出时案件尚未送达执行，此刻代当事人"解除封存"缺乏第33条依据，且会掩盖办案人本该显式作出的解除行为；放在 close()（案卷终态）更站得住。
(c) 看板补 `and cf.status not in ('CLOSED','TERMINATED')` 于 :194-197 与 :86-89——必须做，因为 (a)(b) 都救不了存量已归档案件的脏数据，这是唯一能让现有僵尸行消失的手段，且与同方法 correctOverdue 对齐，纯 SQL 谓词无副作用。
唯一需要一并处理的是存量数据：建议随修复出一条 Flyway 清理（对 status in ('CLOSED','TERMINATED') 或 archive_no is not null 的案件把 sealed/register_hold 置 false），否则库里的历史错误记录仍在，只是被看板过滤器遮住——而"错误数据"恰恰是这个 bug 最实质的危害。
另注：updateSeal 解除时只写 sealed=false，不落解除日期/操作人，case_evidence 也无对应字段；第33条口径上"解除封存"应当有据可查。这属于本 bug 相邻的记录完整性缺口，可视为同批附带评估，不必强绑。

### 25. [MEDIUM] 安全门禁一旦回归就不是失败而是挂死 6 小时，吃掉月度 Actions 额度的 18%
*工程与性能 — `.github/workflows/ci.yml:139`*

**现状/复现**：ci.yml:132-146「拒绝默认密钥启动 pilot（安全门禁）」这一步写的是 `if java -jar server/target/ybcase-server-*.jar --spring.profiles.active=pilot --server.port=8092 > insecure.log 2>&1; then echo 预期启动失败但成功了; exit 1; fi`。这一步依赖“java 进程会自己退出”。门禁本身回归时（有人放宽了 BureauServerApplication/配置里的默认密钥检查，或把属性名从 ybcase.security.jwt-secret 改掉导致检查读不到值），服务会正常起来并**永不返回**——8092 端口空闲、DB 环境变量都已喂好、pilot 配置完整，没有任何东西会让它退出。命令没有 timeout，三个 job 也都没有 timeout-minutes（grep 全文件只有 runs-on，无 timeout-minutes），于是这一步一直阻塞到 GitHub 默认的 360 分钟 job 上限。

**影响**：门禁失效时 CI 的表现不是“红色 + 明确报错”，而是一次 6 小时后被强制取消的 run，日志停在这一步没有任何结论——恰好是最需要看清楚的那次反而看不出原因。成本上：单次 360 分钟，占仓库自述在意的 2000 分钟/月免费额度的 18%；若同一天多推几次（concurrency 只在有新 push 时才取消上一轮），一天就能把月度额度打掉大半。e2e job 前面已经跑了构建+118 步 E2E+pilot 校验，这些分钟也一并作废。

**修法**：给命令加硬超时并把超时当作门禁失败：`if timeout 90 java -jar server/target/ybcase-server-*.jar --spring.profiles.active=pilot --server.port=8092 > insecure.log 2>&1 || [ $? -eq 124 ]; then echo "预期启动失败但成功/挂起了：默认 JWT 密钥竟可用于 pilot"; tail -30 insecure.log; exit 1; fi`（timeout 返回 124 表示进程活过了 90 秒，即启动成功，正是要拦的情况）。同时给 backend/frontend/e2e 三个 job 各加 `timeout-minutes: 25`，避免任何一步挂死后按 360 分钟计费。

> 核实：机制成立但影响被严重夸大，且一半复现场景是错的。

【成立部分】ci.yml:139-142 的命令确实无 timeout，且是 e2e job 最后一步；grep -rn "timeout" .github/ 只命中两处 postgres 的 --health-timeout，三个 job 均无 timeout-minutes，GitHub 也不支持仓库级默认超时。core/pom.xml:19 引入 spring-boot-starter-web，全仓无 System.exit/SpringApplication.exit/WebApplicationType.NONE，启动成功即 Tomcat 非守护线程阻塞、java -jar 永不返回。application-pilot.yml 的 ${YBCASE_DB_URL/USER/PASSWORD}/${YBCASE_JWT_SECRET} 被 ci.yml:134-138 全部喂好，8090 已在 114 行 kill、8091 占用、8092 空闲。所以门禁一旦失效，这一步确实挂到 360 分钟 job 上限，日志停在最需要看清的那步没有结论。最可能的触发路径反而是审阅者没写的第三种：有人轮换 JwtService.java:22 的 DEV_SECRET 常量值，CI 第 138 行导出的老串不再相等，门禁静默不触发 —— 一次看似无害的卫生性修改就能踩中。

【被证伪的影响】仓库是 PUBLIC（gh repo view 返回 isPrivate:false / visibility:PUBLIC，runs-on: ubuntu-latest 为 GitHub 托管标准运行器）。public 仓库标准运行器的 Actions 分钟免费且不计入配额，2000 分钟/月只对 private 生效。因此"吃掉月度额度 18%""一天打掉大半额度""前面的构建+E2E 分钟一并作废"全部不成立，实际零成本。360/2000=18% 算术没错但前提是假的——审阅者照搬了 ci.yml:15 那条注释，而该注释本身对 public 仓库就是误解。

【被证伪的复现场景】"把属性名从 ybcase.security.jwt-secret 改掉导致检查读不到值"恰恰不会挂：JwtService.java:27 的 @Value("${ybcase.security.jwt-secret}") 无默认值，改名即占位符解析失败、IllegalArgumentException 秒级 fail-fast，if 不触发，落到 ci.yml:143 的 grep -q "拒绝以开发默认 JWT 密钥启动"，失配后打印原因+tail -30+exit 1。这条路径现有代码已完整覆盖，正是那个 grep 的价值。

【部分缓解】concurrency.cancel-in-progress: true (ci.yml:16-18) 在同 ref 新 push 时取消上一轮，实践中把 6 小时从典型值降为最坏值，非保证。

【修复】建议的 timeout 90 ... || [ $? -eq 124 ] 三分支逻辑推演正确（if 条件内 set -e 失效不影响）；唯一小坑是合法门禁失败需跑完 Flyway，90s 在慢跑器上偏紧，建议放宽到 120s 与上面 pilot 校验的 60×2s 等待对齐。真实收益是可诊断性（90 秒红叉+insecure.log 尾部，而非 6 小时无结论的 timeout），不是成本。成本论证应整条删除。严重度从 bug 降为 CI 卫生项，两行改动 + timeout-minutes: 25 值得做。

> 核实：REAL — confirmed by reading the code, with one sub-claim overstated and one correction to the proposed fix.

MECHANISM VERIFIED AT EVERY LINK:
1. ci.yml:139-142 runs `java -jar` in the FOREGROUND — contrast lines 100 and 120-121, which both use `nohup … &`. A Spring Boot app with embedded Tomcat holds non-daemon threads and never returns.
2. The gate is a constructor throw in core/src/main/java/cn/ybcase/core/security/JwtService.java:30-33, guarded by `DEV_SECRET.equals(secret) && activeProfiles.contains("pilot")`. If that condition stops matching, the bean constructs normally and the context comes up.
3. Decisive evidence that pilot genuinely boots in this environment: the immediately preceding step (ci.yml:112-130) starts the identical jar, identical profile, identical DB env vars, and asserts a healthy /actuator/health on 8091. The only difference in the gate step is the secret's VALUE. application-pilot.yml needs only YBCASE_DB_URL/USER/PASSWORD/JWT_SECRET, all of which lines 134-138 supply. So "gate doesn't fire -> app stays up forever" is not speculative.
4. Confirmed no `timeout-minutes` anywhere (grep over .github/workflows returns only the three `runs-on`), and no `needs:` between jobs — so even if another job went red from the same regression, nothing cancels the hung e2e job. It blocks to GitHub's 360-minute default.

PROBABILITY — I found a second, MORE likely trigger the report did not name: the dev secret literal is duplicated in three unbound places (JwtService.java:22, application.yml:35, ci.yml:138), and there is NO unit test on this gate at all (only GuardIntegrationTest and WorkdaysTest exist). Rotating a dev default that has been sitting in public git history is routine hygiene; update the two source files and miss ci.yml:138 and the gate is working perfectly while the CI probe is stale — 6 hours burned anyway. This CI step is also the SOLE detector of the gate, which raises the stakes on it being legible when it trips.

ONE SUB-CLAIM OVERSTATED: the "属性名改掉" path does NOT hang. `@Value("${ybcase.security.jwt-secret}")` has no default, so a rename leaves an unresolvable placeholder, the context fails fast, and line 143's grep yields a clean red ("启动失败原因不是密钥门禁"). Already handled. Does not affect the other two paths.

CONSEQUENCE: not data corruption and not user-facing — this is CI reliability, cost, and diagnosability. The important part is that the failure mode INVERTS the signal: the one run that most needs to be legible (security gate regressed) is the one that produces no verdict at all, just a forced cancellation 6 hours later with logs stopped mid-step.

COST FRAMING CHECKS OUT AND IS NOT HYPOTHETICAL HERE: git log shows commit e284b8e — "CI 瘦身…（免费额度 2000 分钟/月已耗尽的应对）". This repo has already exhausted its quota once, and a quota that CAN be exhausted implies a private repo (public repos get unlimited minutes). 360/2000 = 18% is correct arithmetic.

RISK/BENEFIT: fix is near-zero risk — purely additive to CI, touches no production code, and is a no-op when the gate works (the JVM exits in seconds, far under any sane timeout). The proposed `timeout … || [ $? -eq 124 ]` construct is logically correct: exit 0 -> then-branch (fail), 124 -> then-branch (fail), gate-fired nonzero -> falls through to the grep. `timeout` returns 124 even though pilot's `server.shutdown: graceful` handles SIGTERM.

CORRECTION TO THE PROPOSED FIX: `timeout 90` is too tight. The healthy-boot poll directly above allows up to 120s (60 × 2s), and here a timeout is INTERPRETED AS GATE FAILURE — so an undersized value turns a slow runner into a false "security gate failed" alarm, the worst possible thing to make flaky. Use ~150-180s. Same caution on `timeout-minutes: 25`: derive it from observed run durations with headroom, since the e2e job does build + 118-step E2E + two pilot boots before reaching this step.

ADJACENT (not part of the claim): the 8091 pilot process from lines 120-121 is never killed, unlike the 8090 one at line 114.

## 可优化 1 条

### 1. [MEDIUM] 案卷目录接口把每份文书的 text 正文全量返回，而调用方只用得到类型/标题/日期
*其余服务 — `server/src/main/java/cn/ybcase/bureau/service/DocumentService.java:107`*

**现状**：archiveEntries 的查询把 case_document.content（text）也选了出来，archiveCatalog 原样放进 catalog 返回。GET /api/bureau/cases/{id}/archive-catalog 的唯一消费者 CaseDetailView.vue:724-731 只渲染 seq / doc_type / title / made_at 四列，从不读 content；close() 与 archiveTerminated 调 missingRequiredDocs 时也只是把结果折成 doc_type 的 Set。改造前该接口的查询是 select id, doc_type, title, made_at, maker, signed（3be5e73 的 diff 可见），本轮随 archiveEntries 抽取一并带上了正文。

**收益**：一个已结案件常有 15-30 份文书，询问笔录/现场笔录/调查终结报告单份即数 KB 到几十 KB，目录响应从约 3KB 涨到数百 KB；同一份数据在 close() 开启 archive_completeness_required 时还要在结案事务里再拉一遍。CaseService.buildDetailSql 对 documents 已经明确“正文另有详情接口，避免把全部全文一次拉回”，此处与该口径相反。

**做法**：给 archiveEntries 加 boolean withContent 参数（或拆出 archiveEntriesMeta）：archiveCatalog / missingRequiredDocs 走不含 content 的列清单，只有 FormalizationController 的 archive-full（合成打印确实要正文）传 true；虚拟卷内件同理，目录场景下 content 置空即可。
