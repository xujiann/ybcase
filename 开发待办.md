# 开发待办（第八轮摸底，2026-08-29）

五维并行摸底 + 每条经反驳式核实（证伪四问：其实已实现？属实施期配置？需外部凭证？现在不值得做？四问皆不成立才留下）。原始工作流 `wr9rsy7wl`。

**已排除、不要再提**：对外公示渠道真实对接（需机构凭证）、信创真机验证（无金仓/达梦环境）、8 类文书正文（须局方正式文本）、真实裁量基准与法律依据文本（须局方提供）、CaseDetailView 拆分（纯重构，零组件测试下不做机械重排）。

## 已完成（a2fa57a / 3be5e73）

### [HIGH] ~~执行/催缴阶段完全无跟进：缴款逾期、该催告、强执申请3个月时限都要办案人自己记台账~~
*办案闭环*

**为什么是缺口**：决定书送达那天起，系统就不再替办案人计时了。缴款期（payment_days，默认15日）届满没缴，没有任何清单、消息或看板告诉他；他必须自己在 Excel 里记「X案X月X日到期」，隔几天去财政票据系统对一次账。更要命的是行政强制法53条的3个月申请期：ExecutionService.applyCourtEnforce 会在你点下去时拦住「已超过缴款期满起3个月」，但没有任何东西提前提醒你还剩几天——等撞上这个拦截，钱已经收不回来了，而这正是全流程里唯一一个「错过就作废、且金额最大」的期限。催告书（URGE_LETTER）同理：系统只在申请强执时校验有没有、够不够10日，从不主动提示「该做催告书了」。

**证据**：server/src/main/java/cn/ybcase/bureau/web/BureauStatsController.java:56-145 督办看板 13 类预警（clueOverdue/reviewOverdue/holdOverdue/caseNearDeadline/deliveryOverdue:79/installmentOverdue:99/sealExpiring:137 等）无一条与「已送达未缴清」「催告」「强执申请时限」相关，唯一涉及钱的 installmentOverdue 只覆盖已批准分期的案件；server/src/main/java/cn/ybcase/bureau/service/MessageService.java:44-108 定时提醒只生成 线索核查/办案期限(第66-78行，且 status 限 INVESTIGATING/REPORTED/NOTIFIED，DELIVERED 案件被排除)/待批审批/反馈自动关闭 四类；server/src/main/java/cn/ybcase/bureau/service/ExecutionService.java:121-142 三道时限守卫（缴款期届满、催告满10日、3个月内申请）全是「点了才报错」的被动拦截。

**范围**：无需迁移。BureauStatsController.supervision() 增 2-3 段 SQL（已送达且 delivered_at + payment_days < today 且 case_execution 累计未达决定金额；已有 URGE_LETTER 且满 court_urge_days 未申请强执；缴款期满 3 个月临期），复用 ExecutionService.sum() 与 BureauConfig 参数口径避免第二套缴款期；MessageService.generateReminders() 加一段推给 owner_user；SupervisionView.vue 加对应面板。约 1 天。

**核实结论**：四条证伪路径全部不成立。(a)未实现：payment_days 全仓只出现在 ExecutionService.lateFeeQuote(第98行，需办案人手点"加处罚款测算"按钮才算)和 applyCourtEnforce(第126行，点了才拦)两处，无任何主动计时；MessageService.generateReminders 四类提醒中办案期限那段 status 限 INVESTIGATING/REPORTED/NOTIFIED，DELIVERED 案件被明确排除；WorkbenchController.workbench 的 myCases 虽含 DELIVERED，但取的是 deadline_at(办案期限)+扣除期，与缴款期无关，办案人看到的是一个早已过期的无意义日期。(b)非配参数：payment_days/court_urge_days 已是参数，缺的是拿这两个参数去算清单的代码，改数据不产生任何提醒。(c)无外部依赖：URGE_LETTER 模板 V19:62 已内置，全部数据在库内。(d)值得做且必须现在做：行政强制法53条的3个月是全流程唯一"错过即作废、且标的最大"的失权期限，撞上 ExecutionService:141 那句拦截时罚款已永久收不回；这是不可逆法律后果，不是效率问题。额外发现使本项范围比候选描述更大：SupervisionView.vue 只渲染 6 个面板，后端已算的 govRecordMissing/installmentOverdue/hearingOpinionOverdue/summaryRecordOverdue/assistOverdue/correctOverdue/publishOverdue 七类前端完全不显示——所以新增后端 SQL 若不同时补前端面板等于白做，且这七类现有预警本身也处于"算了没人看见"的状态，宜一并修。

### [HIGH] ~~督办看板后端算 13 类预警，前端只渲染 6 类——另外 7 类法定时限超期在系统里任何地方都看不到~~
*前端可用性*

**为什么是缺口**：局方最看重的就是「哪些法定时限快到/已超」。后端每次打开督办页都实打实跑了 13 条 SQL，其中 7 类结果被前端整段丢弃：处罚决定公开超期（辽56条 7日内公开）、简易程序备案超期（第51条）、协查逾期未复函（第34条 15日）、听证意见超期（辽50条 2日）、分期到期未缴（第54条）、重大处罚未报政府备案（辽54条）、责令改正逾期未报告。这 7 类也不在 @Scheduled 期限提醒里（MessageService.generateReminders 只覆盖线索核查/办案期限/待批审批单/反馈自动关闭四类），所以既不会进站内消息、也不在工作台、更不在督办看板——办案人员和督办岗只能靠自己记。用户会卡住的路径：决定书作出后第 8 天没公开，系统数据里明确知道超期，但督办岗打开督办看板从头翻到尾看不到这一条；直到上级检查才发现。

**证据**：server/src/main/java/cn/ybcase/bureau/web/BureauStatsController.java:55-140 依次 put 了 clueOverdue / caseNearDeadline / reviewOverdue / deliveryOverdue / holdOverdue / govRecordMissing / installmentOverdue / hearingOpinionOverdue / summaryRecordOverdue / assistOverdue / correctOverdue / publishOverdue / sealExpiring 共 13 个 key；frontend/src/views/case/SupervisionView.vue:8-52 只绑定了 d.clueOverdue、d.reviewOverdue、d.holdOverdue、d.caseNearDeadline、d.deliveryOverdue、d.sealExpiring 六个，其余 7 个 key 在整个 frontend/src 下无任何引用；server/src/main/java/cn/ybcase/bureau/service/MessageService.java:47-107 的提醒生成只有线索/办案期限/审批单/反馈四段，未覆盖这 7 类。

**范围**：纯前端。SupervisionView.vue 现在 83 行，按现有 el-card + el-table 模式补 7 个卡片（数据已在同一个响应里，无需新接口、无需改后端、无迁移）；顺带给能跳详情的几类补 @row-click。约 +70 行，1 个文件。

**核实结论**：证伪失败，逐条核实全部属实。BureauStatsController.supervision() 的 13 个 key 已复核（clueOverdue/caseNearDeadline/reviewOverdue/deliveryOverdue/holdOverdue/govRecordMissing/installmentOverdue/hearingOpinionOverdue/summaryRecordOverdue/assistOverdue/correctOverdue/publishOverdue/sealExpiring），SupervisionView.vue 只绑了 6 个；对另 7 个 key 在 frontend/src 全量 grep，零命中。MessageService.generateReminders() 确只有线索/办案期限/待批审批单/反馈自动关闭四段。WorkbenchController.workbench() 只返回 myCases/myClues/myApplications/pendingForMe/unread，无一覆盖这 7 类；StatsView、OversightController 亦无对应列表。(a)不成立；(b)不成立——正则/绑定写死在前端，非参数；(c)不成立——无外部依赖；(d)不成立——这 7 类的写入侧全部已通；CaseDetailView 有「公开（7日内）」onPublish、「备案登记」onGovRecord、「简易程序备案」onSummaryRecord、协查复函、分期登记缴纳，即预警可被消解、不会永久刷屏，数据是真实可动的。唯一需要修正的措辞：协查/分期在案件详情页内是能看到 due_at 的，所以不是「任何地方看不到」，而是跨案聚合的督办视角看不到——但督办岗的价值恰在跨案，且本条正是给 LEADER/LEGAL 的专用页。改动为纯前端、同一响应内已有数据、无新接口无迁移，性价比最高。

### [HIGH] ~~审批单只显示「申请理由」，payload 全不渲染——负责人是在看不到立案审批表内容、看不到延期天数的情况下点「批准」的~~
*前端可用性*

**为什么是缺口**：biz_approval.payload 里装着整张立案审批表（当事人、类别、案由、执法事项、程序类型、涉案金额、违法行为终了日、两名办案人员及执法证号）或延期天数，pending() 用 select a.* 已经把它查出来发给前端了，ApprovalsView 的表格却只有 单号/类型/关联/申请理由/申请人/申请时间 六列。后果：①第17条要求立案经负责人审查批准，负责人点「批准」时屏幕上只有一行「立案申请：XX医院」，看不到案由是哪条、涉案金额多少、办案人员是谁够不够两人、行为终了日有没有超追责时效，批下去 ApprovalService.execute 直接建案；②EXTEND 单更直接——第45条首次延期上限 30 日，负责人却看不到申请人填的 days 是 30 还是 90，只能盲签；③批准时若撞上 create() 的守卫（2001 类别与案由不符 / 2002 执法人员不足两人 / 2051 超追责时效），负责人拿到一句错误提示但无从修改 payload，只能驳回让申请人重填，而驳回理由框里他连原始参数都没看见。同样地，案件详情页已经把本案全部审批单（含已批准的立案审批表、批准人、批准意见、驳回理由）随 detail 拉了下来，却只用来算了个 PENDING 计数标签，整个审批留痕在案件里查不到。

**证据**：server/src/main/java/cn/ybcase/bureau/service/ApprovalService.java:97-105 pending() 为 select a.*（含 payload）；frontend/src/views/case/ApprovalsView.vue:7-27 表格列中无 payload，:52-58 onDecide 直接弹 prompt 收意见就提交；执行逻辑见 ApprovalService.java:112-121（FILE_CASE 用 payload 直接 convertValue 建案、EXTEND 取 payload.days）；案件侧 server/.../CaseService.java:709 DETAIL_PARTS 含 "approvals|biz_approval|id desc"，前端 frontend/src/views/case/CaseDetailView.vue:765 只 filter 出 PENDING，:74-75 只渲染成一个计数 el-tag；GET /bureau/cases/{caseId}/approvals（FormalizationController.java:95-98）在 frontend/src 下无任何调用方。

**范围**：纯前端。ApprovalsView 加可展开行或详情抽屉，按 kind 渲染 payload（FILE_CASE 用 el-descriptions 列当事人/案由/金额/办案人员，EXTEND 直接把天数提到列上）；CaseDetailView 加一个「审批留痕」tab 复用已有的 detail.approvals。两个文件约 90 行，无后端与迁移改动。

**核实结论**：证伪失败，且比描述更严重。ApprovalService.pending() 为 select a.* 已带回 payload；ApprovalsView.vue 六列表格（单号/类型/关联/申请理由/申请人/申请时间）确无 payload，onDecide 直接 prompt 收意见即提交。payload 内容已确认：CasesView.vue:188-190 提交 FILE_CASE 时是 payload: { ...form }，即整张立案表单原样打包，而 reason 只是模板串 `立案申请：${form.partyName}`——所以负责人屏幕上真的只有当事人名字，看不到案由/金额/办案人员/行为终了日；ApprovalService.execute() 却用这份 payload 直接 objectMapper.convertValue 建案。EXTEND 同理：CaseDetailView.vue:903 payload 为 { days }，负责人看不到是 30 还是 90，而 CaseService.extend 用它顺延期限。这与第17条「立案应当经负责人审查批准」的留痕本意直接冲突——批准记录在案，被批准的内容却从未呈现给批准人。案件侧亦复核：DETAIL_PARTS 含 approvals，CaseDetailView.vue:765 只 filter PENDING 算计数标签，GET /bureau/cases/{caseId}/approvals 在 frontend/src 无调用方。(a)-(d) 均不成立，纯前端改动，无迁移。


## 待办

> 说明：「节假日表跨年」被可运营性与数据完整性两个视角各报了一次（下方 MEDIUM 两条），是同一件事，合并做即可——真正的开发内容只有"缺失年份时系统主动告警"这一层，批量导入与年份筛选是附带便利。

### [HIGH] ~~终止调查/移送司法的案件永远无法结案归档（第47条+第56-57条断链）~~（已完成 3be5e73）
*法规覆盖*

**为什么是缺口**：terminate() 把案件置为 TERMINATED 终态，decide() 遇 TRANSFER_JUDICIAL（骗保案最常见出口之一）也自动置 TERMINATED；但 close() 只接受 DECIDED/DELIVERED，archive_no 只在 close() 里赋值。结果：一切终止调查的案件——第42/47条五类终止情形、移送司法机关的——都拿不到案卷号、没有结案报告、卷内目录永远显示“（结案时生成）”、合成打印封面印“（未结案）”。第57条一案一卷对这批案件只能线下手工立卷。更糟的是督办看板与工作台都用 status not in ('CLOSED','TERMINATED') 过滤，这批案件从所有监督视图里彻底消失，无人再管其卷宗去向。schema 里 close_reason 注释预留的第四种情形 OTHER 代码从未产出，正是这个缺口的化石。

**证据**：server/src/main/java/cn/ybcase/bureau/service/CaseService.java:247-258（terminate 置终态）；同文件:561-566（TRANSFER_JUDICIAL 自动 TERMINATED）；同文件:645-651（close 仅允许 DECIDED/DELIVERED，否则 2011）；同文件:681（archive_no 只在 close 里生成）；server/src/main/resources/db/migration/V2__bureau_case.sql:60（close_reason 注释含 OTHER，代码只产出 EXECUTED/COURT/NO_NEED）；frontend/src/views/case/CaseDetailView.vue:62,68（结案按钮只在 DECIDED/DELIVERED 出现）；server/src/main/java/cn/ybcase/bureau/web/BureauStatsController.java:128 与 WorkbenchController.java:37（TERMINATED 被排除出监督视图）

**范围**：close() 增加 TERMINATED 分支（close_reason 取 TERMINATED/JUDICIAL），结案报告与必备文书清单按“终止类”另设一组（无 NOTICE/DECISION，改为 TERMINATE_DECISION+移送函），前端在 TERMINATED 状态放开结案按钮。close_reason 已是 varchar，无需迁移。改动集中在 CaseService.close/DocumentService.REQUIRED_* 与一个 Vue 模板块，加 2 步 E2E。约 1 天。

**核实结论**：证伪失败，四条抗辩都不成立。核实：CaseService.java:645-651 close() 白名单只有 DECIDED/DELIVERED，且第643行先 decisionRepository.findByCaseId().orElseThrow(2011)——terminate() 路径根本没有 case_decision，即使放开状态白名单也仍被决定书前置拦死，是双重死锁；archive_no 全仓只在 :681 赋值一处（grep 确认）；close_reason 的 OTHER 在 V2:60 注释里存在而代码只产 EXECUTED/COURT/NO_NEED。V19 已经种了 TERMINATE_DECISION 模板、ARCHIVE_ORDER 也已含该类型，说明作者留了终止类卷宗的位置却没接上出口——不是设计取舍，是断链。

但候选的两处论据要打折，不影响结论只影响范围：(1) 「从所有监督视图彻底消失」不成立——CaseController.list/CaseService.pageList(:801-805) 不过滤状态，终止案件在案件列表里正常可见；BureauStatsController:128 与 WorkbenchController:37 排除终态案件是正确行为（终态无期限可预警、无待办可派），不该改。(2) 「只能线下手工立卷」也夸大——archiveCatalog 与 archive-full 都不校验状态，终止案件能打出卷宗，只是封面印「（未结案）」、目录无案卷号、无结案报告。真正的硬缺口是：终止类案件拿不到案卷号、产不出结案报告、close_reason 永远缺一类，第57条一案一卷对这批案件无法在系统内闭合。骗保案 TRANSFER_JUDICIAL 自动进 TERMINATED（:561-566）意味着这不是边角情形。改动面小（close 增分支 + 必备清单另设一组 + 前端一个 v-if），约 1 天，值得做。

### [HIGH] ~~卷内目录与卷宗合成打印只收 case_document，全部结构化法定材料（立案审批表/法制审核意见/集体讨论/听证/送达回证）不进卷~~（已完成 3be5e73）
*法规覆盖*

**为什么是缺口**：archiveCatalog 与 archive-full 都只查 case_document 表。而立案审批表存在 biz_approval（ApprovalService 自己的注释就写着“第17条立案审批表精神推广到全部负责人批准动作”）、法制审核意见在 case_review、集体讨论在 case_meeting、听证笔录在 case_hearing、送达回证在 case_delivery——这五类恰恰是案卷评查和复议应诉时最先被翻的必备件，没有一件会出现在卷内目录或合成打印里。办案人员打完“卷宗”还得回各页面逐个截屏/抄录再手工插页重编页码，合成打印等于没解决立卷问题。齐全性检查 REQUIRED_NORMAL_PUNISH 只查 4 类文书，缺立案审批表、法制审核意见、送达回证也判“齐全”，给出的是虚假保证。

**证据**：server/src/main/java/cn/ybcase/bureau/web/FormalizationController.java:279-287（archive-full 只取 case_document + doc_signature）；server/src/main/java/cn/ybcase/bureau/service/DocumentService.java:78-96（archiveCatalog 只查 case_document）；同文件:26-34（ARCHIVE_ORDER 与 REQUIRED_NORMAL_PUNISH 只有 4 类必备）；frontend/src/views/case/CaseDetailView.vue:929-958（打印全程只用 d.documents）；server/src/main/java/cn/ybcase/bureau/service/ApprovalService.java:14（自述立案审批表落在 biz_approval）；server/src/main/resources/db/migration/V9__v11_formalization.sql:27-41（biz_approval 与 case_document 无任何关联）

**范围**：在 DocumentService.archiveCatalog 与 FormalizationController.archiveFull 把 biz_approval(APPROVED)/case_review/case_meeting/case_hearing/case_delivery 按固定模板渲染为虚拟卷内件（title+content+made_at），并入 ARCHIVE_ORDER 排序与页码；必备清单补 FILING_APPROVAL/LEGAL_OPINION/DELIVERY_RECEIPT。前端打印复用同一数组无需改结构。不动 schema。约 1.5-2 天含 E2E 断言卷内目录含立案审批表。

**核实结论**：证伪失败。核实：DocumentService.archiveCatalog(:81-96) 与 FormalizationController.archiveFull(:283-284) 各自只 select case_document；前端 onPrintArchive(CaseDetailView.vue:929-958) 只遍历 d.documents，目录与页码全由该数组生成。全仓 case_document 写入点仅 4 处（CaseService:178 通用接口、:294 FINAL_REPORT、:669 CLOSE_REPORT、ProcedureService:236 E_DELIVERY_CONSENT），biz_approval/case_review/case_meeting/case_hearing/case_delivery 无任何一条会写进 case_document——V9:27-41 确认 biz_approval 与 case_document 无关联。

抗辩(a)「已有手工路径」只部分成立：DELIVERY_RECEIPT/MEETING_RECORD/HEARING_RECORD 在 V2:103 枚举和 ARCHIVE_ORDER 里有位置，办案员可手工再录一份，但 doc_template 全表只有 NOTICE/DECISION/FINAL_REPORT/SUSPEND_DECISION/RESUME_NOTICE/TERMINATE_DECISION/PRESERVE_DECISION/SEAL_DECISION/URGE_LETTER/TRANSFER_LETTER，这三类连模板都没有，等于纯手工重录已在库里的结构化数据；FILING_APPROVAL/LEGAL_OPINION 则连 doc_type 都不存在，无论如何进不了卷。抗辩(c)「须局方正式文本」不成立——本条渲染的是系统已采集的结构化字段（审批人/意见/时间、审核意见类型、讨论结论、听证意见、送达方式与签收日），不是八类文书正文。REQUIRED_NORMAL_PUNISH(:32-34) 只查 4 类确实给虚假齐全保证（虽然 archive_completeness_required 默认 false，目录页的 missing 列表始终展示）。

施工时须处理一个候选没提的设计点：手工已录的 DELIVERY_RECEIPT/MEETING_RECORD/HEARING_RECORD 与虚拟卷内件会重复上目录，需按 (doc_type, 来源记录) 去重或以手工件优先。这是已建成功能（合成打印+齐全性检查）达不到设计目的的缺陷，值得做。

### [HIGH] 人员变动（离职停用、执法证到期）没有闭环：停用不拦、找不出待交接案件、提醒发进死账号
*可运营性*

**为什么是缺口**：一个市局一年必然发生几次人员调动。现状：管理员停用离职人员账号时，系统不检查他名下还有多少在办案件（第45条 90 日期限还在跑）；要交接，只能一件一件进案件详情点"移交"，而案件列表只能按状态和案号/案名/当事人关键词筛，压根没有"按承办人"这个筛选条件——负责人没有办法列出"张三名下 12 件在办"这张清单，只能靠记忆。更糟的是期限提醒仍按 owner_user 发送且不判 enabled（同一个方法里给负责人发审批提醒时反而判了 u.enabled=true），临期/超期消息全部投进已停用的信箱，无人接收，直到超期。执法证同理：cert_expire_at 只在立案和增加办案人员那一刻校验，证到期前无任何预警，看板 13 项里也没有这一类；办案员上午还在办案，下午立案被 2055 硬拦才发现证过期了，而他名下在办案件里没有一处提示。

**证据**：core/src/main/java/cn/ybcase/core/web/SysUserController.java:87-95 setEnabled 只置 enabled+token_version，无在办案件校验；server/src/main/java/cn/ybcase/bureau/service/CaseService.java:784-805 pageList 只支持 status 与关键词，无 owner 过滤；server/src/main/java/cn/ybcase/bureau/web/CaseController.java:51-57 transfer-owner 只能逐案，入口仅在 frontend/src/views/case/CaseDetailView.vue:11；server/src/main/java/cn/ybcase/bureau/service/MessageService.java:70-76 按 owner_user 发提醒不判 enabled（对比同文件 :87 审批提醒判了 u.enabled=true）；server/src/main/java/cn/ybcase/bureau/service/ProcedureService.java:33-49 执法证只在立案/加人时校验；frontend/src/views/system/EnforcersView.vue:11-14 仅对已过期标红，无临期

**范围**：1) 停用账号前置校验：名下有在办案件则返回业务码并回带清单，提示先移交；2) 案件列表加 owner 查询参数 + 管理端"按承办人批量移交"接口（复用 transferOwner，循环加消息）；3) MessageService 期限提醒 join sys_user 过滤 enabled，停用人的案件改推给其部门负责人；4) 督办看板加一项"执法证 30 日内到期且有在办案件"。改 4 个文件 + 1 个前端页，1-2 天。

**核实结论**：四条子主张逐条复核全部成立，且这批里唯一含现网可复现缺陷的一条。(1) `core/.../SysUserController.java:85-96` setEnabled 只置 enabled 与 token_version，无在办案件校验。(2) `CaseService.java:784-805` pageList 只支持 status + 关键词(case_no/name/party_name)，且 `frontend/src/views/case/CasesView.vue:15-31` 表格连"承办人"列都没有，`CasesView.vue:151-154` 只传 page/size/status/q——负责人在界面上既筛不出也看不见谁承办；全仓 owner_user 只出现在 CaseService 范围 SQL、MessageService、WorkbenchController:37(仅查自己)，确无任何按承办人聚合的清单或统计。(3) 真 bug：`MessageService.java:64-80` 办案期限提醒按 owner_user 直发、不 join sys_user 判 enabled，而同文件 :32-38 sendToRole 与 :85-91 审批提醒都判了 `u.enabled = true`——同一个类里两套标准，停用账号的临期/超期消息进死信箱无人看；线索提醒发 created_by(:52-61) 同样不判。(4) `ProcedureService.java:31-51` validateEnforcer 只在立案/加办案人员时调用，看板 13 项无执法证临期，`EnforcersView.vue:11-15` 仅对已过期标红。(a)-(c) 均不成立，(d) 也不成立：市局人员调动一年必发生数次，而现状是"停用不拦 + 列不出名下案件 + 提醒进死账号"三重叠加，第45条 90 日期限仍在跑。四项里 (3) 是纯 bug 应立即修，(1)(2) 是交接闭环的必要件，(4) 可作为看板第 14 项顺手加。

### [HIGH] 案件要素录错后没有任何更正通道，只能直连数据库改，且改了不留痕
*数据完整性*

**为什么是缺口**：立案表单一次性写入的当事人名称、统一社会信用代码、地址、法定代表人、联系方式、案情摘要、涉案金额、案由，之后全仓没有任何接口可以改。这些字段不是展示用的：DocumentService.render 把它们全部灌进 {{partyName}}/{{partyCreditNo}}/{{partyAddress}}/{{amountInvolved}}/{{causeText}} 占位符，也就是告知书、决定书、终结报告、结案报告的正文。触发序列很常见：立案时把统一社会信用代码敲错一位或当事人名称多打一个字→当天就发现→现在只有三条路：(a) 走终止程序作废案件重新立案，白占一个案号且卷宗留一份"终止"记录；(b) 带着错误一路做到决定书送达，之后靠更正决定书补救；(c) 找运维直接 update case_file——而这条路会绕过 sys_audit_log（审计只记 method/path，不记字段前后值），事后谁改了什么、经谁批准完全不可追溯，这恰恰是执法全过程记录（局令第4/35条）最不能接受的。案由录错更严重：案由决定法律依据和案件名称，且立案时校验过 partyType 与 cause.subjectType 一致，事后没有任何再校验的口子。

**证据**：全仓只有一个 PutMapping：server/src/main/java/cn/ybcase/bureau/web/FormalizationController.java:116（文书模板）；server/src/main/java/cn/ybcase/bureau/web/CaseController.java 全部 26 个端点无一个更新案件要素的接口（:53 transfer-owner 只改承办人）；写入点 server/src/main/java/cn/ybcase/bureau/service/CaseService.java:87-96；消费点 server/src/main/java/cn/ybcase/bureau/service/DocumentService.java:48-58；前端佐证 frontend/src 全部 client.put/patch 调用只有 depts/roles/users/config/doc-templates 五处（无 cases、无 clues）。

**范围**：中等偏小。加一张 case_correction 更正记录表（case_id、字段名、原值、新值、更正理由、操作人、时间）+ 一个 PUT /api/bureau/cases/{id}/party-info 接口：白名单字段、要求填更正理由、按 @Version 乐观锁更新 case_file 并同步写更正记录，状态守卫限 INVESTIGATING/REPORTED（已进入告知或已决定的走既有更正决定书路径，直接拒绝并提示）；前端在案件详情基本信息区加"更正"按钮和一个更正记录列表。约 1 张迁移 + 1 个 service 方法 + 1 个前端弹窗。

**核实结论**：证伪全部失败。全仓唯一的 PutMapping 是 FormalizationController.java:116（文书模板），grep PutMapping|PatchMapping 只此一处；CaseController.java 全部端点确认无案件要素更新接口（:53 transfer-owner 只改 owner_user）。写入点 CaseService.java:86-99 一次性落 partyName/partyCreditNo/partyAddress/partyLegalRep/partyContact/summary/amountInvolved/causeId，此后无写路径。消费面比候选说的还宽：DocumentService.java:48-58 把这些字段灌进告知书/决定书/终结报告/结案报告正文；更关键的是 CaseService.java:84 用 partyName 拼出 c.setName(partyName + "涉嫌" + category + "案")，而这个 name 被 FormalizationController.java:281 的卷宗合成 archive-full 当封面案件名直接带出——当事人名录错一个字，整册卷宗封面就是错的，且无任何改法。现有唯一系统内出路 CaseService.java:247-258 terminate：要 LEADER/ADMIN 角色 + 法定情形理由，还会连带解除全部查封扣押（:253），为改一个错字作废案件明显不成比例，且白占一个案号。绕过路径（直连 update case_file）会完全躲开审计：BureauAuditFilter.java 只按 method/uri 留痕，不记字段前后值，这正是第4/35条全过程记录最不能接受的。文书在线编辑只能救文书正文，救不了案件表、检索、统计和封面。范围也确实小（1 迁移 + 1 白名单 PUT + 前端弹窗）。

### [HIGH] 线索列表硬顶 200 条、无分页无检索，案件列表已有分页而线索没跟上
*数据完整性*

**为什么是缺口**：线索是只增不减的流水：举报、智能监控批量拉取（/clues/fetch-monitor 一次可批量建多条）、Excel 批量导入、日常检查转线索，且终态 FILED/REJECTED 永久保留。前端只有一个状态下拉，后端固定 findTop200ByOrderByIdDesc，没有 page/size/关键词参数。触发条件：全库线索超过 200 条时（一个地级市按每年 100-500 条线索估，第 1-2 年内必然到），第 201 条以前的线索在线索页上就再也翻不到了——按状态筛选也只是各状态各取最近 200 条，翻页控件根本不存在，界面上不会有任何"还有更多"的提示，用户看到的是一个安静的截断。实际会撞上的场景：查"某定点药店历史上被举报过几次"（同一主体反复出问题是医保稽核的常规动作）、复核去年某条线索的核查结论、迎检时按时间段调线索台账。唯一的兜底是全局搜索，但那条也只 limit 20 且必须先知道关键词，不能按时间段或状态浏览。对照组就在同一个仓库里：案件列表已经实现了 pageList（total/page/size/rows + 案号/案名/当事人 ilike 检索 + 数据范围过滤），线索这条路没跟上。

**证据**：server/src/main/java/cn/ybcase/bureau/web/ClueController.java:23-27 —— list 只接 status，直接返回 findTop200；server/src/main/java/cn/ybcase/bureau/repository/CaseClueRepository.java:10,13 —— 两个 findTop200 方法，注释自己写了"线索终态只增不减"；frontend/src/views/case/CluesView.vue:106 —— 只传 status，无 page/size，页面无分页组件；对照实现 server/src/main/java/cn/ybcase/bureau/service/CaseService.java:784-806（pageList 已有 count + limit/offset + ilike 检索）；全局搜索兜底 server/src/main/java/cn/ybcase/bureau/web/FormalizationController.java:228-232 limit 20。

**范围**：小。照搬 CaseService.pageList 写一个 ClueService.pageList（status + 关键词 ilike clue_no/suspect_name/content + 受理日期区间 + count/limit/offset），ClueController.list 加 page/size/q/日期参数并保持不传 page 时的旧契约（E2E 已依赖数组返回），CluesView 加 el-pagination + 搜索框。建议顺带给 case_clue(received_at) 或 (status, deadline_at) 补索引——V18 已有 clue status 索引，检索列需确认。无数据迁移。

**核实结论**：逐条核实全中：ClueController.java:22-26 list 只接 status，直接返 findTop200ByOrderByIdDesc / findTop200ByStatusOrderByIdDesc；CaseClueRepository.java:10-13 只有这两个 findTop200 方法，无任何分页方法；CluesView.vue:103-107 只传 status，页面无 el-pagination；对照 CaseService.java:784-806 pageList 已经把 count + ilike 检索 + limit/offset + 数据范围过滤全写好了，照抄即可。四条证伪都不成立。而且比候选估的"第 1-2 年内必然到"更早：GoLiveController.java:27-40 的 /clues/import 单批就允许 500 条（if rows.size() > 500 才拒），智能监控疑点导入跑一次批量就直接把列表打穿到只剩最近 200 条——不是几年后的问题，是接监控数据当天的问题。截断完全无声：前端不会显示总数也没有翻页控件，用户看到的就是"一共这些"。兜底不够用：FormalizationController.java:222-232 全局搜索对线索是 limit 20 且要求关键词 ≥2 字符，无法按时间段或状态浏览台账。改动量小、模式现成、复用已有实现，性价比在四条里最高。

### [MEDIUM] 决定被复议/诉讼撤销、变更、确认违法后无任何处置通道，已公示的违法决定撤不回来
*法规覆盖*

**为什么是缺口**：全仓 grep“复议/诉讼”只命中决定书模板正文一处与一条测试注释——系统对救济阶段零承载。publish() 是单向的，且第624-625行显式拒绝二次登记（为防洗白超期），因此一旦登记公开就再无撤回或更新入口；公示导出固定 where d.published = true，被复议机关撤销的决定会永远出现在此后每一次公示导出里，直接违反行政处罚法第48条/局令46条“三日内撤回或更新公开信息”。同时没有复议应诉台账：案件 CLOSED 后被起诉，答复期限、答复书、随案移送的证据材料目录全在系统外，办案人员只能拿 archive-full 手工整理，且系统里看不出这个案子正在被诉（继续按已结案统计）。

**证据**：server/src/main/java/cn/ybcase/bureau/service/CaseService.java:612-628（publish 单向，624-625 拒绝重复登记，无撤回/更新方法）；server/src/main/java/cn/ybcase/bureau/entity/CaseDecision.java:51-55（只有 published/publishedAt，无撤回状态）；server/src/main/java/cn/ybcase/bureau/web/OversightController.java:139-148（公示导出固定 published = true）；全仓“复议|诉讼”仅命中 server/src/main/resources/db/migration/V4__documents_archive.sql:133 与 server/src/test/java/cn/ybcase/bureau/WorkdaysTest.java:14

**范围**：新增一张 case_remedy 表（类型 REVIEW/LITIGATION、受理机关、对方案号、收到应诉通知日、答复期限、结果 UPHOLD/CHANGE/REVOKE/CONFIRM_ILLEGAL、结果文书号）+ 结果回写：撤销/变更时给 case_decision 加 published_withdrawn/withdrawn_at 并留痕，公示导出排除已撤回；答复期限接入现有 @Scheduled 提醒与督办看板；案件详情加一个“救济”页签。一张迁移 + 一个 Service/Controller + 一个前端页签。约 2-3 天。

**核实结论**：证伪失败，但应大幅收窄范围。核实：publish()(CaseService:612-628) 只 setPublished(true)，全仓无任何将 published 置回 false 或标记撤回的代码；CaseDecision.java:51-55 只有 published/publishedAt 两列；OversightController.publishExport(:139-148) 固定 where d.published = true，被撤销的决定会永久出现在每次导出里。:624-625 的幂等守卫（防洗白超期）确实把重新登记的门也一起焊死了——这是正确守卫的副作用，不是它的错，但意味着没有任何合法出口。行政处罚法第48条第2款「被变更、撤销、确认违法或无效的应在三日内撤回并公开说明理由」在系统里零承载，这是硬法律缺陷，且不属于「对外公示渠道对接（需凭证）」——导出是内部产物，不需要外部凭证。

收窄理由：候选把两件事捆成 2-3 天。第一件（决定加 published_withdrawn/withdrawn_at + 撤回原因留痕、导出排除已撤回、时间轴出事件）是上述法条的直接落地，半天量级，必须做。第二件「复议应诉台账+答复期限提醒+救济页签」是超出医保局令第4号（查办程序止于结案归档）的业务扩域，复议应诉通常由法规科另线办理，市级局年发生量个位数，答复期限进 @Scheduled 和督办看板的收益远低于成本。建议只做撤回回路 + 一张最小 case_remedy（类型/受理机关/对方案号/结果/结果文书号，用于把撤销结果回写决定并让已结案案件标出「在诉」），不做期限提醒与完整应诉工作流。按收窄后口径评中等价值。

### [MEDIUM] 管辖认定与指定管辖（第9-13条）在系统内零承载，立案三要件只落实两项
*法规覆盖*

**为什么是缺口**：全仓没有任何管辖字段或判定：case_file 从 V2 建表到 V21 的全部 alter 里没有一列与管辖相关；create() 的立案校验只做了案由主体匹配(2001)和执法人员两人持证(2002)，第15条第三项“属于本部门管辖”既无字段也无提示。医保案件的管辖规则恰恰特殊——违法行为发生地为原则、异地就医按就医地/参保地分流——是复议诉讼中最常被攻的程序点，现在这个判断只存在于办案人员脑子里和线下立案审批表上，卷宗里查不出任何管辖依据。case_transfer 只有 OUT/IN 两种移送方向，管辖争议协商与报请共同上一级指定管辖（第11-12条）无处登记，只能线下发函、批复文件放抽屉。注意：系统开发计划.md 的完工记录声称“第二章条文除上下级视图外全部关闭”，与代码不符。

**证据**：server/src/main/resources/db/migration/V2__bureau_case.sql:33-66 及 V3-V21 全部 alter table case_file（共 7 列新增，无一与管辖相关）；server/src/main/java/cn/ybcase/bureau/service/CaseService.java:56-95（立案只校验 2002/2001/时效，无管辖要件）；server/src/main/resources/db/migration/V5__procedure_complete.sql:21-33（case_transfer 仅 OUT/IN，kind 仅 ADMIN/JUDICIAL/MSA，无指定管辖/管辖争议）；全仓“管辖|就医地|参保地|发生地|指定管辖”仅命中 V5:20 注释与 frontend/src/views/case/TransfersView.vue:4 文案

**范围**：case_file 加 jurisdiction_type（OCCUR 发生地/TREAT 就医地/INSURED 参保地/DESIGNATED 指定管辖）+ jurisdiction_basis 说明 + designated_doc_no 批复文号；立案表单加选择器与依据提示，写入立案审批表 payload 并进卷内目录（与第2条合流）；case_transfer 的 kind 扩两种（DISPUTE 管辖争议协商 / DESIGNATE 报请指定）。一张小迁移 + 立案表单与移送台账两处前端。约 1 天。

**核实结论**：事实核实全部属实，但候选对项目文档的指控要修正。核实：case_file 从 V2:33-66 到 V21 的全部 alter 无一列与管辖相关；create()(CaseService:56-95) 只校验 2002 两人持证、2001 案由主体匹配、2051 追责时效，第15条第三项「属于本部门管辖」既无字段也无提示；V5:21-33 case_transfer 的 direction 仅 OUT/IN、kind 仅 ADMIN/JUDICIAL/MSA，无管辖争议协商与报请指定管辖；全仓「管辖|就医地|参保地|发生地」只命中 V5:20 注释、TransfersView.vue:4 文案、ADR-0001:13 与计划书表格。

修正一处：候选称「系统开发计划.md 完工记录声称第二章条文全部关闭，与代码不符」——计划书第29-34行的第二章表格本身仍标着 ⬜（第9条管辖判定辅助、第10-12条移送与管辖）且写明「三期」，第40行第15条那行也明写「管辖要件待三期补判定」；只有第189行那句概括性完工语与表格自相矛盾。所以文档主体是诚实的，是那一句概括写过头了，不构成「文档撒谎」。

仍判 real：抗辩(b)不成立（字段不存在，加列加表单是开发工作，不是实施期填数据）、(a)(c)不成立。但价值下调为中：市级局绝大多数案件是本地发生地管辖，选择器多数时候是常量输入，系统也无法真正校验管辖真伪，收益本质是留痕/举证，真正有医保特异性的只有异地就医的就医地/参保地分流。且它天然应与第2条合流（管辖依据写进立案审批表 payload 并随之进卷），不宜作为独立一项排期——单独做完而第2条没做，管辖依据照样进不了卷内目录。case_transfer 扩两个 kind 是枚举加宽，近乎零成本，可顺手带上。

### [MEDIUM] 卷内目录只编文书，证据材料和附件不进目录、不编号——证据卷的编排必须在 Excel 上手做
*办案闭环*

**为什么是缺口**：归档时办案人拿到的是一份只列文书的目录。案卷里体量最大的证据材料（书证、视听资料、执法记录仪录像、笔录扫描件）既没有卷内序号（证1、证2…），也不出现在目录和打印件里。一案一卷的目录要交档案室，办案人只能把证据部分抄到 Excel 里手工编号排序，再打印出来夹进卷。齐全性检查也只校验 4 类文书是否存在，对「证据是否都归了卷」完全无感知。

**证据**：server/src/main/java/cn/ybcase/bureau/service/DocumentService.java:78-96 archiveCatalog 只 select case_document，ARCHIVE_ORDER（第26-30行）也只枚举 doc_type，REQUIRED_NORMAL_PUNISH（第32-33行）只校验 FINAL_REPORT/NOTICE/DECISION/CLOSE_REPORT 四类；frontend/src/views/case/CaseDetailView.vue:930-958 卷宗合成打印的 toc 与 pages 均由 d.documents 生成，证据与附件不参与；server/src/main/resources/db/migration/V2__bureau_case.sql:82-98 case_evidence 无序号/页码列，后续迁移（V3 只加质证两列、V4 只加 case_attachment.evidence_id）也未补。

**范围**：新迁移 V22 给 case_evidence 加 seq_no（卷内证据序号，按 case_id 唯一）；archiveCatalog 返回改成「文书卷 + 证据卷（含挂在证据下的 case_attachment）」两段编排；CaseDetailView 打印目录按两段渲染，齐全性检查可选加「有决定但零证据」提示。约 0.5-1 天。

**核实结论**：四条全不成立。(a)未实现，逐条复核无误：DocumentService.archiveCatalog 只 select case_document 并按 ARCHIVE_ORDER(全是 doc_type)排序编 seq；CaseDetailView.vue onPrintArchive 的 toc 与 pages 均由 d.documents 生成，证据与 case_attachment 完全不参与；case_evidence(V2__bureau_case.sql:82-98)无序号/页码列，V3 只加质证两列、V4 只加 case_attachment.evidence_id，确未补。REQUIRED_NORMAL_PUNISH 只有四类文书，"有决定但零证据"无感知。(b)非配数据：ARCHIVE_ORDER 和必备清单都是 Java 常量不是配置表，改不了。(c)无外部依赖：卷内目录的编排格式是通用档案要求，不需要局方正式文本。(d)值得做：卷宗合成打印是本系统少有的、输出物要交到系统外部（档案室归档、行政执法案卷评查）的成果，而证据材料是一案一卷里体量最大的部分，现在整块缺失；成本又是四条里最低的（加一列 seq_no + 目录分两段 + 打印分段渲染，0.5-1 天）。降半档的理由：页码那一列仍无解——扫描件页数系统不知道，case_attachment 无页数字段，所以生成的证据段目录仍需人工补页码，不是 100% 免手工，价值 medium 而非 high。

### [MEDIUM] 立案之前没有任何地方放材料：行政检查现场照片/录音/手写笔录只能留在手机和微信里
*办案闭环*

**为什么是缺口**：附件接口整体挂在 /api/bureau/cases/{id} 下，case_attachment.case_id 是 not null，所以在案件存在之前上传不了任何东西。行政检查在系统里只有一个自由文本 result 字段，线索只有 content 和 verify_result 两段文字。现场检查那天拍的照片、执法记录仪视频、当场手写的现场笔录，从检查日到立案日（线索核查法定 15 个工作日，可延）全程无处安放，办案人只能存手机、发微信群、拷 U 盘；立案后再补传时，取证时间与保管链条已经断了。更糟的是未发现违法或不予立案的检查——这类材料在系统里永远只剩一行文字，检查→线索的「闭环」传过去的也只是 result 字符串拼成的一句话。

**证据**：server/src/main/java/cn/ybcase/bureau/web/AttachmentController.java:23 @RequestMapping("/api/bureau/cases") + :38 @PostMapping("/{id}/attachments") + :51 先校验 case_file 存在，全系统仅此一处上传入口；server/src/main/resources/db/migration/V4__documents_archive.sql:34-45 case_attachment.case_id not null references case_file；server/src/main/resources/db/migration/V6__inspection_reward_discretion.sql:4-18 inspection 表只有 result text，无笔录/附件关联；server/src/main/java/cn/ybcase/bureau/service/OversightService.java:59-73 inspectionToClue 仅把 result 字符串拼进线索 content；server/src/main/resources/db/migration/V2__bureau_case.sql:14-30 case_clue 亦无附件关联。

**范围**：迁移 V22：case_attachment.case_id 放开为空并加 owner_kind(CASE/CLUE/INSPECTION)+owner_id（或新建通用 biz_attachment），补索引；AttachmentController 抽出通用上传/下载并按 owner 类型各自鉴权（线索按 created_by/handler，检查按 officers/created_by）；inspectionToClue 与 ClueService.markFiled 时把附件 owner 迁移到新线索/新案件。含权限与迁移约 1.5-2 天。

**核实结论**：四条全不成立。(a)未实现，已穷举确认：全仓 MultipartFile 只出现在 AttachmentController 一个文件，@RequestMapping("/api/bureau/cases") + 第51行先查 case_file 存在，case_attachment.case_id not null references case_file(V4:34-45)；前端 el-upload 只在 CaseDetailView.vue:855 一处，CluesView.vue 与 InspectionsView.vue 均无任何上传控件；inspection 表只有 result text，case_clue 只有 content/verify_result，都无附件关联。立案前确实无处存放。(b)(c)不适用。(d)值得做：医保线索绝大多数来自投诉举报与智能监控，举报信扫描件、微信截图、监控明细在法定 15 个工作日核查期内无处安放，是每天都撞的事而非边缘场景；不予立案的核查结论也需要留存支撑材料。降到 medium 而非 high 的两点：其一，对最终立案的案件，材料事后仍能以真实 obtained_at 补登进 case_evidence + 附件，进入正式案卷的完整性并未丢失，真正不可逆的只有"未立案的检查/线索"这一支；其二，候选提出的通用 owner_kind/owner_id 多态方案是四条里风险最高的改动——放开线上表的 not null 外键、新建一套鉴权面（线索/检查目前完全不在 CaseScopeInterceptor 的案件中心口径里）、还要在 inspectionToClue 与 markFiled 时迁移归属，在七轮审阅已收敛、pilot 已部署的节点上引入回归风险。建议按窄口径落地（case_attachment 加可空 clue_id/inspection_id 并把 case_id 放开，鉴权分别按 created_by/handler 与 officers/created_by 判定），把 1.5-2 天压到 1 天并大幅降低迁移面。

### [MEDIUM] 「模板生成」入口只放行 6 种文书模板（库里有 14 种），且要求用户手打英文枚举码——催告书/中止决定书/先行登记保存决定书等 8 种模板从 UI 完全无法到达
*前端可用性*

**为什么是缺口**：onRenderTpl 用 ElMessageBox.prompt + 硬编码正则收文书类型，用户必须照着提示文字手打 NOTICE / DECISION / FINAL_REPORT / ORDER_CORRECT / CLOSE_REPORT / ASSIST_LETTER 之一，打错（比如打「决定书」或小写 notice）直接「类型无效」退出。而 doc_template 表里实际有 14 种模板，V19 迁移的注释明确写着补这些骨架是因为「此前 render 这些类型会抛 2053，办案员只能手工录入且不进齐全性检查」——结果前端正则没同步，SUSPEND_DECISION / RESUME_NOTICE / TERMINATE_DECISION / PRESERVE_DECISION / SEAL_DECISION / URGE_LETTER / TRANSFER_LETTER / ACCEPT_RECEIPT 这 8 种至今无路可走。具体卡住场景：案件到 DELIVERED、当事人逾期未缴，办案人员点「申请法院强制执行」；行政强制法54条要求先下催告书，URGE_LETTER 模板就在库里，但他在「模板生成」里打 URGE_LETTER 会被正则拒绝，只能整篇手敲。同理证据页勾了「先行登记保存」「封存」却生成不了对应决定书。另有 ORDER_CORRECT / TRANSFER_LETTER / ACCEPT_RECEIPT 三个键在 labels.ts 的 DOC_TYPE 里也没有，所以「制作文书」的类型下拉里选不到，文书列表里还会直接显示英文原码。

**证据**：frontend/src/views/case/CaseDetailView.vue:818-826（prompt + inputPattern 只允许 6 种）；模板全集见 server/src/main/resources/db/migration/V4__documents_archive.sql:105、V9__v11_formalization.sql:82、V19__matrix_gaps.sql:11,61，共 14 个 doc_type；后端 GET /bureau/doc-templates（server/.../FormalizationController.java:108-111）已返回全部模板行、TemplatesView 已在用；server/src/main/java/cn/ybcase/bureau/service/DocumentService.java:41 未命中即抛 2053；frontend/src/views/case/labels.ts:26-33 的 DOC_TYPE 缺 ORDER_CORRECT/TRANSFER_LETTER/ACCEPT_RECEIPT（CaseDetailView.vue:423 那个只对 ORDER_CORRECT 显示的「改正期限」字段因此在「制作文书」路径下永远出不来，而 BureauStatsController.java:125-128 的责令改正逾期预警正是靠这个 due_at）。

**范围**：纯前端。把 CaseDetailView.vue:818-826 的 prompt 换成一个下拉弹窗，选项由已有的 GET /bureau/doc-templates 拉取、用 DOC_TYPE 映射中文名（后端零改动）；labels.ts 补 3 个键。约 40 行，2 个文件。

**核实结论**：主体属实但两处细节需更正，故降一档。已核实：doc_template 共 14 个 doc_type（V4/V9/V19 抽取比对），CaseDetailView.vue:818-821 的 inputPattern 只放行 6 种，不在名单的 8 种（SUSPEND_DECISION/RESUME_NOTICE/TERMINATE_DECISION/PRESERVE_DECISION/SEAL_DECISION/URGE_LETTER/TRANSFER_LETTER/ACCEPT_RECEIPT）确实无法走模板渲染；GET /bureau/doc-templates（FormalizationController:110）无 @PreAuthorize，办案员可读，「后端零改动」成立。最有力的实锤是 ExecutionService.java:135 的报错原文让用户「请先制作催告书（文书类型 URGE_LETTER）」，而模板入口的正则恰恰拒收 URGE_LETTER——系统自己指的路自己堵死。更正一：这 8 种并非「完全无法到达」，labels.ts 的 DOC_TYPE 已含其中 5 种（URGE_LETTER/SUSPEND_DECISION/RESUME_NOTICE/PRESERVE_DECISION/SEAL_DECISION/TERMINATE_DECISION），「制作文书」下拉能选中并手工录入，所以强执前置不是硬阻断，只是整篇手敲；真正一个选项都没有的是 TRANSFER_LETTER/ACCEPT_RECEIPT/ORDER_CORRECT 三种。更正二：候选说 ORDER_CORRECT 的「改正期限」字段永远出不来是错的——ORDER_CORRECT 在模板正则的放行名单里，走「模板生成」会把 documentForm.docType 置为 ORDER_CORRECT，v-if 生效、due_at 可填，correctOverdue 预警的数据源并未断。(c) 不成立：「8类文书正文须局方正式文本」是已排除项，但本条改的是入口路由不是正文，骨架已在库、TemplatesView 可由局方在线核定正文。

### [MEDIUM] 作出决定/结案前无前置条件清单：办案人员填完整张表单才被守卫逐条弹回，一次决定要来回四五轮
*前端可用性*

**为什么是缺口**：decide() 在 NOTIFIED 分支上串了 8 道守卫（2007 金额不得高于告知、2075 听证未举行、2076 陈述申辩期未届满、2005 法制审核未通过、2045 证据未质证、2047 未集体讨论、2062 缺裁量理由、2008 超办案期限），前端 NOTIFIED 状态下只是无条件亮出「作出决定」按钮。实际路径：办案员点开决定弹窗、选类型、填三笔金额、写完决定内容 → 提交 → 「尚有 3 份证据未经当事人质证」→ 关弹窗、切到证据 tab、逐条质证 → 再提交 → 「较大数额罚款应当经负责人集体讨论决定」→ 找集体讨论按钮录入 → 再提交 → 「陈述申辩期至 2026-09-05 届满前不得作出处罚决定」→ 只能等。每轮都要重填一次表单（onDecide 失败不关弹窗但 openDecide 只预填金额）。更关键的是，触发这些守卫的阈值全在 sys_config 里而 GET /api/config/all 是 ADMIN 独占，办案员连「罚多少会触发集体讨论/法制审核」「本局有没有开质证强制」都无从知道，只能靠撞。结案同理：onCloseCase 让用户在一行 prompt 里写完结案报告才提交，然后可能被「罚没款项未执行完毕」或 2052 必备文书缺失弹回（而必备文书缺失清单其实藏在文书 tab 的「案卷目录」按钮后面，跟结案按钮不在一处）。

**证据**：server/src/main/java/cn/ybcase/bureau/service/CaseService.java:468-542 decide() 的守卫链（2007/2075×2/2076/2005/2045/2047/2062/2008）；:645-676 close() 的 2011/2052 链；前端 frontend/src/views/case/CaseDetailView.vue:47-53 NOTIFIED 分支只有裸按钮、无任何前置提示，:1312-1316 onCloseCase 直接 prompt 收报告；阈值参数不可读见 server/src/main/java/cn/ybcase/bureau/web/BureauConfigController.java:22-27（/api/config/all 标了 @PreAuthorize("hasRole('ADMIN')")）；可用素材已在手上——detail 返回的 evidences.cross_exam_at、meetings、reviews、notices.statementDeadline/hearingRequested、hearings.status、effectiveDeadline（CaseService.java:730-747）以及 archive-catalog 的 missing（DocumentService.java:90-96）。

**范围**：前端为主 + 一小段后端。前端在决定按钮旁渲染一份 checklist（质证 n/N、集体讨论有无签字、法制审核通过否、申辩期到期日、有效期限剩余），未满足项直接给出跳转，数据大部分来自已有的 detail；结案按钮旁复用 archive-catalog 的 missing 做同样提示。后端需要新开一个只读的 GET /bureau/cases/{id}/decide-preflight（或把几个阈值加进一个非 ADMIN 可读的白名单接口），把 meeting/legal_review 阈值、cross_exam_required、statement_wait_required 这几项按本案算好返回。约 1 个新接口 + CaseDetailView 约 80 行，无迁移。

**核实结论**：缺口成立但严重性被明显夸大，且提议的后端部分不该做，故降为 medium 并建议缩范围。属实的部分：CaseService.decide() 的 NOTIFIED 分支确有 2007/2075×2/2076/2005/2045/2047/2062/2008 守卫链，close() 有 2011/2052 链，而 CaseDetailView.vue:50-56 的 NOTIFIED 分支只有裸按钮、无任何前置提示，质证/集体讨论/法制审核/申辩期分散在四个 tab 里、与决定按钮不在一处。夸大之处有三，均已实测代码：①「每轮都要重填一次表单」为假——decisionForm 是组件级 reactive，只有 resetCaseLocalForms() 在切换案件时（watch route.params.id）才复位，提交失败 dlg.decide 不会置 false，关弹窗也不清空，数据全程留存；②「阈值无从知道」被守卫文案自己化解——2047 报「较大数额罚款（≥100000元）」、2045 报「尚有 N 份证据未经当事人质证」、2076 报出具体届满日、2008 报出有效期限，都是带数值和下一步动作的中文提示，靠错误撞出来虽笨但不盲；③结案侧压力小于描述——archive_completeness_required 默认 false，2052 通常根本不触发。另外「作出决定」按钮是 v-if=isLeader，撞守卫的是局领导不是办案员，叙事路径需改写。据此我反对候选提出的新增 GET /decide-preflight 或放宽 /api/config/all 权限：把 8 条守卫的判定逻辑在第二处重写，一旦与 decide() 漂移就会出现「清单全绿仍被驳回」或「清单误报阻断」这一新缺陷类，代价高于收益。值得做的是纯前端的那一半：用 detail 已返回的 evidences.cross_exam_at、meetings、reviews、notices.statementDeadline/hearingRequested、hearings.status、effectiveDeadline 以及 archive-catalog.missing，在决定/结案按钮旁渲染只读清单并给跳转，零新接口、零迁移、与守卫失配也只是提示不准而不会误阻断。

### [MEDIUM] 节假日表跨年缺失无任何检测，工作日期限静默退化为"只跳周末"
*可运营性*

**为什么是缺口**：系统 2027-01-01 一开机就撞上：sys_holiday 只有 2026 年数据，2027 年春节/国庆全被当成工作日。BureauConfig.plusWorkdays 查到空集不报错、不降级提示，直接按"只跳周末"算——送达 7 个工作日（第59条）、法制审核 10 个工作日（第40条）、简易程序备案 7 个工作日（第51条）、听证通知期全部算短，文书上白纸黑字写着错误的期限届满日，且督办看板会把没超期的案子报成超期、把超期的漏掉。整个过程没有一处提醒管理员"该维护明年节假日了"，操作手册里那句"每年初维护"是纸面约束，系统侧零兜底。加上录入方式是一天一条（一年约 30 条放假 + 若干调休），漏录概率很高。

**证据**：server/src/main/resources/db/migration/V4__documents_archive.sql:179-190 只种了 2026 一年（注释自认"示例，须按国办通知每年校核维护"）；server/src/main/java/cn/ybcase/bureau/service/BureauConfig.java:86-100 plusWorkdays 直接 select 全表，空集也照常返回结果，无"该年度无数据"判断；server/src/main/java/cn/ybcase/bureau/web/BureauStatsController.java:60-146 督办看板 13 类预警无"节假日表未维护"一项；frontend/src/views/system/HolidaysView.vue:1-30 只有单日"添加/更新"，无按年批量导入/上年复制

**范围**：1) 新增年度维护体检：任一期限推算会落到的年份（当年+次年）在 sys_holiday 无记录时，在督办看板与管理员站内消息各出一条告警（BureauStatsController.supervision 加一项 + MessageService.generateReminders 加一段，12 月起提前提醒）；2) POST /bureau/holidays/batch 批量录入（粘贴"日期,类型,名称"多行）+ HolidaysView 加年度筛选与批量框。约 1 个接口 + 2 处告警 + 1 个前端弹窗，半天到 1 天。

**核实结论**：证伪失败，四条出路都不成立。(a) 无实现：`server/src/main/java/cn/ybcase/bureau/service/BureauConfig.java:86-98` plusWorkdays 直接 `select day, kind from sys_holiday` 全表读入 Set，空集照常返回，全仓 grep sys_holiday 只命中 BureauConfig / BureauLawController(CRUD) / V4 迁移 / WorkdaysTest 四处，无任何年度体检、启动校验或健康检查；`BureauStatsController.java:55-140` 13 项预警逐条核对（clueOverdue/caseNearDeadline/reviewOverdue/deliveryOverdue/holdOverdue/govRecordMissing/installmentOverdue/hearingOpinionOverdue/summaryRecordOverdue/assistOverdue/correctOverdue/publishOverdue/sealExpiring）确无"节假日表未维护"；`HolidaysView.vue` 只有单日添加+删除，无年度筛选/批量。(b) 最有力的反驳是"填节假日是实施期数据活"——填数据确实是，但"没填时系统一声不吭"是代码缺陷，候选提的正是检测那一半，(b) 打不掉。(c) 国办放假通知是公开文本，不需要局方凭证。(d) 值得做但要缩水：真正吃工作日的只有四条路径（CaseService:313 法制审核 10 工作日、ClueService:36/51 线索核查 15 工作日、EvidenceService:44 先行登记保存 7 工作日、BureauStatsController:82/115 送达与简易备案两个过滤器，V3 里这几个 *_day_unit 默认确为 WORKDAY），候选说的"听证通知期"并不走工作日，属夸大；且漏 HOLIDAY 的方向是少跳假日→期限算得偏早→督办误报超期，而非候选说的"把超期的漏掉"（只有漏录 SHIFT_WORK 才偏晚），后果是保守性错误+文书日期错，不是漏管。建议只做督办看板+管理员消息的"次年节假日缺失"告警（12 月起提前），批量导入接口属可选便利（一年约 30 条，单日录入并非不可行）。

### [MEDIUM] 参数变更没有"改前→改后+操作人"历史，界面却宣称"修改留痕入审计"
*可运营性*

**为什么是缺口**：sys_config 里放的是 case_deadline_days、hearing_days、听证/告知期、较大数额罚款阈值、辽宁口径开关这类直接决定法定期限与守卫判定的值。改一次就全局即时生效（ConfigChangedEvent 清缓存），但系统只留下最新值：sys_config 无 updated_by，审计表只记 method/path/status——PUT /api/config/{key}?value=xx 的 value 在 query string 里，getRequestURI() 根本不含，所以审计里只有"某人 PUT 了 case_deadline_days"，改成了几、原来是几、几点生效全部丢失。一年后行政复议或案卷评查问"这个案子当时按 90 天还是 60 天算的"，系统答不出；同一个参数被人调过又调回来，更是完全无痕。项目里 doc_template 已经用 doc_template_history 解决过同一个问题，参数这条线漏了。

**证据**：core/src/main/java/cn/ybcase/core/web/SysConfigController.java:38-46 update 只 set cfg_value/updated_at 并发事件，不写历史、不记操作人；server/src/main/resources/db/migration/V1__bureau_core.sql:60-65 sys_config 无 updated_by 列；server/src/main/java/cn/ybcase/bureau/common/BureauAuditFilter.java:66-73 审计 insert 只有 username/method/path/http_status/client_ip/request_id；对照 server/src/main/resources/db/migration/V9__v11_formalization.sql:4 doc_template_history 已有同款模式；frontend/src/views/system/SettingsView.vue:4 标题写着"修改留痕入审计"

**范围**：V22 新增 sys_config_history(cfg_key, old_value, new_value, changed_by, changed_at)；SysConfigController.update 改为先取旧值再更新并插历史（同一事务）；SettingsView 每行加"变更历史"抽屉 + 一个 GET /api/config/{key}/history。1 个迁移 + 1 个控制器方法 + 1 处前端，半天。

**核实结论**：证伪失败。(a) 无实现：`core/src/main/java/cn/ybcase/core/web/SysConfigController.java:34-42` update 只 `update sys_config set cfg_value=?, updated_at=now()` 并发 ConfigChangedEvent，不写历史不记操作人；`V1__bureau_core.sql:60-65` sys_config 确无 updated_by；全仓 grep `config_history|cfg_history|updated_by` 零命中，而 `V9__v11_formalization.sql:4` 的 doc_template_history 证明同款模式项目里已有先例；`SettingsView.vue:4` 标题确实写着"修改留痕入审计"。(b)(c) 不适用。(d) 值得做，但候选把损失说过头了：`BureauAuditFilter.java:63-71` 用的是 getRequestURI()，确实不含 query string，但审计里仍留有 username + method + path(含 cfg_key) + created_at，所以"谁、何时、改了哪个参数"是有的，真正丢的只有旧值和新值——候选"改成了几、原来是几、几点生效全部丢失"三项里有两项站不住。另有一条更强的辩护：审计从不记任何请求体/参数（对所有接口一视同仁，避免落 PII），参数不是特例。但结论不变：cfg_value 是短、非 PII、且直接决定法定期限的值（case_deadline_days/hearing_days/较大数额阈值/辽宁口径开关），一改就对全部在办案件即时生效，复议或案卷评查时"当时按 90 还是 60 天"无从回答，专表留痕是对症的最小修法。顺带把 SettingsView 那句过度承诺的文案改准。

### [MEDIUM] 节假日表只种到 2026-10-07 且无一条调休补班记录，跨年后全部"工作日"法定期限静默算错，系统零提示
*数据完整性*

**为什么是缺口**：所有工作日口径的法定期限都读 sys_holiday：线索核查15工作日(ClueService)、法制审核10工作日(CaseService.submitReview)、先行登记保存7工作日(EvidenceService)、送达7工作日与简易备案7工作日(督办看板)。BureauConfig.plusWorkdays 在表里查不到目标年份时不报错、不告警，直接退化成"只跳周末"。触发条件精确到日：2027-01-01 零点起自动发生，无需任何人操作。具体错法两个方向同时存在——(a) 缺 HOLIDAY 行：2027 春节7天被当成工作日，2027-02-08 收到的线索正确期限约 3月8日，系统算成 2月26日，督办看板从 2/27 起把它标红成超期，办案人员被逼在法定期限内提前办结或补走延期审批；(b) 缺 SHIFT_WORK 行（现在种子里一条都没有，2026 年就已经错了）：调休上班的周六被当成非工作日不计数，期限被往后推，出现"实际已超法定期限而系统显示未到期"——这个方向直接导致漏报超期、错过第40条法制审核和第59条送达时限。管理员没有任何途径知道该维护：上线清单没写、无启动自检、无看板提示，HolidaysView 只能一天一条手工点。

**证据**：server/src/main/resources/db/migration/V4__documents_archive.sql:179-190 —— 种子注释自己写着"须按国办通知每年校核维护"，但只种了 2026-01-01 至 2026-10-07 共 20 条，kind 全是 HOLIDAY，SHIFT_WORK 一条没有；server/src/main/java/cn/ybcase/bureau/service/BureauConfig.java:86-99 —— plusWorkdays 把整表读进 Set 后交给 Workdays.plus，查不到就当没有假期，无任何缺失校验；依赖方：server/src/main/java/cn/ybcase/bureau/service/ClueService.java:36、server/src/main/java/cn/ybcase/bureau/service/CaseService.java:313、server/src/main/java/cn/ybcase/bureau/service/EvidenceService.java:44、server/src/main/java/cn/ybcase/bureau/web/BureauStatsController.java:75-84 与 :112-119；frontend/src/views/system/HolidaysView.vue:6-14 —— 单条日期选择器逐日添加，无批量导入、无年份覆盖提示；deploy/上线清单.md 全文无"节假日"字样。

**范围**：小。三处改动：(1) BureauLawController 增一个批量导入接口（接收一年的 [{day,kind,name}] 数组，走现有 on conflict do update + evictHolidays）；(2) 增一个覆盖度自检——启动时与督办看板/工作台各查一次 "当年及次年 sys_holiday 是否有数据"，缺失则在看板顶部和管理员消息里挂红条"XXXX 年法定节假日未维护，工作日期限计算将按仅跳周末近似"；(3) HolidaysView 加年份筛选（后端 /holidays?year= 已支持）+ 粘贴批量录入框。可选补一条 2027 种子迁移。不涉及数据迁移。

**核实结论**：代码事实核实：V4__documents_archive.sql:179-190 确实只有 20 条 2026 年 HOLIDAY 行、SHIFT_WORK 零条；全仓 sys_holiday 只在 V4 出现过 insert（其余 V5-V21 无补种）；BureauConfig.java:85-99 读整表进 Set 后交 Workdays.plus，Workdays.java:16-27 在集合为空时纯跳周末，无任何缺失年份告警；BureauLawController.java:39-49 只有单条 upsert，无批量接口；HolidaysView.vue 单条日期选择器，无年份筛选、无批量框。四条证伪路径都不成立但需修正候选的两处夸大：(a) 不成立——无任何覆盖度自检；(c) 不成立——自检与批量导入本身不需要局方文本（只有 2027 具体日期需要国办通知）；(b) 部分成立——2027 数据本身确属实施期填数据，且候选说"管理员没有任何途径知道该维护"是错的：docs/操作手册.md:37 写了"节假日表：每年初按国办通知维护（影响一切工作日计算）"，:47 的上线检查单里也列了"节假日表"这一项（只有 deploy/上线清单.md 没写）。所以真正的开发内容只剩"系统内强制"这一层：当前是纯流程约束，没写就静默降级，而 ClueService.java:36、CaseService.java:313、EvidenceService.java:44、BureauStatsController.java:75-84/112-119 全部依赖它算法定期限。缺 SHIFT_WORK 的方向更隐蔽——期限被往后推，督办看板不报警，属于漏报超期。判 real=true，但真正值钱的只有那个覆盖度红条自检（约 30 行），批量导入和年份筛选是锦上添花（一年 20 多条手工点也能忍）。

### [MEDIUM] 附件误传后无删除/作废通道；FILE 模式下即便改库删行也会留下永久孤儿文件
*数据完整性*

**为什么是缺口**：AttachmentController 只有上传、列表、下载三个动作，没有删除也没有作废。触发序列：办案人员在案件 A 的附件页误传了案件 B 的病历扫描件（含参保人姓名、身份证、诊疗记录），或者传错页数重传了一份——现在这两份都永久留在案件 A 的附件列表里，卷宗打印和归档时一并带出去。对当事人个人信息而言这是必须能删的（个人信息保护法删除权），对执法卷宗而言又不应当物理删除而应当"作废+留痕"，两种诉求现在一种都没实现。第二层问题在 FILE 模式：file_path 是服务端生成的路径，如果运维直接 delete from case_attachment 收场，磁盘上的文件没人删，200MB 的执法音像永久占着 attachments 卷；反过来如果只删文件不删行，下载会走到 AttachmentController.java:100 的"文件不存在"分支，列表里挂着一条永远点不开的记录。数据量增长后这些孤儿文件会持续吃掉备份体积（backup.sh 的 att-*.tar 保留 14 份，每份都带上这些垃圾）。

**证据**：server/src/main/java/cn/ybcase/bureau/web/AttachmentController.java:38（POST 上传）、:77（GET 列表）、:86（GET 下载）—— 全类无 DeleteMapping、无作废字段更新；表结构 server/src/main/resources/db/migration/V4__documents_archive.sql:35-46 无 voided/deleted 之类列；前端 frontend/src/views/case/CaseDetailView.vue:172-186 附件表格只有"下载"一列，无删除/作废按钮；孤儿文件分支见 AttachmentController.java:98-102。

**范围**：小。case_attachment 加 voided/void_reason/voided_by/voided_at 四列（迁移一条）；加 DELETE 或 POST /attachments/{id}/void 接口：要求填作废理由、限案件未结案、限上传人或负责人、写审计；list 查询默认过滤 voided=false（归档/卷宗合成同步过滤），管理员视图可看作废记录；FILE 模式下作废时同步把文件移到 attachments/_voided/ 或直接删文件但保留记录行。前端附件表加一列按钮。

**核实结论**：核心事实成立：AttachmentController.java 全类只有 :38 upload、:77 list、:86 download，无 DeleteMapping、无作废字段更新；表结构 V4:35-46 + V9:48-50 只加了 category/file_path，无 voided 类列；CaseDetailView.vue:181-186 附件表格确实只有"下载"一列。误传他案参保人病历扫描件后系统内零补救手段，这是真缺口。但候选的两条论据经核实站不住，须下调权重：(1)"卷宗打印和归档时一并带出去"是错的——DocumentService.archiveCatalog（:78-97）只查 case_document，FormalizationController.archiveFull（:279-287）只返回 caseFile/documents/signatures，附件根本不进卷宗目录和合成打印，误传件不会被打印进卷；(2) FILE 模式孤儿文件不是系统自身会产生的缺陷，必须先有运维手工 delete from case_attachment 才会出现，属于"没有正规通道所以逼人做 DB 手术"的二阶后果，不能单独计价。剩下的真实价值就是"作废+留痕+列表过滤"这一条基线能力（个人信息删除诉求 vs 执法卷宗留痕诉求，现在一条都没实现），范围确实小（4 列迁移 + 一个 void 接口 + 一列按钮），但紧迫度低于候选 2 和候选 4。

### [LOW] 审计日志在系统里查不了：接口没有页面、只能按用户名看最近 200 条，月导出还漏了 request_id 列
*可运营性*

**为什么是缺口**："局里的人出了问题怎么自查"这条路目前是断的。/api/audit/logs 存在但前端没有任何页面调用它，管理员在界面上唯一能做的是导出某个月的 CSV 拿去 Excel 里翻。接口本身也只支持 username 和一个 sensitive 开关，硬编码 limit 200、无时间范围、无路径/案件筛选、无分页——跑满一年后审计表几万条，按用户名取最近 200 条等于查不到上个月的事。最要命的是闭环断在最后一环：用户反馈直报特意记录了 requestId 用于对账，sys_audit_log 也有 request_id 列，但月度导出 CSV 的 select 里偏偏没带这一列，导出的表头是"id,用户,方法,路径,状态码,IP,时间"——办案人员报障给出的请求号，在唯一能拿到手的审计文件里根本对不上。

**证据**：frontend/src 全量 grep "audit" 只命中 frontend/src/views/system/SettingsView.vue:6-46 的月度导出，无任何页面调用 /api/audit/logs；core/src/main/java/cn/ybcase/core/web/AuditQueryController.java:21-40 仅 username+sensitive，order by id desc limit 200 写死；server/src/main/java/cn/ybcase/bureau/web/FormalizationController.java:244-252 导出 select 与表头均无 request_id，而 server/src/main/resources/db/migration/V11__feedback.sql:25 已给 sys_audit_log 加了 request_id、server/src/main/java/cn/ybcase/bureau/web/FeedbackController.java:153 反馈导出里带着它

**范围**：审计导出 select 与表头补 request_id（一行改动）；AuditQueryController 加 from/to、path 关键字、requestId、分页参数；新增"审计查询"管理员页面（表格+筛选，可复用 SettingsView 布局）并挂菜单。1 个控制器 + 1 个新 Vue 页面 + 1 条菜单迁移，半天到 1 天。

**核实结论**：事实全部属实，但价值要大幅下调。核实：`frontend/src/router/index.ts` 全部路由无审计页，全仓 grep audit 前端只命中 `SettingsView.vue:6-46` 的月度导出，`/api/audit/logs` 确为无人调用的死接口；`core/.../AuditQueryController.java:21-40` 仅 username+sensitive、`order by id desc limit 200` 写死、无时间范围无分页（且其自带注释已承认 sensitive 过滤路径是医院项目遗留、近乎失效）；`FormalizationController.java:244-252` 导出 select 与表头 "id,用户,方法,路径,状态码,IP,时间" 确无 request_id，而 `V11__feedback.sql:25` 加该列时的注释白纸黑字写着"用户反馈 ↔ 服务端记录对账的钥匙"，`FeedbackController.java:146-153` 反馈导出也确实带了它——一头带一头不带，闭环断在最后一环。但 (d) 对其中最大的一块成立：管理员"自查"这条路并没有断，月度 CSV 导出是完整全列全月数据、拿到 Excel 里筛是等保归档的正常姿势，新增一个审计查询页属便利而非缺口；且 V11 注释里给的定位路径本就是 request_id → 服务端日志直查，CSV 缺列并不致命。真正该做的只有两件小事：导出 select 与表头补 request_id（一行），AuditQueryController 补 from/to/path/requestId/分页把这个已暴露的接口从"查不到上个月"修成可用。新 Vue 页面建议砍掉或压到最后。
